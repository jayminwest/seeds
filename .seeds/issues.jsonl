{"id":"seeds-0tk","title":"Build infra files (version-bump.ts, CHANGELOG.md, README.md)","status":"closed","type":"task","priority":1,"createdAt":"2026-02-23T07:23:51.70417-08:00","updatedAt":"2026-02-23T07:28:04.077946-08:00","assignee":"jayminwest@gmail.com","description":"Create version-bump script, CHANGELOG, and README. See SPEC.md for exact definitions.","closeReason":"Infrastructure files created: version-bump.ts (atomic semver bump with rollback), CHANGELOG.md (Keep a Changelog), README.md (comprehensive project docs)","closedAt":"2026-02-23T07:28:04.077946-08:00"}
{"id":"seeds-5mn","title":"Fix 11 test failures from merge integration","status":"closed","type":"bug","priority":1,"createdAt":"2026-02-23T07:51:26.393379-08:00","updatedAt":"2026-02-23T07:58:05.446049-08:00","assignee":"jayminwest@gmail.com","description":"After merging 3 parallel builder branches, 11 tests fail due to API mismatches. Root causes: (1) id.test.ts passes arrays but generateId expects Set<string> — 7 failures, (2) tpl.test.ts expects different pour/status behavior — 3 failures, (3) init.test.ts expects idempotent init but impl rejects it — 1 failure. Fix either tests or implementations to be consistent. 100/111 tests pass.","closeReason":"All implementations merged to main. 111 tests pass, lint clean, typecheck clean.","closedAt":"2026-02-23T07:58:05.446049-08:00"}
{"id":"seeds-awf","title":"Infrastructure: CI workflows, version-bump script, CHANGELOG, README","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-23T07:21:42.575673-08:00","updatedAt":"2026-02-23T07:58:05.444867-08:00","assignee":"jayminwest@gmail.com","description":"Build project infrastructure from SPEC.md. Includes: .github/workflows/ci.yml (lint+typecheck+test), .github/workflows/auto-tag.yml (auto-tag+GitHub release on version bump), scripts/version-bump.ts (bump version in package.json + src/index.ts), CHANGELOG.md (Keep a Changelog format), README.md. File area: .github/**, scripts/**, CHANGELOG.md, README.md","closeReason":"All implementations merged to main. 111 tests pass, lint clean, typecheck clean.","closedAt":"2026-02-23T07:58:05.444867-08:00"}
{"id":"seeds-b0k","title":"CLI entry + all 12 command files (init/create/show/list/ready/update/close/dep/sync/blocked/stats/tpl/migrate)","status":"closed","type":"task","priority":1,"createdAt":"2026-02-23T07:29:46.615134-08:00","updatedAt":"2026-02-23T07:58:05.448522-08:00","assignee":"jayminwest@gmail.com","closeReason":"All implementations merged to main. 111 tests pass, lint clean, typecheck clean.","closedAt":"2026-02-23T07:58:05.448522-08:00"}
{"id":"seeds-bku","title":"Command tests + infrastructure (CI/CD, scripts, CHANGELOG, README, release.md)","status":"closed","type":"task","priority":1,"createdAt":"2026-02-23T07:29:47.549965-08:00","updatedAt":"2026-02-23T07:58:05.449771-08:00","assignee":"jayminwest@gmail.com","closeReason":"All implementations merged to main. 111 tests pass, lint clean, typecheck clean.","closedAt":"2026-02-23T07:58:05.449771-08:00"}
{"id":"seeds-l1q","title":"Core implementation: project config, core modules, all commands, CLI entry, tests","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-23T07:21:41.088435-08:00","updatedAt":"2026-02-23T07:58:05.442826-08:00","assignee":"jayminwest@gmail.com","description":"Build the entire seeds CLI source code from SPEC.md. Includes: package.json, tsconfig.json, biome.json, src/types.ts, src/yaml.ts, src/id.ts, src/config.ts, src/store.ts, src/output.ts, src/index.ts (CLI entry + command router), all 12 command files in src/commands/, and all 7 test files. Must pass bun test, bun run lint, bun run typecheck. File area: package.json, tsconfig.json, biome.json, src/**","closeReason":"All implementations merged to main. 111 tests pass, lint clean, typecheck clean.","closedAt":"2026-02-23T07:58:05.442826-08:00"}
{"id":"seeds-s4m","title":"Foundation: package.json, tsconfig, biome, core modules (types/yaml/id/store/config/output) + core tests","status":"closed","type":"task","priority":1,"createdAt":"2026-02-23T07:29:45.571757-08:00","updatedAt":"2026-02-23T07:58:05.447326-08:00","assignee":"jayminwest@gmail.com","closeReason":"All implementations merged to main. 111 tests pass, lint clean, typecheck clean.","closedAt":"2026-02-23T07:58:05.447326-08:00"}
{"id":"seeds-vyq","title":"Fix 11 test failures: id.test Set, tpl.test status nesting + prefix, init.test idempotent","status":"closed","type":"bug","priority":1,"createdAt":"2026-02-23T07:52:44.161923-08:00","updatedAt":"2026-02-23T07:56:40.802533-08:00","assignee":"jayminwest@gmail.com","closeReason":"Fixed 11 test failures: generateId accepts Set|array, init idempotent, tpl pour requires --prefix, tpl status nests JSON","closedAt":"2026-02-23T07:56:40.802533-08:00"}
{"id":"seeds-xmq","title":"Build CI/CD workflows (.github/workflows/ci.yml, auto-tag.yml)","status":"closed","type":"task","priority":1,"createdAt":"2026-02-23T07:23:50.541356-08:00","updatedAt":"2026-02-23T07:28:02.95785-08:00","assignee":"jayminwest@gmail.com","description":"Create GitHub Actions workflows for CI and auto-tagging. See SPEC.md for exact definitions.","closeReason":"CI/CD workflows created: ci.yml (lint+typecheck+test) and auto-tag.yml (version sync + idempotent tagging + GitHub release)","closedAt":"2026-02-23T07:28:02.95785-08:00"}
{"id":"seeds-9530","title":"Accept --desc as alias for --description in create and update","status":"closed","type":"bug","priority":2,"createdAt":"2026-02-24T03:00:30.934Z","updatedAt":"2026-02-24T03:00:33.375Z","description":"The --desc flag was silently ignored. Both create.ts and update.ts now accept --desc as a shorthand for --description.","closedAt":"2026-02-24T03:00:33.375Z","closeReason":"Fixed: both --desc and --description now accepted in create and update commands"}
{"id":"seeds-afd6","title":"Adopt Commander for CLI parsing","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:29:35.260Z","updatedAt":"2026-02-24T14:57:00.397Z","description":"Replace the manual process.argv parsing (switch statement in src/index.ts) with Commander. Benefits: structured arg/option definitions, auto-generated help with argument types and choices, built-in version display, .addHelpText() for detailed per-command docs. Each command in src/commands/ should export a register function that takes a Commander program instance. Business logic stays untouched — only the parsing/routing layer changes.","blocks":["seeds-f350","seeds-afbb"],"closedAt":"2026-02-24T14:57:00.397Z"}
{"id":"seeds-f350","title":"Adopt chalk for output formatting","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T03:29:39.506Z","updatedAt":"2026-02-24T15:07:12.805Z","description":"Replace raw ANSI escape codes in src/output.ts with chalk. Currently using manual TTY/NO_COLOR detection and c.green/c.red/c.bold/c.dim helper functions. Chalk handles terminal capability detection automatically. Migrate: c.green → chalk.green, c.red → chalk.red, etc. Preserve the outputJson/printSuccess/printError/printIssueOneLine/printIssueFull helpers but use chalk underneath. Success indicator should be ✔ (green), errors should use 'Error:' prefix (red).","blockedBy":["seeds-afd6"],"closedAt":"2026-02-24T15:07:12.805Z","closeReason":"Migrated output.ts from manual ANSI escape codes to chalk. Updated printSuccess (✔ green), printError (Error: red), and all inline color calls in stats.ts, dep.ts, tpl.ts, doctor.ts. All quality gates pass."}
{"id":"seeds-afbb","title":"Use process.exitCode instead of process.exit()","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T03:29:43.197Z","updatedAt":"2026-02-24T15:16:42.978Z","description":"The main catch block in src/index.ts uses process.exit(1) which kills the event loop immediately. Switch to process.exitCode = 1 for better testability — allows the event loop to drain so bun test can capture output cleanly. The JSON error shape { success: false, command, error } is already good. Ensure all commands surface errors consistently: console.error() with chalk.red in human mode, structured JSON in --json mode.","blockedBy":["seeds-afd6"],"closedAt":"2026-02-24T15:16:42.978Z","closeReason":"Replaced process.exit(1) with process.exitCode = 1 in src/index.ts catch block; added chalk.red for human-mode errors. All quality gates passed."}
{"id":"seeds-d6c7","title":"Build: Commander CLI refactor implementation","status":"closed","type":"task","priority":1,"createdAt":"2026-02-24T04:02:31.268Z","updatedAt":"2026-02-24T14:57:00.397Z","description":"Implement Commander.js adoption across all 16 command files and index.ts. Install commander dep, add register() exports, rewrite routing. Subtask of seeds-afd6.","closedAt":"2026-02-24T14:57:00.397Z"}
{"id":"seeds-fc8b","title":"Visual branding: forest palette, status icons, message formats","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T15:40:38.226Z","updatedAt":"2026-02-24T15:59:14.206Z","description":"Apply os-eco branding standards to seeds CLI visual output. Changes needed in src/output.ts and src/commands/{doctor,stats,tpl,dep}.ts. (1) Forest palette: define brand=chalk.rgb(124,179,66), accent=chalk.rgb(255,183,77), muted=chalk.rgb(120,120,110) in output.ts. (2) Status icon set D: replace ○/◐/● with green '-'/cyan '>'/dim 'x', add yellow '\\!' for blocked. (3) Message formats: success uses brand color for ✓ and message text (not generic green), IDs in accent color. Error uses red ✗ prefix (not 'Error:'). Warning uses yellow \\! prefix. (4) ID highlighting: issue IDs in accent amber everywhere. (5) doctor.ts: update ✓/⚠/✗ icons and colors to match spec. (6) All inline chalk in stats/tpl/dep: use muted for secondary info, accent for IDs. See branding/visual-spec.md and branding/checklists.md for exact spec.","closedAt":"2026-02-24T15:59:14.206Z","closeReason":"Visual branding implemented: forest palette (brand/accent/muted), status icon set D (-/>/ x/!), printWarning, accent IDs in output.ts, doctor/stats/tpl/dep commands updated. All quality gates passed."}
{"id":"seeds-58c5","title":"CLI standards: help screen, global flags, version, upgrade command","status":"closed","type":"feature","priority":1,"createdAt":"2026-02-24T15:40:45.302Z","updatedAt":"2026-02-24T16:10:32.624Z","description":"Implement remaining CLI standards from branding/cli-standards.md. Changes in src/index.ts, src/commands/sync.ts, new src/commands/upgrade.ts. (1) Help screen style A: custom Commander help with brand-colored header line 'seeds v0.2.1 — Git-native issue tracking', green command names, dim args, aligned columns. (2) Version flag: change -V to -v. (3) --version --json: output {name,version,runtime,platform} envelope. (4) Global flags: --quiet/-q (suppress non-error output), --verbose (extra diagnostics), --timing (show execution time on stderr as dim text). (5) --dry-run on sd sync. (6) Typo suggestions for unknown commands (edit distance 'did you mean?'). (7) Shell completions (--completions <shell> for bash/zsh/fish). (8) New sd upgrade command: check npm registry for latest @os-eco/seeds-cli, compare with local VERSION, install via bun install -g, support --check (dry check) and --json output. See branding/cli-standards.md, branding/checklists.md, and branding/roadmap.md for full spec.","closedAt":"2026-02-24T16:10:32.624Z","closeReason":"Phase 1 CLI standards merged: -v flag, --version --json, --quiet, --verbose, --dry-run on sync, sd upgrade command. 171 tests pass. Phase 2 (help screen, typo suggestions, completions, timing) tracked in seeds-9cb0."}
{"id":"seeds-6d59","title":"Build: CLI standards implementation (help, version, flags, upgrade)","status":"closed","type":"task","priority":1,"createdAt":"2026-02-24T15:50:31.621Z","updatedAt":"2026-02-24T15:59:12.541Z","description":"Implement all CLI standards from branding/cli-standards.md for seeds. Files: src/index.ts, src/output.ts, src/commands/sync.ts, src/commands/upgrade.ts (new). Subtask of seeds-58c5.","closedAt":"2026-02-24T15:59:12.541Z","closeReason":"Implemented: -v version flag, --version --json envelope, --quiet/-q + --verbose global flags, --dry-run on sync, sd upgrade command. Quality gates passed (171 tests, lint, typecheck)."}
{"id":"seeds-9cb0","title":"CLI standards phase 2: help screen, typo suggestions, completions, timing","status":"closed","type":"feature","priority":2,"createdAt":"2026-02-24T15:58:09.878Z","updatedAt":"2026-02-25T23:08:26.257Z","description":"Remaining CLI standards from seeds-58c5 not yet implemented: (1) Help screen Style A — custom Commander help with brand-colored header, green command names, dim args, aligned columns per visual-spec.md. (2) Typo suggestions — Levenshtein edit-distance 'did you mean?' for unknown commands. (3) Shell completions — --completions <shell> for bash/zsh/fish. (4) --timing flag — show execution time on stderr as dim text '[debug] completed in Nms'. See branding/cli-standards.md and branding/visual-spec.md for specs.","closedAt":"2026-02-25T23:08:26.257Z","closeReason":"All four items implemented: branded help screen, typo suggestions, shell completions, and --timing flag"}
{"id":"seeds-00c7","title":"Add --root flag to sd create for writing to project root .seeds/","status":"closed","type":"feature","priority":3,"createdAt":"2026-02-24T16:17:20.131Z","updatedAt":"2026-02-25T23:09:28.646Z","description":"When agents run in git worktrees, sd create writes to the worktree .seeds/ directory. If that branch is never merged (e.g. lead agents), those issues are lost. Add a --root flag that writes directly to the project root .seeds/ instead of the worktree copy, so issues survive worktree cleanup regardless of merge status.","closedAt":"2026-02-25T23:09:28.646Z","closeReason":"Already solved by resolveWorktreeRoot() in config.ts — findSeedsDir() auto-resolves worktree paths to main repo .seeds/ when main repo has config.yaml. No --root flag needed."}
{"id":"seeds-9474","title":"Add sd unblock command to clear resolved blockers","status":"closed","type":"feature","priority":2,"createdAt":"2026-03-03T21:09:53.703Z","updatedAt":"2026-03-04T17:30:42.376Z","description":"There is no way to remove a blocker from an issue via the CLI. When a blocking issue is closed, the blocked issue's blockedBy array retains the stale reference. The only way to clear it today is manually editing issues.jsonl — which defeats the purpose of a CLI tool. Add: (1) sd unblock <id> --from <blocker-id> — removes a specific blocker from the issue's blockedBy array, (2) sd unblock <id> --all — clears all resolved (closed) blockers from the issue, (3) Optionally, sd ready could auto-clear closed blockers when computing the ready set so stale blocks don't accumulate. Also consider the inverse: sd block <id> --by <blocker-id> to add a blocker after creation. Currently blockers can only be set at create time or by editing JSONL directly.","closedAt":"2026-03-04T17:30:42.376Z","closeReason":"Implemented sd unblock and sd block commands. unblock supports --from (specific blocker) and --all (clear closed blockers). block supports --by. Both maintain bidirectional blockedBy/blocks consistency. 211 tests pass."}
{"id":"seeds-9aa2","title":"CI: extract changelog notes for GitHub releases","status":"closed","type":"task","priority":2,"createdAt":"2026-03-04T01:29:31.031Z","updatedAt":"2026-03-04T01:31:21.327Z","description":"Update publish.yml to extract release notes from CHANGELOG.md instead of using --generate-notes. Match the pattern from overstory: awk-extract the version section, write to temp file, use --notes-file with fallback to --generate-notes if no entry found.","closedAt":"2026-03-04T01:31:21.327Z","closeReason":"Added changelog extraction step to publish.yml matching overstory pattern."}
{"id":"seeds-df9f","title":"Backfill GitHub releases with CHANGELOG.md notes","status":"closed","type":"task","priority":3,"createdAt":"2026-03-04T01:29:35.362Z","updatedAt":"2026-03-04T01:31:14.383Z","description":"One-time backfill: update existing GitHub releases with curated notes from CHANGELOG.md. Create missing release for v0.2.1. Create missing tags and releases for v0.2.0-v0.2.4.","closedAt":"2026-03-04T01:31:14.383Z","closeReason":"Backfilled 5 releases with changelog notes (v0.1.0, v0.2.0, v0.2.2-v0.2.4). Skipped v0.2.1 (no tag)."}
{"id":"seeds-905e","title":"Build: sd unblock and sd block commands","status":"closed","type":"task","priority":1,"createdAt":"2026-03-04T17:23:41.830Z","updatedAt":"2026-03-04T17:29:15.876Z","description":"Implement sd unblock and sd block commands per seeds-9474","closedAt":"2026-03-04T17:29:15.876Z","closeReason":"Implemented sd block and sd unblock commands with tests. 211 tests pass, lint and typecheck clean."}
{"id":"seeds-5960","title":"Add labels support to issues","status":"closed","type":"feature","priority":2,"createdAt":"2026-03-04T19:25:32.836Z","updatedAt":"2026-05-05T04:23:11.213Z","description":"Add labels field (string[]) to the Issue data model with full CLI support. Inspired by PR #5 from n3s-online. Scope: labels?: string[] on Issue type, sd label add/remove/list/list-all subcommand, --labels on sd create, --add-label/--remove-label/--set-labels on sd update, --label (AND) / --label-any (OR) / --unlabeled filters on sd list, label display in sd show/list/stats/prime, doctor validation for label arrays, full test suite.","closedAt":"2026-05-05T04:23:11.213Z","closeReason":"All scope items implemented across prior commits: types, label cmd, create --labels, update label flags, list/ready/search label filters, show/list/stats/prime display, doctor checkLabelSchema with --fix, full test suite"}
{"id":"seeds-d5e0","title":"Refine V1_DONE.md — deep codebase audit and checklist expansion","status":"closed","type":"task","priority":2,"createdAt":"2026-03-07T14:26:24.766Z","updatedAt":"2026-03-07T14:41:07.187Z","description":"Dispatch an overstory coordinator run with Lead -> Scouts -> Builders. Scouts should deeply explore the codebase (source, tests, open issues, recent commits, edge cases, CI config) to gather intelligence about what's done, what's missing, and what's broken. Builders should then UPDATE V1_DONE.md ONLY — expanding checklists with more specific items, refining current state assessment, adding open questions, and tightening the out-of-scope list. Builders must NOT fix code, implement features, or close any actual issues. The only deliverable is a more thorough V1_DONE.md.","closedAt":"2026-03-07T14:41:07.187Z","closeReason":"V1_DONE.md expanded with deep codebase audit: 2 scouts explored source+tests, 1 builder wrote expanded doc. Now includes detailed checklist (all 21 commands verified), source audit findings, test coverage gaps (10/21 untested, prioritized), known issues, resolved open questions, and post-V1 backlog."}
{"id":"seeds-f501","title":"Scout: audit source code, CI, and commands","status":"closed","type":"task","priority":2,"createdAt":"2026-03-07T14:32:10.544Z","updatedAt":"2026-03-07T14:34:36.490Z","closedAt":"2026-03-07T14:34:36.490Z","closeReason":"Deep audit completed: 235 passing tests, clean TypeScript, identified 1 minor convention issue (process.exit), 20 commands with --json support verified, CI/CD workflows solid. Comprehensive findings in spec file. V1 ready."}
{"id":"seeds-637e","title":"Scout: audit tests, coverage gaps, and edge cases","status":"closed","type":"task","priority":2,"createdAt":"2026-03-07T14:32:11.217Z","updatedAt":"2026-03-07T14:33:42.098Z","closedAt":"2026-03-07T14:33:42.098Z","closeReason":"Completed comprehensive audit of test coverage. 10 untested commands identified with detailed edge case gaps. Spec written to .overstory/specs/seeds-637e.md with prioritized recommendations."}
{"id":"seeds-e54b","title":"Build: write expanded V1_DONE.md from audit findings","status":"closed","type":"task","priority":1,"createdAt":"2026-03-07T14:37:12.772Z","updatedAt":"2026-03-07T14:40:20.799Z","closedAt":"2026-03-07T14:40:20.799Z","closeReason":"Wrote expanded V1_DONE.md synthesizing source-scout and tests-scout audit findings: all V1 criteria marked complete, one minor code fix noted, 10-command test gap documented as post-V1 backlog."}
{"id":"seeds-663f","title":"Test","status":"closed","type":"task","priority":2,"createdAt":"2026-03-10T15:04:40.556Z","updatedAt":"2026-05-05T03:35:55.647Z","closedAt":"2026-05-05T03:35:55.647Z"}
{"id":"seeds-2085","title":"Test Quiet Mode","status":"closed","type":"task","priority":2,"createdAt":"2026-03-10T15:04:44.961Z","updatedAt":"2026-05-05T03:35:59.732Z","closedAt":"2026-05-05T03:35:59.732Z"}
{"id":"seeds-8505","title":"Replace process.exit() with process.exitCode at src/index.ts:15","status":"closed","type":"task","priority":2,"createdAt":"2026-03-10T15:12:45.887Z","updatedAt":"2026-03-10T15:19:36.138Z","description":"V1 CLI standard requires process.exitCode instead of hard process.exit(). Line 15 of src/index.ts uses process.exit() — change to process.exitCode = 0; return;","closedAt":"2026-03-10T15:19:36.138Z"}
{"id":"seeds-0074","title":"Fix isInsideWorktree false positive in git submodules","status":"closed","type":"bug","priority":1,"createdAt":"2026-04-03T20:01:19.162Z","updatedAt":"2026-04-03T20:08:57.963Z","description":"isInsideWorktree() in src/config.ts:57-65 returns true when called from inside a git submodule. In submodules, .git is a file (not directory) containing gitdir pointer, so --git-common-dir returns path like /repo/.git/modules/<name> which doesn't end in .git. Fix: check if path.join(cwd, '.git') is a file (submodule) before existing logic, and return false if so.","closedAt":"2026-04-03T20:08:57.963Z","closeReason":"Fixed: isInsideWorktree now compares git-dir vs git-common-dir instead of path suffix heuristic. Merged from overstory/fix-worktree-builder/seeds-7ad3."}
{"id":"seeds-7ad3","title":"Build: fix isInsideWorktree submodule false positive","status":"closed","type":"task","priority":1,"createdAt":"2026-04-03T20:04:02.062Z","updatedAt":"2026-04-03T20:06:42.945Z","description":"Fix isInsideWorktree in config.ts and add submodule test in config.test.ts","closedAt":"2026-04-03T20:06:42.945Z","closeReason":"Fixed isInsideWorktree false positive for git submodules: compare --git-dir vs --git-common-dir instead of path heuristics. Added submodule test."}
{"id":"seeds-7e20","title":"seeds: add 'sd mcp serve' subcommand for claude-agent-sdk MCP bridge","status":"closed","type":"task","priority":2,"createdAt":"2026-04-24T16:00:50.101Z","updatedAt":"2026-05-10T18:02:17.284Z","description":"grove-agent-claude wrapper needs to register sd as an MCP stdio server in @anthropic-ai/claude-agent-sdk mcpServers. Current CLI (v0.2.5) has no 'mcp' subcommand. Implement 'sd mcp serve' (stdio transport) exposing issue read/write tools (sd ready, sd create, sd update, sd close, sd show, sd list at minimum).\n\nReference implementation pattern: @modelcontextprotocol/sdk Server + StdioServerTransport.\n\nBlocks: os-eco/grove#grove-b37b (MCP bridges for seeds/mulch/canopy inside claude-agent-sdk harness).","closedAt":"2026-05-10T18:02:17.284Z"}
{"id":"seeds-8526","title":"sd update --status doesn't clear closedAt/closeReason when reopening","status":"closed","type":"bug","priority":2,"createdAt":"2026-04-30T21:37:17.635Z","updatedAt":"2026-05-10T18:06:46.436Z","description":"## Symptom\n\nIssues can end up with `status:\"open\"` co-existing with a `closedAt` timestamp and `closeReason`. They appear in `sd ready` (because status is open) but `sd show` displays a stale \"closed at\" timestamp and stale close reason.\n\nObserved in overstory's `.seeds/issues.jsonl` on 2026-04-30 (overstory-4670, overstory-8e69 — both `status:\"open\"` with `closedAt` set and a triage-style close reason).\n\n## Reproduction\n\n```\nsd close <id> --reason \"...\"\nsd update <id> --status open\nsd show <id>     # status: open, but closedAt and closeReason still set\n```\n\n## Root cause\n\n`src/commands/update.ts:64-70` accepts `--status` changes but only writes the new status into the patch. It never clears `closedAt` / `closeReason` when the new status is non-`closed`, so stale close metadata persists.\n\n```ts\nif (typeof flags.status === \"string\") {\n    const s = flags.status;\n    if (!(VALID_STATUSES as readonly string[]).includes(s)) {\n        throw new Error(`--status must be one of: ${VALID_STATUSES.join(\", \")}`);\n    }\n    patch.status = s as Issue[\"status\"];\n}\n```\n\n## Fix\n\nIn `update.ts`, when `patch.status` is being set to something other than `\"closed\"`, also set `patch.closedAt = undefined` and `patch.closeReason = undefined`. (Same applies if a future `sd reopen` command is added — preferable, since reopen is a distinct verb.)\n\n## Doctor follow-up\n\n`src/commands/doctor.ts` has no check for status/closedAt consistency. Add `checkClosedFieldsConsistency`:\n\n- `status !== \"closed\"` AND (`closedAt` set OR `closeReason` set) → fail (fixable: clear both fields)\n- `status === \"closed\"` AND `closedAt` missing → warn (fixable: set to `updatedAt`)\n\nMake it `--fix`-able so existing inconsistent records (like the two overstory issues) can be repaired.\n\n## Acceptance\n\n- After `sd close <id>` followed by `sd update <id> --status open`, `sd show <id>` reports no `closedAt` and no `closeReason`.\n- `sd doctor` reports a failure on issues with mismatched status/closedAt; `sd doctor --fix` repairs them.\n- Tests in `src/commands/update.test.ts` and `src/commands/doctor.test.ts` cover the close→reopen→show round-trip and the doctor check.","closedAt":"2026-05-10T18:06:46.436Z","closeReason":"Fixed in update.ts: --status to non-closed now clears closedAt/closeReason. Added doctor closed-fields-consistency check (fail on stale fields, warn on missing closedAt; both --fix-able). Tests added in update.test.ts and doctor.test.ts; doctor pass count bumped to 12."}
{"id":"seeds-d5d9","title":"Add sd search command for full-text search","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-05T03:35:04.770Z","updatedAt":"2026-05-05T04:19:10.475Z","description":"No way to search issue title/description text today. Even at modest issue counts, finding 'the one about retry logic' forces sd list --json | grep. Implement sd search <query> with case-insensitive substring match on title + description; same filters as list (--status, --type, --assignee, --label); honor --format/--json output. Borrowed from ml search.","labels":["qol"],"closedAt":"2026-05-05T04:19:10.475Z","closeReason":"Implemented sd search with case-insensitive substring on title+description; reuses applyIssueFilters for shared filters; covered by 15 tests in src/commands/search.test.ts"}
{"id":"seeds-332e","title":"Default sort by priority on sd list and sd ready","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-05T03:35:07.455Z","updatedAt":"2026-05-05T03:39:45.534Z","description":"list.ts and ready.ts return issues in JSONL file order (essentially arbitrary). For an agent, sd ready should be a queue, not a pile. Sort by priority asc (P0 first), tie-break by createdAt desc. Add --sort flag (priority|created|updated|id) for override.","labels":["qol"],"closedAt":"2026-05-05T03:39:45.534Z","closeReason":"Added --sort flag to sd list and sd ready (priority|created|updated|id). Default is priority asc, tie-break createdAt desc. Shared sort logic in src/sort.ts."}
{"id":"seeds-829a","title":"Add priority filters to sd list and sd ready","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-05T03:35:10.942Z","updatedAt":"2026-05-05T04:13:33.068Z","description":"Cannot filter by priority today, despite it being one of the most common triage ops. Add --priority <n[,n,...]> for exact match (e.g. --priority 0,1) and --priority-max <n> for ceiling (e.g. --priority-max 1 = P0+P1). Accept both numeric (0-4) and P-prefixed forms (P0-P4) consistent with create/update parsing.","labels":["qol"],"closedAt":"2026-05-05T04:13:33.068Z","closeReason":"Added --priority and --priority-max filters to sd list/sd ready in src/filter.ts; supports numeric (0-4) and P-prefixed (P0-P4) forms"}
{"id":"seeds-7871","title":"Backport sd list filters to sd ready","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-05T03:35:13.615Z","updatedAt":"2026-05-05T03:58:19.660Z","description":"sd ready (ready.ts) accepts only --json today. sd list has --type, --assignee, --label, --label-any, --unlabeled, --limit. Filtering ready work by type or label is a basic agent need. Refactor shared filter logic out of list.ts into a helper and apply to both commands.","labels":["qol"],"closedAt":"2026-05-05T03:58:19.660Z","closeReason":"Added --type, --assignee, --label, --label-any, --unlabeled, --limit to sd ready. Extracted shared filter logic to src/filter.ts (list + ready)."}
{"id":"seeds-efa0","title":"Resolve --assignee me from git config","status":"open","type":"feature","priority":3,"createdAt":"2026-05-05T03:35:16.117Z","updatedAt":"2026-05-05T03:35:16.117Z","description":"Small ergonomics: when --assignee me is passed to create/update/list, resolve it from git config user.email (or user.name as fallback). Avoids agents and humans having to type/remember their own handle. Pure convenience, no schema change.","labels":["qol"]}
{"id":"seeds-8bb6","title":"Global --format flag (markdown|compact|plain|ids|json)","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-05T03:35:19.795Z","updatedAt":"2026-05-05T03:50:01.901Z","description":"Borrowed from mulch. Replace per-command --json with a global --format option respected by output-emitting commands (list, ready, show, blocked, search, stats). Formats: markdown (default human), compact (one record/line, dense), plain (no ANSI), ids (just IDs, one per line — pipeable to xargs), json (current --json). Keep --json as alias for --format json. The 'ids' mode in particular unlocks pipelines like: sd list --label bug --format ids | xargs sd close.","labels":["qol"],"closedAt":"2026-05-05T03:50:01.901Z","closeReason":"Added global --format flag with markdown|compact|plain|ids|json modes on list/ready/show/blocked/stats; --json kept as alias"}
{"id":"seeds-8fdb","title":"sd plan: structured planning facilitation","status":"closed","type":"epic","priority":1,"createdAt":"2026-05-06T18:04:45.861Z","updatedAt":"2026-05-06T20:21:54.032Z","description":"Extend seeds beyond issue tracking into a structured planning layer for AI agents. Plans are JSONL-stored, schema-validated, config-customizable, and produce child seeds with blocking edges wired automatically. Soft-coupled to mulch for prior-art enrichment.\n\nSpec: PLAN_SPEC.md (root of seeds repo).\n\n## Thesis\nMost attention should go to planning, not review. Plans are the artifact reviewed; implementation should be straightforward enough that small models can execute. When implementation fails, fix the planning *process*, not the post-hoc code review.\n\n## Architecture\n- New file: .seeds/plans.jsonl (parallel to issues.jsonl, same locking model)\n- Plans get their own ID space (pl-XXXX)\n- Bidirectional linking: plan.seed ↔ issue.plan_id\n- Templates declared in config.yaml plan_templates: block; AJV validates\n- Children spawned 1:1 from steps[]; blocks wired via step indices\n\n## Rollout (this epic blocks on phase features)\nPhase 0: Validation foundation (AJV scaffold)\nPhase 1: Core MVP — hardcoded feature template, basic spawn loop\nPhase 2: Custom templates from config + lifecycle + --overwrite\nPhase 3: Mulch prior_art enrichment + --record-decision\nPhase 4: Sub-plans (nested decomposition)\nPhase 5: Outcomes + review + bug/refactor templates + docs\n\n## v1 surface (per planning alignment)\nIN: phases 0-5, sub-plans, mulch enrichment, outcomes/review\nOUT (followups): bug/refactor built-in templates land in phase 5 but are lower priority\n\n## Decisions locked\n- yaml.ts upgrade to nested (vs separate file) — keeps spec-as-written\n- AJV as runtime dep (vs hand-rolled) — matches spec, prior art in mulch\n- Shell out to ml query --json (soft coupling, language-agnostic)","closedAt":"2026-05-06T20:21:54.032Z","closeReason":"sd plan feature complete across all 6 phases:\n- Phase 0 (seeds-bcdc) ajv validation foundation — 535a8b5\n- Phase 1 (seeds-a672) command tree + hardcoded feature template — c67dfcf\n- Phase 2 (seeds-c95d) config-driven templates + lifecycle + --overwrite — 315d817\n- Phase 3 (seeds-bd0b) mulch prior_art + --record-decision — 5315449\n- Phase 4 (seeds-f873) sub-plans (requires_plan + recursive show) — 33c1fb9\n- Phase 5 (seeds-6d30) outcomes, review, bug + refactor templates, docs — 8e0c9e0\nPLAN_SPEC.md committed at 6b0736b. All 366→428 tests pass; lint + typecheck green."}
{"id":"seeds-bcdc","title":"Phase 0: Validation foundation (AJV)","status":"closed","type":"feature","priority":1,"createdAt":"2026-05-06T18:06:07.022Z","updatedAt":"2026-05-06T18:18:01.559Z","description":"## Phase 0 — Validation foundation (AJV)\n\nEstablishes the validation primitives that all later `sd plan` phases consume. This phase ships the central `src/validation.ts` module and its tests, plus the `ajv` runtime dependency. It does NOT ship template-config-to-schema generation (Phase 2) — only hand-written schemas can be compiled by the primitives delivered here.\n\n### Spec references\n\n- `PLAN_SPEC.md:358–371` — Validation rules surface (required, min_length, min, step ref integrity, kind matching, template name resolution).\n- `PLAN_SPEC.md:180–195` — Partial-state diff shape that validation failures must emit:\n  ```json\n  {\n    \"errors\": [\n      { \"path\": \"sections.risks\", \"code\": \"required\", \"fix\": \"add a 'risks' array (string entries)\" },\n      { \"path\": \"sections.steps\", \"code\": \"min\", \"fix\": \"add at least 1 more step entry\" }\n    ],\n    \"current\": { \"...plan-as-submitted...\" }\n  }\n  ```\n\n### Why this phase exists\n\n`sd plan submit` and `sd plan validate` (later phases) both need a single, reusable validator that:\n\n1. Compiles AJV schemas with consistent options (allErrors: true, strict mode, etc).\n2. Translates AJV errors into the spec's stable error shape so the LLM can patch and resubmit without re-prompting.\n3. Keeps the AJV import surface confined to one file, preserving the project's \"minimal runtime dependencies\" rule (CLAUDE.md — only chalk + commander today).\n\n### Scope\n\nIn scope:\n- Add `ajv` (^8.x) under `dependencies` in `package.json`.\n- Create `src/validation.ts` exporting `compileSchema` and an error formatter.\n- Define a TS interface for the partial-state diff structure.\n- Unit tests in `src/validation.test.ts` covering valid/invalid cases.\n\nOut of scope (later phases):\n- Generating AJV schemas from `plan_templates` config (Phase 2 task 14).\n- Wiring validation into `sd plan submit` / `sd plan validate` commands (Phase 1+).\n- Mulch enrichment, prompt JSON, persistence — none of these touch this module.\n\n### Tasks\n\n- Task 0.1 — Add ajv runtime dep and scaffold `src/validation.ts` (this phase has a single task).\n\n### Definition of done\n\n- Phase 0 task is closed.\n- `bun test && bun run lint && bun run typecheck` all green.\n- `ajv` appears in `dependencies`, not `devDependencies`.\n- `src/validation.ts` is importable and consumed by zero callers yet (Phase 1 will wire it in).","blocks":["seeds-8fdb","seeds-a672"],"closedAt":"2026-05-06T18:18:01.559Z","closeReason":"Phase 0 complete: ajv added as runtime dep, src/validation.ts ships compileSchema + formatErrors with PartialStateDiff/ErrorEntry types. 11 tests cover valid/invalid, required-path, minLength, minItems→min normalization, current round-trip, allErrors, fix non-empty, type mismatch. Quality gates green; ajv import boundary verified (only src/validation.ts imports it)."}
{"id":"seeds-6e92","title":"Add ajv runtime dep and scaffold src/validation.ts","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:06:41.507Z","updatedAt":"2026-05-06T18:18:01.559Z","description":"## Task 0.1 — Add ajv runtime dep and scaffold `src/validation.ts`\n\nPart of Phase 0 (validation foundation). Establishes the single AJV-using module that every later `sd plan` command will consume.\n\n### What\n\n1. Add `ajv` (^8.x) to seeds runtime dependencies in `package.json` (under `dependencies`, NOT `devDependencies`). Run `bun install` to update lockfile.\n2. Create `src/validation.ts` exporting at minimum:\n   - `compileSchema(schema: object): ValidatorFn` — wraps `new Ajv({ allErrors: true, strict: true })` + `ajv.compile(schema)`. Returns a function `(data: unknown) => { valid: boolean, errors: ErrorEntry[] }` (or equivalent) that already runs the formatter under the hood, OR returns the raw compiled validator and exposes `formatErrors` separately. Pick whichever feels cleaner; document the choice in a top-of-file comment.\n   - `formatErrors(ajvErrors, submitted): PartialStateDiff` — turns AJV's `ErrorObject[]` plus the originally-submitted plan into the spec's partial-state diff shape (see PLAN_SPEC.md:180–195).\n   - A TypeScript `interface PartialStateDiff` (or similar name) describing `{ errors: ErrorEntry[]; current: unknown }` and `interface ErrorEntry { path: string; code: string; fix: string }`.\n3. Mapping rules inside `formatErrors`:\n   - AJV `instancePath` (e.g. `/sections/risks`) → spec `path` (e.g. `sections.risks`). Strip the leading `/`, replace remaining `/` with `.`. Empty string for root.\n   - For `required` errors, append the missing property to the path (AJV reports `instancePath` as the parent and puts the missing key in `params.missingProperty`).\n   - AJV `keyword` → spec `code` (`required`, `minLength`, `minItems`, `type`, etc.). For now do a 1:1 passthrough, but normalize `minItems` and `minLength` callers can choose to map to spec's shorter `min` if convenient — document the chosen convention.\n   - Generate a human-readable `fix` string per error keyword. Examples:\n     - `required` + missing `risks` → `\"add a 'risks' array (string entries)\"` style. Use the schema's surrounding context if available; otherwise emit a generic `\"add a '<prop>' field\"`.\n     - `minItems` (got n, need m) → `\"add at least <m-n> more entr<y|ies>\"`.\n     - `minLength` → `\"expand '<path>' to at least <N> characters\"`.\n     - `type` mismatch → `\"'<path>' must be a <expected-type>\"`.\n     - Fallback for unknown keywords: a string built from `keyword` + `params`.\n4. `current` must round-trip the submitted object verbatim — pass it through unchanged (the LLM patches this and resubmits).\n\n### Why\n\nPer PLAN_SPEC.md:358–371, validation runs on every `sd plan submit` and on `sd plan validate <pl-id>`. The failure shape (PLAN_SPEC.md:180–195) is what lets the LLM patch-and-retry without re-running `sd plan prompt`. Centralising AJV in one module keeps the dep boundary tight: only `src/validation.ts` imports `ajv`, satisfying the project's \"minimal runtime dependencies\" rule (see seeds CLAUDE.md, \"Dependencies\" section).\n\n### Acceptance criteria\n\n- [ ] `package.json` lists `ajv` under `dependencies` (not `devDependencies`); `bun.lockb` (or equivalent lockfile) updated.\n- [ ] `src/validation.ts` exists and exports `compileSchema`, `formatErrors`, and the `PartialStateDiff` / `ErrorEntry` types.\n- [ ] Mapping is correct: AJV `instancePath` → spec `path` (slash-to-dot, leading slash stripped); `required` errors include the missing property in the path.\n- [ ] `formatErrors` emits a non-empty `fix` string for every error (no empty/undefined fixes).\n- [ ] `current` field round-trips the submitted object verbatim (deep-equal to input).\n- [ ] `src/validation.test.ts` covers:\n  - Valid object passes (no errors, `valid: true`).\n  - Missing required field fails with `code: \"required\"` and the path includes the missing property.\n  - Too-short string fails with `code: \"minLength\"` (or normalized equivalent — document choice).\n  - Submitted object survives in `current` byte-for-byte.\n  - Multiple simultaneous errors all surface (AJV `allErrors: true` is honored).\n- [ ] `bun test && bun run lint && bun run typecheck` all pass.\n- [ ] No file outside `src/validation.ts` imports `ajv` (grep check).\n\n### Out of scope\n\n- Generating AJV schemas from `plan_templates:` YAML config — that is Phase 2 task 14. Phase 0 only ships hand-written schema support.\n- Wiring this validator into any `sd plan ...` subcommand — Phase 1+.\n- Mulch enrichment, prompt JSON shape, plan persistence — unrelated subsystems.\n\n### Files\n\n- `src/validation.ts` (new)\n- `src/validation.test.ts` (new)\n- `package.json` (edit — add ajv)\n- `bun.lock` / `bun.lockb` (edit — regenerate)\n\n### Implementation hints\n\n- Tests follow seeds convention: real I/O where needed, no mocks. Validation is pure so just feed JS objects — no temp dirs required for this module.\n- Keep the file small (<150 LOC); the hard work is in keyword-to-`fix` translation. A `switch (keyword)` inside `formatErrors` is fine.\n- If AJV's strict mode complains about meta-keywords used in tests, add only the necessary `strict: false` overrides locally inside tests; production `compileSchema` should stay strict.","blocks":["seeds-bcdc"],"closedAt":"2026-05-06T18:18:01.559Z","closeReason":"Phase 0 complete: ajv added as runtime dep, src/validation.ts ships compileSchema + formatErrors with PartialStateDiff/ErrorEntry types. 11 tests cover valid/invalid, required-path, minLength, minItems→min normalization, current round-trip, allErrors, fix non-empty, type mismatch. Quality gates green; ajv import boundary verified (only src/validation.ts imports it)."}
{"id":"seeds-a672","title":"Phase 1: Core MVP (sd plan command tree, hardcoded feature template)","status":"closed","type":"feature","priority":1,"createdAt":"2026-05-06T18:06:59.102Z","updatedAt":"2026-05-06T18:43:19.141Z","description":"## What\n\nPhase 1 of the `sd plan` feature (parent epic seeds-8fdb). Ships a working `sd plan` command tree with a single hardcoded `feature` template, basic spawn loop, and integration with `sd ready` / `sd show` / `sd list`.\n\nSpec: PLAN_SPEC.md\n- Architecture & on-disk format: lines 22-107\n- Lifecycle + command surface: lines 109-155\n- Walkthrough protocol + JSON shapes: lines 159-256\n- Default feature template: lines 313-325\n\n## Why\n\nThis is the MVP slice — proves the architecture end-to-end (types, store, template+schema, command parent, prompt/submit/show/list/validate, integration with existing commands) without taking on mulch enrichment, sub-plans, or YAML-configurable templates. Those are Phase 2/3.\n\n## Scope (constituent tasks)\n\n1. Plan type + Issue field extensions in `src/types.ts`\n2. Plan store layer in `src/store.ts`\n3. Hardcoded built-in `feature` template + AJV schema (`src/plan-templates/feature.ts`)\n4. `sd plan` command parent + `sd plan templates` subcommand\n5. `sd plan prompt <seed-id>` command\n6. `sd plan submit <seed-id> --plan <file>` command\n7. `sd plan show <pl-id>` command\n8. `sd plan list` command with filters\n9. `sd plan validate <pl-id>` command\n10. `sd ready`/`sd show`/`sd list` plan-awareness integration\n\n## Out of scope (Phase 1)\n\n- Mulch enrichment (`prior_art` arrays remain empty)\n- Sub-plans / nested plans / `requires_plan`\n- Custom templates from `.seeds/config.yaml` (YAML upgrade is Phase 2)\n- `sd plan outcome`, `sd plan review`, `--overwrite` behavior (Phase 2/3)\n- `--record-decision` outbound mulch write\n\n## Constraints\n\n- `feature` template is a hardcoded TypeScript object, not loaded from YAML\n- Validation uses a hand-written AJV schema via the `compileSchema` primitive from Phase 0's `src/validation.ts`\n- Phase 1 must NOT depend on Phase 2's yaml.ts upgrade\n- Existing seeds remain valid (additive `plan_id` / `plan_step_index` fields)\n\n## Acceptance\n\n- All 10 child tasks closed\n- `sd plan prompt` → LLM-fillable JSON → `sd plan submit` → spawned children with correct `blockedBy` wiring works end-to-end\n- `bun test && bun run lint && bun run typecheck` all green","blocks":["seeds-8fdb","seeds-c95d","seeds-bd0b"],"closedAt":"2026-05-06T18:43:19.141Z","closeReason":"Phase 1 MVP complete: all 10 child tasks closed; bun test/lint/typecheck green (366 tests)"}
{"id":"seeds-90ef","title":"Plan type + Issue field extensions in src/types.ts","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:07:12.432Z","updatedAt":"2026-05-06T18:21:45.552Z","description":"## What\n\nAdd a new `Plan` interface and extend the existing `Issue` interface with optional planning back-pointers in `src/types.ts`.\n\nPLAN_SPEC.md references:\n- Plan row shape: lines 92-98\n- Issue field additions: lines 100-107\n\n### `Plan` interface\n\n```ts\ninterface Plan {\n  id: string;                      // \"pl-{4hex}\"\n  seed: string;                    // parent seed id\n  template: string;                // e.g. \"feature\"\n  status: \"draft\" | \"approved\" | \"active\" | \"done\";\n  revision: number;\n  sections: Record<string, unknown>;\n  children: string[];              // spawned child seed ids\n  outcome?: \"success\" | \"partial\" | \"failure\";\n  outcomeNote?: string;\n  reviewedBy?: string;\n  createdAt: string;               // ISO 8601\n  updatedAt: string;\n}\n```\n\n### `Issue` extension\n\nTwo new optional fields (additive; backward-compatible):\n\n- `plan_id?: string` — the plan this seed is part of (parent or spawned child).\n- `plan_step_index?: number` — for spawned children, index in the plan's `steps[]` array (enables stale-child detection on revision).\n\n### Constants\n\nAdd `PLANS_FILE = \"plans.jsonl\"` next to existing `ISSUES_FILE` / `TEMPLATES_FILE`.\n\n## Why\n\nFoundation type for every other Phase 1 task. Has to land before store, template, or commands can be built. Keeping the Issue extension additive guarantees existing JSONL rows remain valid.\n\n## Acceptance\n\n- `bun run typecheck` passes\n- All existing tests still pass (`bun test`)\n- `Plan` and `PLANS_FILE` are exported from `src/types.ts`\n- A `Plan` literal with all required fields type-checks; one with extras (`outcome`, `reviewedBy`) also type-checks\n- Existing `Issue` rows in fixtures still parse without the new fields\n\n## Files\n\n- `src/types.ts` (edit)","blocks":["seeds-a672","seeds-0692","seeds-6916","seeds-8640","seeds-a001","seeds-6ccc","seeds-85b6","seeds-c0f0","seeds-4b9c"],"closedAt":"2026-05-06T18:21:45.552Z","closeReason":"Plan + Issue field extensions added in src/types.ts; typecheck and tests pass"}
{"id":"seeds-917b","title":"Plan store layer in src/store.ts","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:07:22.734Z","updatedAt":"2026-05-06T18:22:33.824Z","description":"## What\n\nAdd a plan storage layer to `src/store.ts` mirroring the existing issue/template helpers.\n\nNew exports:\n- `plansPath(root: string): string` — resolve `.seeds/plans.jsonl`\n- `readPlans(root: string): Promise<Plan[]>` — read with dedup-on-read (last occurrence wins, mirrors issue dedup)\n- `writePlans(root: string, plans: Plan[]): Promise<void>` — atomic temp+rename under `withLock`\n- `appendPlan(root: string, plan: Plan): Promise<void>` — locked append\n\nUse the same lock model as issues:\n- `withLock` wrapper\n- `O_CREAT | O_EXCL` lock files\n- 30s stale, 50ms retry, 5s timeout\n- Atomic writes via temp file + rename for full rewrites\n\nReference PLAN_SPEC.md lines 92-98 for the plan row shape and CLAUDE.md \"Concurrency\" section for the lock contract.\n\n## Why\n\nEvery plan command (`prompt`, `submit`, `show`, `list`, `validate`) goes through this layer. Reusing the existing patterns guarantees the same multi-agent safety properties seeds already provides for issues.\n\n## Acceptance\n\n- New functions exported from `src/store.ts`\n- Tests in `src/store.test.ts` cover:\n  - Round-trip: write → read returns the same plans\n  - Dedup-on-read: duplicate plan IDs in the JSONL collapse to last-occurrence\n  - Append safety: concurrent `appendPlan` calls do not corrupt the file\n  - Atomic rewrite: a `writePlans` call interrupted partway leaves the original file intact (or at least leaves no partial line)\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/store.ts` (edit)\n- `src/store.test.ts` (edit — add plan tests)\n\n## Depends on\n\nTask 1 (Plan type must exist before the store can read/write it).","blocks":["seeds-a672","seeds-0692","seeds-6916","seeds-8640","seeds-a001","seeds-6ccc","seeds-85b6","seeds-c0f0","seeds-4b9c"],"closedAt":"2026-05-06T18:22:33.824Z","closeReason":"plansPath/readPlans/writePlans/appendPlan added to src/store.ts; round-trip, dedup, concurrent append, and atomic-rewrite tests in store.test.ts all pass"}
{"id":"seeds-c95d","title":"Phase 2: Custom templates from config + lifecycle + --overwrite","status":"closed","type":"feature","priority":1,"createdAt":"2026-05-06T18:07:37.688Z","updatedAt":"2026-05-06T19:13:31.443Z","description":"## What\n\nPhase 2 of the `sd plan` feature (parent epic: seeds-8fdb). Replaces Phase 1's hardcoded `feature` template with user-defined plan templates declared in `.seeds/config.yaml`, adds plan lifecycle auto-transitions (draft → approved → active → done), and implements `--overwrite` semantics for plan revisions.\n\nPhase 2 covers:\n1. Upgrade YAML parser to support nested maps + lists (required for `plan_templates:` block).\n2. Parse `plan_templates:` config block into typed `PlanTemplate` records.\n3. Generate AJV schemas dynamically from `PlanTemplate` config (replaces Phase 1 hand-written feature schema).\n4. Wire plan lifecycle auto-transitions on child seed status changes.\n5. Implement `sd plan submit --overwrite` with revision increment + obsolete-child flagging.\n\n## Why\n\nPer PLAN_SPEC.md:11–19 (Design Principles), plans must be customizable per-project. Hardcoded templates only cover the default `feature` shape; teams need `spike`, `bug`, `refactor`, and bespoke templates without forking seeds. The lifecycle transitions and `--overwrite` semantics are explicit requirements in PLAN_SPEC.md:109–127 (lifecycle) and 374–391 (re-submission).\n\nThis phase transforms `sd plan` from a hardcoded prototype into a config-driven planning layer.\n\n## Acceptance\n\n- Plan templates declared in `.seeds/config.yaml` under `plan_templates:` are loadable, validatable, and overridable.\n- Built-in `feature` template behaves identically to Phase 1 when no config override exists.\n- Plan status auto-transitions: `draft → approved` (on submit), `approved → active` (on first child `in_progress`), `active → done` (when all children closed); reverts `done → active` if a closed child reopens.\n- `sd plan submit --overwrite` rewrites `plans.jsonl` row, increments `revision`, flags obsolete children to stderr (does NOT auto-close), spawns new children for new steps.\n- Re-submitting without `--overwrite` is rejected when a non-`draft` plan exists for the seed.\n- All 5 sub-tasks closed; phase blocking edges cleared.\n\n## Files\n\n- `src/yaml.ts` (extend or replace)\n- `src/types.ts` (add `PlanTemplate`, `SectionSpec`)\n- `src/config.ts` (add `loadPlanTemplates`)\n- `src/plan-schema.ts` (NEW — generates AJV schemas from templates)\n- `src/commands/plan-submit.ts` (extend with `--overwrite`)\n- `src/commands/update.ts`, `src/commands/close.ts` (lifecycle hooks)\n\n## Spec references\n\n- PLAN_SPEC.md:36–78 — config.yaml additions, plan_templates structure\n- PLAN_SPEC.md:109–127 — lifecycle state machine\n- PLAN_SPEC.md:261–325 — templates customization, kinds, default feature template\n- PLAN_SPEC.md:374–391 — re-submission and --overwrite semantics\n\n## Cross-phase\n\nDepends on Phase 1 landing (plan command tree, store, types). The orchestrator wires Phase 1 → Phase 2 blocking at the phase-feature level.","blocks":["seeds-8fdb","seeds-f873","seeds-6d30"],"closedAt":"2026-05-06T19:13:31.443Z","closeReason":"Phase 2 complete: yaml parser nesting, plan_templates loader, AJV schema gen, lifecycle auto-transitions, --overwrite. 428 tests pass. All 5 sub-tasks closed."}
{"id":"seeds-0692","title":"Hardcoded built-in feature template + AJV schema","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:07:41.849Z","updatedAt":"2026-05-06T18:25:49.800Z","description":"## What\n\nCreate `src/plan-templates/feature.ts` (or similar location) that exports the built-in `feature` template as a hardcoded TypeScript object plus a hand-written AJV schema for validating submissions.\n\n### Six default sections\n\nPer PLAN_SPEC.md lines 313-325 and 38-78:\n\n1. `context` — required, `kind: text`, `min_length: 50`\n2. `approach` — required, `kind: text`\n3. `alternatives` — optional, `kind: list`, item is `{name: text, rejected_because: text}`\n4. `steps` — required, `kind: steps`, `min: 2`. Each step is `{title, type, priority, blocks: number[]}`. (Phase 1 ignores `plan_template` on steps — that's nested-plans, Phase 3.)\n5. `risks` — optional, `kind: list`, item `text` (Phase 1: no mulch enrichment, but the `mulch_source: failure` hint can live in the template metadata for forward-compat)\n6. `acceptance` — required, `kind: list`, item `text`, `min: 1`\n\n### AJV schema\n\nHand-write the JSON schema. Use `compileSchema` from Phase 0's `src/validation.ts` (already in place). The schema must enforce:\n- Required sections present and non-empty\n- `min_length` on `context`\n- `min` on `steps` and `acceptance`\n- `steps[].blocks` references valid step indices (no forward references beyond array length, no self-reference) — this is the structural validation called out in PLAN_SPEC.md lines 366-369\n- `alternatives[]` items match the `{name, rejected_because}` shape\n\n### Partial-state diff shape\n\nOn validation failure, callers must be able to emit the partial-state JSON in PLAN_SPEC.md lines 180-195:\n\n```json\n{\n  \"errors\": [{ \"path\": \"sections.X\", \"code\": \"...\", \"fix\": \"...\" }],\n  \"current\": { ...plan-as-submitted... }\n}\n```\n\nProvide a helper that converts AJV errors into this shape (or returns enough information for the submit command to do so).\n\n## Why\n\nThis is the validation core. `sd plan prompt` reads the template metadata to emit the prompt JSON; `sd plan submit` and `sd plan validate` use the schema to gate writes. Hardcoding it (vs. deriving from YAML) lets Phase 1 ship without depending on Phase 2's yaml.ts upgrade.\n\n## Acceptance\n\n- A valid plan submission (all required sections, >=2 steps with valid `blocks` indices, >=1 acceptance, context >= 50 chars) passes\n- Each of the following fails with the documented partial-state diff shape:\n  - Missing required section (e.g. omit `context`)\n  - `context` too short (< 50 chars)\n  - `steps` with only 1 entry\n  - `steps[0].blocks: [0]` (self-reference)\n  - `steps[0].blocks: [5]` when there are only 2 steps (out of range)\n  - `acceptance: []`\n- Tests for each of the above in a new `src/plan-templates/feature.test.ts`\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/plan-templates/feature.ts` (new)\n- `src/plan-templates/feature.test.ts` (new)\n- May need a small `src/plan-templates/index.ts` registry that maps name → template (Phase 1 has only `feature`)\n\n## Depends on\n\n- Task 1 (types)\n- Phase 0's `src/validation.ts` (`compileSchema`)","blocks":["seeds-a672","seeds-8640","seeds-a001","seeds-c0f0"],"closedAt":"2026-05-06T18:25:49.800Z","closeReason":"feature template + AJV schema in src/plan-templates/feature.ts; structural blocks-index check via second pass; 10 tests covering golden + 6 documented failure cases"}
{"id":"seeds-6916","title":"sd plan command parent + sd plan templates subcommand","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:07:53.470Z","updatedAt":"2026-05-06T18:28:30.978Z","description":"## What\n\nWire the `sd plan` parent command and its first child, `sd plan templates`.\n\nUse the same nested-subcommand pattern as `src/commands/tpl.ts` (Commander parent with `.command(...)` children). Register from `src/index.ts`.\n\nCreate `src/commands/plan.ts` exporting `register(program: Command)` that:\n1. Builds a `plan` parent: `program.command('plan').description('Plan management')`\n2. Adds child commands as Phase 1 tasks land. The first child is `templates`:\n   - `sd plan templates` — list available templates with `name` + `description`\n   - Phase 1 lists only `feature`\n   - Support `--json` for structured output via the existing `output.ts` helpers\n\n## Why\n\nEvery other Phase 1 plan command attaches to this parent. Landing the parent + a trivial child early de-risks the wiring and gives subsequent tasks a stable target.\n\n## Acceptance\n\n- `sd plan --help` shows the parent with subcommand list\n- `sd plan templates` (human) prints `feature` with description\n- `sd plan templates --json` emits structured JSON via `output.ts` conventions\n- `sd plan` (no subcommand) prints help and exits non-zero\n- Test in `src/commands/plan.test.ts` covers `templates` human + JSON output\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/plan.ts` (new)\n- `src/commands/plan.test.ts` (new)\n- `src/index.ts` (edit — register the plan parent)\n\n## Depends on\n\n- Task 1 (types — needs Plan/template types eventually)\n- Task 2 (store — though `templates` subcommand doesn't read plans, it should live in the same module as the rest)","blocks":["seeds-a672","seeds-8640","seeds-a001","seeds-6ccc","seeds-85b6","seeds-c0f0"],"closedAt":"2026-05-06T18:28:30.978Z","closeReason":"src/commands/plan.ts created with parent + templates subcommand; registered in src/index.ts; help, no-subcmd, human, and JSON tests pass"}
{"id":"seeds-736f","title":"Upgrade src/yaml.ts to support nested maps + lists","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:07:54.481Z","updatedAt":"2026-05-06T18:53:29.531Z","description":"## What\n\nUpgrade `src/yaml.ts` from the current flat key-value parser (~50 LOC) to support nested maps and lists. This is required to parse the `plan_templates:` block in `.seeds/config.yaml`.\n\nTwo acceptable approaches — implementer's call:\n1. Extend the in-tree parser (preserves zero-runtime-dep posture; matches CLAUDE.md \"minimal runtime dependencies\" convention).\n2. Add `js-yaml` (~9KB) as a runtime dep.\n\nDocument the choice in the commit message.\n\n## Why\n\nPer PLAN_SPEC.md:36–78, the config.yaml block needed for Phase 2 is deeply nested:\n\n```yaml\nplan_templates:\n  feature:\n    sections:\n      context:\n        required: true\n        kind: text\n        min_length: 50\n        prompt: \"...\"\n      alternatives:\n        kind: list\n        item:\n          name: { kind: text }\n          rejected_because: { kind: text }\n      steps:\n        kind: steps\n        min: 2\n```\n\nThe current parser cannot represent this. Without nested-map + list support, no other Phase 2 task can begin.\n\n## Acceptance\n\n- Parses the `plan_templates:` block from PLAN_SPEC.md:42–77 round-trip into a typed structure.\n- Existing flat-config tests in `src/yaml.test.ts` still pass unchanged.\n- New tests cover:\n  - Nested maps (3+ levels deep).\n  - Lists of strings (e.g., `[a, b, c]` and block-style `- a\\n- b`).\n  - Lists of objects.\n  - Mixed quoted (`\"foo: bar\"`) and unquoted scalars.\n  - Inline flow maps (e.g., `{ kind: text }`) since spec uses them.\n  - Booleans, integers, and string scalars correctly typed.\n- `bun test src/yaml.test.ts && bun run lint && bun run typecheck` clean.\n\n## Files\n\n- `src/yaml.ts` — parser implementation\n- `src/yaml.test.ts` — extend tests\n- `package.json` — add `js-yaml` if that route is chosen\n\n## Spec references\n\n- PLAN_SPEC.md:36–78 — config.yaml additions\n\n## Notes\n\nDocument parser choice in the commit message body. If extending in-tree, keep the LOC budget reasonable; if it grows past ~200 LOC, the dep route is probably the better call.","blocks":["seeds-c95d","seeds-fc62"],"closedAt":"2026-05-06T18:53:29.531Z","closeReason":"yaml parser upgraded: nested maps, block sequences, flow maps/seqs, typed scalars; in-tree (no js-yaml dep); 35 tests"}
{"id":"seeds-bd0b","title":"Phase 3: Mulch prior_art enrichment + --record-decision","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-06T18:07:54.555Z","updatedAt":"2026-05-06T19:28:11.792Z","description":"# Phase 3: Mulch prior_art enrichment + --record-decision\n\nPhase 3 of the `sd plan` rollout. Makes `sd plan prompt` smarter by injecting\nprior art from mulch (when `ml` is on PATH) into each section's `prior_art`\narray, and adds an opt-in outbound `ml record` write on `sd plan submit`.\n\n## What\n\nThree pieces of work:\n1. Domain inference helper used by both prompt enrichment and outbound write\n   (PLAN_SPEC.md:347).\n2. Per-section prior_art population via shelling out to `ml query --json`\n   (PLAN_SPEC.md:344-352, prompt JSON shape PLAN_SPEC.md:200-234, section hint\n   PLAN_SPEC.md:36-78).\n3. `sd plan submit --record-decision` outbound write — best-effort, off by\n   default (PLAN_SPEC.md:354-356).\n\n## Why\n\n- Plans grounded in prior conventions, patterns, decisions, and failures are\n  better plans. Mulch already accumulates that prior art; surfacing it in the\n  planning prompt closes the loop.\n- Soft coupling preserves seeds' standalone story (PLAN_SPEC.md:17) — `ml`\n  absent must not break planning.\n\n## Acceptance\n\n- `sd plan prompt <seed-id>` populates `prior_art` arrays on sections when\n  `ml` is on PATH and a domain can be inferred; arrays are empty otherwise\n  with no error.\n- `sd plan submit --record-decision` invokes `ml record` on success when `ml`\n  is available; submit succeeds atomically even when the outbound `ml` call\n  fails.\n- All 3 child tasks closed with passing tests.\n\n## Design decision (locked)\n\nShell out to `ml query --json` / `ml record` via `Bun.spawnSync` and resolve\n`ml` with `Bun.which(\"ml\")`. **Do NOT** import `@os-eco/mulch-cli`\nprogrammatically — soft coupling and language-agnostic.\n\n## Dependencies\n\n- Depends on Phase 1 (prompt + submit commands must exist).\n- Wired at the feature level by the orchestrator.","blocks":["seeds-8fdb"],"closedAt":"2026-05-06T19:28:11.792Z"}
{"id":"seeds-fc62","title":"Parse plan_templates block from config.yaml into typed PlanTemplate","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:07.668Z","updatedAt":"2026-05-06T18:57:43.804Z","description":"## What\n\nAdd typed loading of the `plan_templates:` block from `.seeds/config.yaml` into seeds.\n\n1. Add to `src/types.ts`:\n   ```ts\n   export interface SectionSpec {\n     required: boolean;\n     kind: \"text\" | \"list\" | \"steps\" | Record<string, SectionSpec>;\n     prompt: string;\n     min_length?: number;\n     min?: number;\n     item?: SectionSpec | \"text\";\n     mulch_source?: string;\n   }\n   export interface PlanTemplate {\n     name: string;\n     sections: Record<string, SectionSpec>;\n   }\n   ```\n2. Extend `src/config.ts` with `loadPlanTemplates(): Record<string, PlanTemplate>`.\n   - Read parsed config (relies on the upgraded parser from the blocking task).\n   - If `plan_templates:` block is missing or empty, return built-ins (just `feature` from Phase 1 — keep behavior backward-compatible).\n   - User-declared templates with the same name as a built-in override the built-in.\n   - Validate every section has `required` (bool), `kind` (known value or object spec), and `prompt` (string).\n   - Reject unknown `kind` values with a clear error: `plan_templates.<name>.sections.<section>: unknown kind '<value>' (expected text|list|steps|object)`.\n\n## Why\n\nPer PLAN_SPEC.md:261–325, templates are config-declared with required/optional sections. Phase 2 needs typed access to these templates so the schema generator (next task) can compile them into AJV validators.\n\n## Acceptance\n\n- `loadPlanTemplates()` returns typed `PlanTemplate` records.\n- Built-in `feature` template is loaded when config has no `plan_templates:` block.\n- User-declared `feature` template fully overrides the built-in.\n- Custom templates (e.g., `spike` from PLAN_SPEC.md:285–310) load correctly.\n- Unknown `kind` is rejected with the error format above.\n- Round-trip test: read a YAML file with a custom template, re-serialize via the typed structure, parse again — equivalent.\n- `bun test && bun run lint && bun run typecheck` clean.\n\n## Files\n\n- `src/types.ts` — add `PlanTemplate`, `SectionSpec` (and re-export)\n- `src/config.ts` — add `loadPlanTemplates`\n- `src/config.test.ts` (or new `src/plan-templates.test.ts`) — round-trip + validation tests\n\n## Spec references\n\n- PLAN_SPEC.md:36–78 — config.yaml plan_templates block\n- PLAN_SPEC.md:80–90 — section field reference\n- PLAN_SPEC.md:261–325 — templates customization, kinds, default feature template\n\n## Blocked by\n\n- Task: \"Upgrade src/yaml.ts to support nested maps + lists\"","blocks":["seeds-c95d","seeds-6bd8"],"closedAt":"2026-05-06T18:57:43.804Z","closeReason":"added SectionSpec/PlanTemplate to types.ts; loadPlanTemplates() in config.ts with built-in fallback + override semantics; renamed runtime PlanTemplate to CompiledTemplate to avoid collision; 9 tests"}
{"id":"seeds-8640","title":"sd plan prompt <seed-id> [--template <name>]","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:08:08.709Z","updatedAt":"2026-05-06T18:29:43.731Z","description":"## What\n\nImplement `sd plan prompt <seed-id> [--template <name>]` — emits the structured prompt JSON the LLM consumes to fill out a plan.\n\nPLAN_SPEC.md references:\n- Successful path: lines 162-175\n- Prompt JSON shape: lines 200-234\n- Default template inference: line 271 + Open Question 4 (line 429)\n\n### Default template inference\n\nPer the spec recommendation at line 429:\n- `task` → `feature`\n- `bug` → `bug`  *(deferred — not in Phase 1)*\n- `feature` → `feature`\n- `epic` → `feature`\n\nIn Phase 1 only `feature` exists, so any seed type maps to `feature` by default. `--template` always overrides.\n\n### Output shape\n\nEmit JSON matching PLAN_SPEC.md lines 200-234:\n\n```json\n{\n  \"plan_request\": {\n    \"seed\": \"seeds-9c4d\",\n    \"template\": \"feature\",\n    \"instructions\": \"Fill every section. Required fields are marked.\",\n    \"sections\": [\n      { \"name\": \"context\", \"required\": true, \"kind\": \"text\", \"min_length\": 50, \"prompt\": \"...\", \"prior_art\": [] },\n      ...\n    ],\n    \"validation\": {\n      \"all_required_present\": true,\n      \"min_steps\": 2,\n      \"min_acceptance\": 1\n    }\n  }\n}\n```\n\n`prior_art: []` — Phase 1 emits empty arrays. Phase 3 fills them from mulch.\n\n### Validation rules block\n\n`min_steps` and `min_acceptance` are computed from the template's section `min` values. `all_required_present` is always `true` (it's an instruction, not a config knob).\n\n### Errors\n\n- Unknown seed → exit non-zero with clear message\n- Unknown template name → exit non-zero, hint the available list\n\n## Why\n\nStage 1 of the planning loop. The output is the contract between seeds and the LLM; getting the JSON shape exactly right means the LLM can fill it out and round-trip back through `submit` without extra glue.\n\n## Acceptance\n\n- `sd plan prompt seeds-XXXX` (human, non-JSON) prints something readable\n- `sd plan prompt seeds-XXXX --json` emits the full `plan_request` JSON above\n- Default template is inferred from seed.type (`task`/`feature`/`epic` → `feature`)\n- `--template feature` is accepted; `--template missing` errors with the available list\n- Unknown seed errors cleanly\n- Tests in `src/commands/plan.test.ts` cover human output, JSON output, default inference, and both error paths\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/plan.ts` (edit — add `prompt` subcommand)\n- `src/commands/plan.test.ts` (edit)\n\n## Depends on\n\n- Task 1 (types)\n- Task 2 (store — read seed)\n- Task 3 (template definition + section metadata)\n- Task 4 (command parent)","blocks":["seeds-a672"],"closedAt":"2026-05-06T18:29:43.731Z","closeReason":"sd plan prompt landed: --json emits documented plan_request shape, --template override + missing-template error, unknown-seed error, default inference all tested"}
{"id":"seeds-d82f","title":"Domain inference helper for sd plan (mulch integration)","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:11.869Z","updatedAt":"2026-05-06T19:28:11.792Z","description":"# Domain inference helper for `sd plan`\n\nShared helper that infers the mulch `--domain` to query for a given seed.\nUsed by both prior_art enrichment (`sd plan prompt`) and outbound\n`--record-decision` (`sd plan submit`).\n\n## What\n\nImplement a helper that, given a seed, returns the mulch domain to use (or\n`null` if none can be inferred). Inference order per PLAN_SPEC.md:347:\n\n1. Explicit `--domain <name>` flag passed on the command.\n2. Seed's `labels` intersected with mulch's declared domains\n   (`ml status --json` lists them).\n3. File anchors: `git diff --name-only` against any files referenced in the\n   seed's description, mapped to mulch domains by directory anchor.\n\nIf none match, return `null` and callers skip prior_art queries (empty\narrays) — does not error.\n\n## Why\n\n- Both Phase 3 features (per-section `prior_art` enrichment and\n  `--record-decision` outbound write) need the same domain answer.\n  Centralizing keeps behavior consistent and testable.\n- Spec PLAN_SPEC.md:344-352 mandates soft coupling: when `ml status` errors\n  or returns no domains, planning still works.\n\n## Acceptance\n\n- Returns the explicit `--domain` value when passed, with no `ml` call.\n- Returns the matching domain when seed labels intersect `ml status --json`\n  domains.\n- Falls back to file-anchor mapping via `git diff --name-only` against files\n  parsed out of the seed description.\n- Returns `null` (graceful degradation) when:\n  - `ml` is not on PATH (`Bun.which(\"ml\")` returns null), OR\n  - `ml status --json` exits non-zero, OR\n  - no signal matches.\n- Unit tests cover each branch including the degradation paths. Tests use a\n  fake `ml` script on PATH (a tempdir bin/ prepended to `$PATH`) to avoid\n  depending on a real mulch install.\n\n## Files\n\n- New: `src/plan-domain.ts` (helper module).\n- New: `src/plan-domain.test.ts` (colocated tests, real I/O per project\n  testing convention).\n- Call site: `src/commands/plan.ts` `prompt` handler (passes inferred\n  domain into per-section enrichment).\n\n## Implementation notes\n\n- Use `Bun.which(\"ml\")` to resolve `ml` — explicitly required; do not assume\n  PATH.\n- Use `Bun.spawnSync` for shelling out (matches existing seeds pattern for\n  git invocations).\n- File-anchor parsing: simple regex over the description for tokens that\n  look like paths (`src/...`, `*.ts`, etc.), then `git diff --name-only`\n  filtered to those references.\n- This task **blocks** the per-section prior_art task and the\n  `--record-decision` task. Both consume the same inferred domain.","blocks":["seeds-bd0b","seeds-f170","seeds-ad53"],"closedAt":"2026-05-06T19:28:11.792Z"}
{"id":"seeds-f873","title":"Phase 4: Sub-plans (nested decomposition)","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-06T18:08:20.413Z","updatedAt":"2026-05-06T19:48:53.072Z","description":"## What\n\nPhase 4 unlocks recursive plan decomposition. A step in a plan template's `steps` kind may declare `plan_template: <name>`; on `sd plan submit`, the spawned child seed is created with `requires_plan: true`. `sd plan show` then recurses through nested plans up to a configurable depth (default 3).\n\nSpec references:\n- PLAN_SPEC.md:328-342 (Nested Plans, including the load-bearing invariant at line 342: \"A child seed marked `requires_plan: true` is excluded from `sd ready` until its sub-plan reaches `approved`.\")\n- PLAN_SPEC.md:36-78 (steps kind on plan templates — host for `plan_template` field)\n- PLAN_SPEC.md:425, 430 (open question on `max_plan_depth` — adopt spec recommendation: configurable in config.yaml, default 3)\n\n## Why\n\nWithout nested plans, epics that decompose into other epics force a context switch: plan the parent, manually file child epics, plan each separately. With sub-plans, a single plan submission can declare which children themselves require planning, and the LLM walks each via `sd plan prompt <child>` recursively. This decomposes complex work in-place without losing the plan-graph topology.\n\nThe `requires_plan` invariant (spec:342) guarantees no implementation begins on un-planned epic branches — a critical safety property for autonomous swarms.\n\n## Acceptance\n\n- A step with `plan_template: <name>` spawns a child seed where `requires_plan: true`.\n- A seed with `requires_plan: true` is hidden from `sd ready` until its plan reaches `approved`.\n- `sd plan show` recursively renders nested plans down to `max_plan_depth` (default 3), with truncation hints at the boundary.\n- Sub-plans can stack arbitrarily deep at the data layer; only display is depth-limited.\n- All three child tasks closed; quality gates pass.\n\n## Tasks\n\n1. `requires_plan` field on Issue + `sd ready` exclusion\n2. Step `plan_template` field — spawn child with `requires_plan: true`\n3. `sd plan show` recursive display + `max_plan_depth` config\n\n## Dependencies\n\n- Depends on Phase 1 (plan data model) and Phase 2 (custom templates — `plan_template` field is part of template config). The orchestrator wires Phase 2 -> Phase 4 at the feature level.\n\n## Files (cross-task)\n\n- `src/types.ts` — `Issue.requires_plan`, `PlanTemplate.steps[].plan_template`\n- `src/commands/plan.ts` — submit (spawn with requires_plan), show (recursive)\n- `src/commands/ready.ts`, `src/filter.ts` — exclude requires_plan seeds\n- `src/config.ts`, `.seeds/config.yaml` — `max_plan_depth`","blocks":["seeds-8fdb"],"closedAt":"2026-05-06T19:48:53.072Z","closeReason":"Phase 4 complete: requires_plan + plan_template + recursive show shipped; tests cover all acceptance criteria; quality gates green"}
{"id":"seeds-6bd8","title":"AJV schema generation from PlanTemplate config","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:23.210Z","updatedAt":"2026-05-06T19:03:13.720Z","description":"## What\n\nReplace Phase 1's hand-written feature plan schema with a `generatePlanSchema(template: PlanTemplate): JSONSchema` function that walks `SectionSpec` recursively and emits a JSON Schema honoring all declared validation rules.\n\nAdd a new module `src/plan-schema.ts` (or similar). Use the validation primitives from `src/validation.ts` (added in Phase 0).\n\nSchema generation must honor:\n- `required` (top-level array of section names where `required: true`).\n- `kind: text` → `{ type: \"string\", minLength: <min_length || 1> }`.\n- `kind: list` → `{ type: \"array\", minItems: <min || 0>, items: <item-schema> }`.\n- `kind: steps` → `{ type: \"array\", minItems: <min || 0>, items: <step-schema> }` where step-schema is `{title, type, priority, blocks, plan_template?}`.\n- `item: \"text\"` → `{ type: \"string\" }`.\n- `item: <object spec>` → `{ type: \"object\", properties: ..., required: ... }` recursively.\n- Object-spec `kind` (nested `Record<string, SectionSpec>`) → recursive object schema.\n\n## Why\n\nPer PLAN_SPEC.md:312, \"Validation is generated from the template config the same way mulch generates AJV schemas from `custom_types`.\" Phase 1 shipped a hand-written feature schema as a stopgap; Phase 2 must dynamically generate schemas from any template, including user-defined ones.\n\nWithout dynamic schema generation, custom templates can't be validated, defeating the customization goal of Phase 2.\n\n## Acceptance\n\n- `generatePlanSchema(template)` returns a JSON Schema object compatible with the validation harness from `src/validation.ts`.\n- Round-trip test: built-in `feature` template produces a schema that validates the same submissions accepted by Phase 1's hand-written schema (and rejects the same failures).\n- Custom template test: the `spike` example from PLAN_SPEC.md:285–310 compiles to a valid schema and validates submissions correctly.\n- Edge cases tested:\n  - Required vs optional section presence.\n  - `min_length` enforcement on text.\n  - `min` enforcement on list and steps.\n  - Object-spec list items (e.g., `alternatives` in feature template).\n  - Step `blocks` array references — values must be integers (deeper validation that they reference valid step indices is fine to keep at submit-time runtime checks rather than schema-level).\n- `bun test && bun run lint && bun run typecheck` clean.\n\n## Files\n\n- `src/plan-schema.ts` (NEW) — `generatePlanSchema` + helpers\n- `src/plan-schema.test.ts` (NEW)\n- `src/commands/plan-submit.ts` — swap the hand-written schema call site for `generatePlanSchema(loadPlanTemplates()[name])`\n\n## Spec references\n\n- PLAN_SPEC.md:80–90 — section field reference (validation surface)\n- PLAN_SPEC.md:273–280 — section kind reference\n- PLAN_SPEC.md:285–310 — custom template example (spike)\n- PLAN_SPEC.md:358–371 — validation requirements\n\n## Blocked by\n\n- Task: \"Parse plan_templates block from config.yaml into typed PlanTemplate\"","blocks":["seeds-c95d","seeds-9bd4"],"closedAt":"2026-05-06T19:03:13.720Z","closeReason":"src/plan-schema.ts: generatePlanSchema + compilePlanTemplate; replaces hand-written feature schema; plan.ts uses loadPlanTemplates()+compilePlanTemplate; deleted obsolete plan-templates/; 16 tests"}
{"id":"seeds-f170","title":"Per-section prior_art via ml query --json shell-out","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:27.546Z","updatedAt":"2026-05-06T19:28:11.792Z","description":"# Per-section prior_art via `ml query --json` shell-out\n\nFor each section on a plan template, populate the section's `prior_art`\narray with top mulch records of the appropriate type when `ml` is available.\n\n## What\n\nIn the `sd plan prompt` flow, for each section, resolve the mulch record\ntype to query:\n\n- Explicit `mulch_source: <type>` hint on the section (PLAN_SPEC.md:36-78,\n  see `risks` example at line 70 with `mulch_source: failure`).\n- Otherwise, well-known section name defaults (PLAN_SPEC.md:349):\n  - `approach` -> pattern AND decision\n  - `risks`   -> failure\n  - `acceptance` -> guide\n- Sections without a hint and not matching a well-known name -> skip.\n\nFor each (section, type) pair, shell out:\n\n```\nml query --domain <inferred-domain> --type <type> --json --limit 5\n```\n\nTop results become `prior_art` entries with the shape from\nPLAN_SPEC.md:222-225:\n\n```\n[{ \"id\": \"mx-902\", \"type\": \"failure\", \"summary\": \"...\", \"relevance\": <number> }]\n```\n\n## Why\n\nPLAN_SPEC.md:344-352: prior art grounds the LLM's planning in real\nconventions, patterns, decisions, and failures already recorded in mulch.\nSoft coupling means absent `ml` must not break the prompt.\n\n## Acceptance\n\n- Sections with `mulch_source` hints get `prior_art` populated when `ml` is\n  available and a domain is inferred.\n- Well-known section names (`approach`, `risks`, `acceptance`) get the\n  correct default record types per PLAN_SPEC.md:349.\n- `prior_art` entry shape matches PLAN_SPEC.md:222-225\n  (`{id, type, summary, relevance}`).\n- **Integration test 1:** stub `ml` via a fake script on PATH (tempdir bin/\n  prepended to `$PATH`) that emits canned JSON. Assert `prior_art` is\n  populated correctly across multiple sections.\n- **Integration test 2:** `ml` absent (`Bun.which(\"ml\")` returns null) ->\n  all `prior_art` arrays are empty, no crash, no error logged to stderr,\n  validation rules unaffected, prompt JSON still emitted.\n- **Integration test 3:** `ml query` exits non-zero for one section -> that\n  section's `prior_art` is empty; other sections still populated; submit\n  flow unaffected.\n- No domain inferred (helper returns null) -> all `prior_art` arrays empty.\n\n## Files\n\n- New: `src/plan-mulch.ts` (helper module that wraps `ml query` shell-out).\n- New: `src/plan-mulch.test.ts` (colocated integration tests).\n- Call site: `src/commands/plan.ts` `prompt` handler — after building the\n  prompt JSON skeleton, walks sections and fills `prior_art` arrays.\n\n## Implementation notes\n\n- Use `Bun.spawnSync` (already used elsewhere in seeds for git).\n- Resolve `ml` via `Bun.which(\\\"ml\\\")` — explicitly required, easy to forget.\n- Graceful degradation: any failure in the mulch path produces empty\n  `prior_art`, never throws, never logs to stderr (silent fall-through).\n- Reuses the domain inference helper from the blocker task.","blocks":["seeds-bd0b"],"closedAt":"2026-05-06T19:28:11.792Z"}
{"id":"seeds-a001","title":"sd plan submit <seed-id> --plan <file>","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:08:32.144Z","updatedAt":"2026-05-06T18:33:48.923Z","description":"## What\n\nImplement `sd plan submit <seed-id> --plan <file>` — the validation + spawn loop.\n\nPLAN_SPEC.md references:\n- Successful flow: lines 162-175 (output shape) + 236-257 (submission JSON shape)\n- Validation failure flow: lines 178-197\n- Re-submission rejection: lines 374-391 (Phase 1 implements rejection only; `--overwrite` is Phase 2)\n- `blocks: [step_index]` translation: line 257\n\n### Behavior\n\n1. Read plan JSON from `<file>` or `-` (stdin).\n2. Validate against the template's AJV schema (Task 3).\n3. **On validation failure**: emit the partial-state diff JSON to stderr (PLAN_SPEC.md lines 180-195 shape), exit non-zero. Do not write anything.\n4. **On success**:\n   - Generate a new plan id `pl-{4hex}` (mirror seed id generator from `src/id.ts`)\n   - For each entry in `sections.steps[]`, allocate a new seed id and create a child seed: title/type/priority from the step, plus `plan_id` and `plan_step_index` set on the child\n   - Translate `step.blocks: [index]` references → child seed ids → write to each child's `blockedBy` field\n   - Append the plan row to `plans.jsonl` with `status: approved`, `revision: 1`, `children: [...]`\n   - Update the parent seed: set `plan_id` back-pointer\n5. Resubmission of a non-`draft` plan for the same seed → reject with a clear message (\"Use --overwrite to replace it\" — but Phase 1 does not implement `--overwrite`; just print the rejection and exit non-zero)\n\n### Spawn ordering / atomicity\n\nAll writes (children + plan row + parent seed update) must happen under appropriate locks so a crash mid-spawn doesn't leave dangling state. Acceptable approach: lock issues + plans, build all rows in memory, then commit. Document the chosen ordering in code comments.\n\n### Output\n\n- Human: matches PLAN_SPEC.md lines 172-175 (`✓ plan pl-a1b2 created`, `✓ spawned N child seeds: ...`, `✓ <parent> now blocked by N children`)\n- `--json`: structured `{plan_id, children: [...], parent_seed}`\n\n## Why\n\nThis is the engine of the planning workflow. Getting `blocks: [step_index]` → `blockedBy` translation right is the linchpin — the index-to-id remap has to happen *after* all child IDs are allocated.\n\n## Acceptance\n\n- **Golden path test**: 4-step plan with `steps[1].blocks: [0]` and `steps[3].blocks: [2]` produces:\n  - 4 child seeds in `issues.jsonl`\n  - Each child has `plan_id` and `plan_step_index` set\n  - `seeds-aa01.blockedBy === []`, `seeds-aa02.blockedBy === [seeds-aa01]`, etc. (correct id remap)\n  - Parent seed gains `plan_id` back-pointer\n  - One row in `plans.jsonl` with `status: approved`, `revision: 1`, `children: [seeds-aa01..04]`\n- **Validation-failure test**: missing `risks` (note: `risks` is optional — pick a *required* section like omit `context`); assert stderr is the partial-state diff JSON shape and `plans.jsonl` / `issues.jsonl` are unchanged\n- **Resubmit-without-overwrite test**: second submit for the same seed errors and writes nothing\n- Stdin input (`--plan -`) supported\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/plan.ts` (edit)\n- `src/commands/plan.test.ts` (edit — golden path, validation failure, resubmit rejection, stdin input)\n- May add helpers in `src/id.ts` (plan id generation)\n\n## Depends on\n\n- Task 1 (types)\n- Task 2 (store)\n- Task 3 (template + AJV schema)\n- Task 4 (command parent)","blocks":["seeds-a672","seeds-4b9c"],"closedAt":"2026-05-06T18:33:48.923Z","closeReason":"sd plan submit: golden path, validation failure (no writes), resubmit rejection, stdin input, unknown seed all tested. step.blocks[idx] -> child.blockedBy id remap; parent gets plan_id + blockedBy=children"}
{"id":"seeds-2fff","title":"Phase 4 Task 1: requires_plan field on Issue + sd ready exclusion","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:38.608Z","updatedAt":"2026-05-06T19:36:46.549Z","description":"## What\n\nAdd an optional `requires_plan?: boolean` flag to the `Issue` interface, and teach `sd ready` to exclude seeds that have `requires_plan: true` AND either no plan or a plan whose status is `draft`. Once the seed's plan reaches `approved`, the seed becomes ready-eligible again (subject to the normal `blockedBy` rules from the children spawned by that plan).\n\nThis task lays the load-bearing invariant for the entire phase. Spec PLAN_SPEC.md:342 states:\n\n> A child seed marked `requires_plan: true` is excluded from `sd ready` until its sub-plan reaches `approved`. This guarantees no implementation begins on un-planned epic branches.\n\nThe data-layer invariant: sub-plans can stack arbitrarily deep — only `sd plan show` is depth-limited (Task 3). The `requires_plan` flag is the gate that keeps un-planned epics out of the work queue, recursively.\n\nSpec references: PLAN_SPEC.md:328-342 (Nested Plans), PLAN_SPEC.md:339-342 (bidirectional linking + ready-exclusion invariant).\n\n## Why\n\nWithout this flag, `sd ready` would surface a spawned epic-typed child the moment its parent plan is approved, even though that child itself was declared as needing its own sub-plan. Implementation would begin on an un-planned branch — exactly what the planning workflow is designed to prevent.\n\nThis is the gate that propagates the plan-first discipline through arbitrarily deep decompositions.\n\n## Acceptance\n\n- `Issue` interface gains optional `requires_plan?: boolean` field; existing seeds without the field behave as before (treated as `false`).\n- New test: create a seed with `requires_plan: true` (no plan attached). Assert it does NOT appear in `sd ready` output.\n- Test: same seed, create a plan in `draft` status. Assert still hidden from `sd ready`.\n- Test: transition plan to `approved`. Assert the seed now appears in `sd ready` (assuming no other blockers).\n- Test: with the plan approved AND children spawned that block this seed via normal `blockedBy`, assert the seed remains hidden until children close — confirming the requires_plan check composes with the existing blocker check.\n- `--json` output includes `requires_plan` when present.\n\n## Files\n\n- `src/types.ts` — add `requires_plan?: boolean` to `Issue` interface.\n- `src/filter.ts` — extend the shared filter to honor `requires_plan` + plan-status check.\n- `src/commands/ready.ts` — wire the new exclusion path. (May need to read `plans.jsonl` to check plan status; coordinate with the Phase 1 plan store.)\n- Test files: extend `src/commands/ready.test.ts` (or create one) with the four cases above.","blocks":["seeds-f873","seeds-b78d","seeds-0a06"],"closedAt":"2026-05-06T19:36:46.549Z","closeReason":"implemented requires_plan field + sd ready exclusion; tests in src/commands/ready.test.ts"}
{"id":"seeds-6ccc","title":"sd plan show <pl-id>","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:08:39.701Z","updatedAt":"2026-05-06T18:34:43.344Z","description":"## What\n\nImplement `sd plan show <pl-id>` — display a plan with sections, children, status, revision, timestamps.\n\n### Human output\n\n- Plan id, parent seed, template, status, revision, createdAt/updatedAt\n- Sections rendered in template-order (context, approach, alternatives, steps, risks, acceptance)\n- Children rendered as a list with each child's id, title (resolved via store lookup), and status\n- \"Review suggested\" hint when `reviewedBy` is null (PLAN_SPEC.md line 410) — informational only\n\n### `--json` output\n\nFull plan row plus enriched child summaries (id, title, status). Use the existing `output.ts` JSON conventions.\n\n### Errors\n\n- Unknown plan id → clear error, exit non-zero, hint to run `sd plan list`\n\n## Why\n\nPrimary read-side surface for plans. Resolving children titles inline makes review usable without manual lookups.\n\n## Acceptance\n\n- `sd plan show pl-XXXX` (human) renders a valid plan readably\n- `sd plan show pl-XXXX --json` emits structured JSON\n- Unknown id errors cleanly\n- Test covers both output modes plus the error path\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/plan.ts` (edit)\n- `src/commands/plan.test.ts` (edit)\n\n## Depends on\n\n- Task 1 (types)\n- Task 2 (store)\n- Task 4 (command parent)","blocks":["seeds-a672","seeds-4b9c"],"closedAt":"2026-05-06T18:34:43.344Z","closeReason":"sd plan show: human + JSON output, child summaries (id/title/status), review-suggested hint when reviewedBy is null, unknown-id error"}
{"id":"seeds-def0","title":"Plan lifecycle auto-transitions (draft -> approved -> active -> done)","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:39.980Z","updatedAt":"2026-05-06T19:08:06.908Z","description":"## What\n\nWire automatic plan status transitions per PLAN_SPEC.md:111–127. The state machine:\n\n```\ndraft  →  approved  →  active        →  done\n              ↑             ↑               ↑\n        sd plan submit  child→in_progress  all children closed\n```\n\nTransitions to wire:\n1. `draft → approved`: Triggered by `sd plan submit` succeeding. Phase 1 already does this — verify the field is set and assert in tests.\n2. `approved → active`: Triggered when ANY child seed transitions to `in_progress`. Hook into the `sd update --status in_progress` flow in `src/commands/update.ts`. Must look up `plan_id` on the seed and bump the plan's status if currently `approved`.\n3. `active → done`: Triggered when ALL children of a plan are `closed`. Hook into `src/commands/close.ts` — after closing a seed with a `plan_id`, check the parent plan's children; if all closed, set plan status to `done` and stamp `updatedAt`.\n\nEdge case: re-opening a closed child (transitioning from `closed` back to `open` or `in_progress`) when the plan is `done` must revert the plan to `active`. Hook this into `src/commands/update.ts` similarly.\n\nAll transitions atomic under the plans.jsonl lock.\n\n## Why\n\nPer PLAN_SPEC.md:109–127, plan status is derived state — managed by seeds, not the user. Without auto-transitions, plan status stays at `approved` forever, defeating queryability. The reverse transition (`done → active`) on child reopen is required by the acceptance criterion below.\n\n## Acceptance\n\n- `sd plan submit` sets plan status to `approved` (verify in test).\n- Moving any child to `in_progress` flips the plan from `approved` to `active`.\n- Closing the last open child flips plan status from `active` to `done` with `updatedAt` stamped.\n- Re-opening a closed child of a `done` plan reverts plan status to `active`.\n- Idempotent: closing an already-closed seed does not regress plan status; moving an already-`active` plan's child to `in_progress` is a no-op for plan status.\n- Concurrent test: two children moved to `in_progress` simultaneously; only one final plan-status mutation observed (file lock works).\n- Integration test: create plan with 2 children, walk through full lifecycle (draft → approved → active → done), then reopen one child and verify revert to `active`, close again and verify back to `done`.\n- `bun test && bun run lint && bun run typecheck` clean.\n\n## Files\n\n- `src/commands/update.ts` — hook on status transitions\n- `src/commands/close.ts` — hook on close\n- `src/commands/plan-submit.ts` — verify draft→approved set\n- `src/store.ts` — may need a helper `updatePlanStatus(planId, newStatus)` if not already present\n- `src/commands/update.test.ts`, `src/commands/close.test.ts`, `src/commands/plan-lifecycle.test.ts` (NEW integration test)\n\n## Spec references\n\n- PLAN_SPEC.md:109–127 — plan lifecycle\n- PLAN_SPEC.md:120–123 — transition triggers\n\n## Notes\n\nThis task is INDEPENDENT of tasks 1–3 and 5; it can proceed in parallel. It touches plan store + status only, not templates or schemas.","blocks":["seeds-c95d"],"closedAt":"2026-05-06T19:08:06.908Z","closeReason":"src/plan-lifecycle.ts: computeNextPlanStatus + applyPlanTransitions; update.ts/close.ts hook on status change under plans+issues lock; 17 tests covering pure rules, idempotency, lifecycle walk-through"}
{"id":"seeds-ad53","title":"sd plan submit --record-decision outbound ml record","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:42.801Z","updatedAt":"2026-05-06T19:28:11.792Z","description":"# `sd plan submit --record-decision` outbound write\n\nAdd an opt-in `--record-decision` flag to `sd plan submit` that calls\n`ml record` after a successful submit, recording the chosen approach as a\nmulch `decision` linked back to the plan.\n\n## What\n\nPLAN_SPEC.md:354-356: off by default. When `--record-decision` is passed,\non a successful submit, shell out:\n\n```\nml record <inferred-domain> \\\n  --type decision \\\n  --rationale <approach-section-text> \\\n  --evidence-seeds <plan-id>\n```\n\n`<inferred-domain>` reuses the domain inference helper from the blocker\ntask. `<approach-section-text>` is the value of the plan's `approach`\nsection.\n\n## Why\n\n- Closes the loop: a chosen approach becomes durable mulch expertise linked\n  to the plan that produced it.\n- Off by default keeps seeds standalone (PLAN_SPEC.md:17, 354-356) and\n  avoids surprising users who have `ml` installed but don't want every plan\n  to mint a decision record.\n\n## Acceptance\n\n- `--record-decision` is a new flag on `sd plan submit`, default off.\n- When the flag is set AND submit succeeds AND `ml` is on PATH AND a domain\n  is inferred: `ml record` is invoked with the documented args.\n- When `ml` is **not** on PATH (`Bun.which(\\\"ml\\\")` returns null): submit\n  still succeeds; warn on stderr (`ml not found; skipping --record-decision`)\n  but exit 0.\n- When `ml record` exits non-zero: submit still succeeds atomically; warn\n  on stderr but exit 0. The plan write must remain atomic — outbound write\n  is best-effort.\n- When no domain can be inferred: warn on stderr; submit still succeeds.\n- **Integration test 1:** stub `ml` via a fake script on PATH that records\n  invocation args. Submit with `--record-decision` -> asserts `ml record`\n  was called with `<domain> --type decision --rationale <text>\n  --evidence-seeds <plan-id>`.\n- **Integration test 2:** stub `ml record` to exit non-zero. Submit still\n  succeeds (exit 0), plan file written, stderr warns. Assert plan JSON is\n  intact in `plans.jsonl`.\n- **Integration test 3:** `ml` absent. Submit with `--record-decision`\n  succeeds with stderr warning.\n\n## Files\n\n- Edit: `src/commands/plan.ts` — add `--record-decision` flag to the\n  `submit` subcommand and post-success hook.\n- Edit: `src/commands/plan.test.ts` (or new `src/commands/plan-submit.test.ts`)\n  — integration tests for the three branches above.\n- Reuses `src/plan-domain.ts` from the blocker task and `Bun.spawnSync`\n  pattern from the prior_art task.\n\n## Implementation notes\n\n- Use `Bun.which(\\\"ml\\\")` to resolve — explicit, never assume PATH.\n- Use `Bun.spawnSync` — matches existing seeds pattern.\n- Graceful degradation is mandatory: the plan write must remain atomic. The\n  outbound `ml record` call happens **after** the plan is fully persisted to\n  `plans.jsonl` and child seeds are spawned. A failed `ml record` must not\n  roll back any of that.\n- Stderr warnings only — never throw, never exit non-zero from this path.","blocks":["seeds-bd0b"],"closedAt":"2026-05-06T19:28:11.792Z"}
{"id":"seeds-85b6","title":"sd plan list [--seed --status --outcome --template]","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:08:46.486Z","updatedAt":"2026-05-06T18:35:40.821Z","description":"## What\n\nImplement `sd plan list` with filters per PLAN_SPEC.md lines 142-143:\n\n- `--seed <id>` — only plans for this parent seed\n- `--status <draft|approved|active|done>` — filter by status\n- `--outcome <success|partial|failure>` — filter by outcome\n- `--template <name>` — filter by template name\n\nDefault sort: `createdAt` desc. Mirror the existing `sd list` ergonomics for consistency.\n\n### Output\n\n- Human: tabular (id, seed, template, status, revision, children count, createdAt)\n- `--json`: array of plan rows (raw, no enrichment)\n\n## Why\n\nDiscovery surface for plans. Lets agents and humans answer \"what plans are still in draft?\", \"which plans failed?\", etc. without grepping JSONL.\n\n## Acceptance\n\n- All four filters tested individually (single-filter assertions)\n- Combined filters tested (e.g. `--seed X --status approved`)\n- Empty result returns valid empty list / empty JSON array (not an error)\n- `--json` and human output both covered\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/plan.ts` (edit)\n- `src/commands/plan.test.ts` (edit)\n\n## Depends on\n\n- Task 1 (types)\n- Task 2 (store)\n- Task 4 (command parent)","blocks":["seeds-a672"],"closedAt":"2026-05-06T18:35:40.821Z","closeReason":"sd plan list: --seed/--status/--outcome/--template filters, combined filters, empty result, invalid status, human output all tested. Default sort createdAt desc"}
{"id":"seeds-6d30","title":"Phase 5: Outcomes, review, secondary templates, docs","status":"closed","type":"feature","priority":3,"createdAt":"2026-05-06T18:08:46.793Z","updatedAt":"2026-05-06T20:21:50.158Z","description":"# Phase 5: Outcomes, review, secondary templates, docs\n\nRounds out the `sd plan` feature with the soft-review workflow, plan outcomes, the two remaining built-in templates (`bug`, `refactor`), and documentation. Lower priority than Phases 1-4 — these are enhancements, not the core spawn loop.\n\n## Scope\n\nPer PLAN_SPEC.md:\n- Outcomes (lines 393-402): `sd plan outcome <pl-id> --result success|partial|failure [--note <text>]`\n- Reviewer suggestion (lines 404-413): informational `reviewedBy` field + `sd plan review` command + show-hint\n- Built-in `bug` template (line 268): adds `reproduction` and `root_cause` sections\n- Built-in `refactor` template (line 269): adds `behavior_invariant` (must stay equal)\n- Docs: README \"Planning\" section, CLAUDE.md updates, `sd prime` / `sd onboard` plan-aware context\n\n## Cross-phase deps\n\nDepends on Phase 2 (custom templates infrastructure) — `bug` and `refactor` flow through the same machinery as `feature`. Orchestrator wires Phase 2 -> Phase 5 at the feature level.\n\n## Tasks\n\n1. `sd plan outcome <pl-id> --result ... [--note ...]`\n2. `sd plan review <pl-id> --by <name>` + `reviewedBy` field + \"review suggested\" hint\n3. Built-in `bug` template\n4. Built-in `refactor` template\n5. Docs: README, CLAUDE.md, sd prime / sd onboard awareness\n\n## Intra-phase deps\n\n- Tasks 1, 2, 3, 4 are independent of each other.\n- Task 5 (docs) depends on tasks 1-4 landing first.\n- All tasks block this phase feature; this phase feature blocks parent epic seeds-8fdb.","blocks":["seeds-8fdb"],"closedAt":"2026-05-06T20:15:16.864Z","closeReason":"Phase 5 complete: outcome (seeds-aa81), review (seeds-2a2d), bug template (seeds-0189), refactor template (seeds-6730), docs (seeds-5935)"}
{"id":"seeds-b78d","title":"Phase 4 Task 2: Step plan_template field — spawn child with requires_plan: true","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:08:52.574Z","updatedAt":"2026-05-06T19:41:47.314Z","description":"## What\n\nExtend the `steps` kind in `PlanTemplate` (src/types.ts) so each step entry can carry an optional `plan_template?: string` field. In `sd plan submit`, when a step declares `plan_template`, the spawned child seed is created with `requires_plan: true`.\n\nSpec references:\n- PLAN_SPEC.md:329-338 — `plan_template` declaration on steps; example:\n  ```json\n  {\n    \"title\": \"OAuth integration\",\n    \"type\": \"epic\",\n    \"priority\": 1,\n    \"plan_template\": \"feature\"\n  }\n  ```\n- PLAN_SPEC.md:339-342 — bidirectional linking is preserved: the sub-plan's `seed` field points at the spawned child; the spawned child's `plan_id` is null until that child's own plan is created. This invariant means: marking `requires_plan: true` is the only thing the parent submission does for the child — actual sub-plan creation is deferred until the LLM runs `sd plan prompt <child>` separately.\n\n## Why\n\nThis is the wiring step that turns the data-model flag from Task 1 into something a plan author can actually request. Without it, `requires_plan` is a dead field — there's no way to set it via the planning workflow.\n\n## Acceptance\n\n- `PlanTemplate.steps[]` gains optional `plan_template?: string` (must reference a known template name in `plan_templates`; validation on submit).\n- Submit-time validation: unknown `plan_template` name produces a friendly error pointing at the template config.\n- Test: submit a feature plan with one step `{title: \"OAuth integration\", type: \"epic\", plan_template: \"feature\"}`. Assert:\n  - The spawned child seed has `requires_plan: true`.\n  - The child is excluded from `sd ready` (composes with Task 1).\n  - The child's `plan_id` is null (no auto-spawned plan).\n- Test: run `sd plan prompt <child-seed-id>`; assert the recursion entry point works — output references the named template and the child is now plannable.\n- Test: `plan_template` referencing a non-existent template fails validation with a clear message.\n\n## Files\n\n- `src/types.ts` — `PlanTemplate.steps[].plan_template?: string`.\n- `src/commands/plan.ts` — submit handler: pass `requires_plan: true` when spawning child seeds for steps with `plan_template`. Validation hook for unknown template names.\n- Tests: extend the plan submit test suite (Phase 1 surface) with the cases above.\n\n## Dependencies\n\n- Blocked by Task 1 (`requires_plan` field must exist before this task can set it).","blocks":["seeds-f873","seeds-0a06"],"closedAt":"2026-05-06T19:41:47.314Z","closeReason":"step.plan_template wiring: spawn child with requires_plan + null plan_id; submit-time validation; sd plan prompt inherits template from parent step"}
{"id":"seeds-c0f0","title":"sd plan validate <pl-id>","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:09:01.847Z","updatedAt":"2026-05-06T18:37:02.521Z","description":"## What\n\nImplement `sd plan validate <pl-id>` — re-runs the plan's submitted `sections` against the *current* template definition.\n\nPLAN_SPEC.md references:\n- Command surface: lines 148-149\n- Validation failure shape: lines 180-195\n\n### Behavior\n\n1. Load the plan from `plans.jsonl`\n2. Resolve its `template` name → current template definition (Task 3 hardcoded `feature` in Phase 1)\n3. Run the AJV schema against `plan.sections`\n4. **Pass** → exit 0, human \"✓ plan pl-XXXX valid\", `--json: {valid: true, plan_id: \"...\"}`\n5. **Fail** → emit the partial-state diff JSON (PLAN_SPEC.md lines 180-195 shape) to stderr, exit non-zero. The shape and helpers are shared with `sd plan submit` (Task 6).\n\n### Phase 1 utility\n\nIn Phase 1 the template is hardcoded so this command is mostly a sanity check / regression detector. It becomes useful in Phase 2 once templates can be edited via config (a tightened template can flag previously-valid plans as now-invalid). Implementing it now keeps Phase 2 from needing to retrofit the validate path.\n\n## Why\n\nRe-runnable validation per PLAN_SPEC.md line 148. Cheap to add now; expensive to retrofit if the partial-state diff plumbing is built into `submit` only.\n\n## Acceptance\n\n- Valid plan passes (exit 0, human + JSON output)\n- Synthesized invalid plan (e.g. tamper with the row to remove `acceptance`) surfaces partial-state diff with the same JSON shape `submit` uses\n- Unknown plan id errors cleanly\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/plan.ts` (edit)\n- `src/commands/plan.test.ts` (edit)\n\n## Depends on\n\n- Task 1 (types)\n- Task 2 (store)\n- Task 3 (template + AJV schema)\n- Task 4 (command parent)","blocks":["seeds-a672"],"closedAt":"2026-05-06T18:37:02.521Z","closeReason":"sd plan validate: valid plan passes, tampered plan emits partial-state diff with same shape as submit, unknown id error"}
{"id":"seeds-9bd4","title":"sd plan submit --overwrite for plan revisions","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:09:02.260Z","updatedAt":"2026-05-06T19:13:29.176Z","description":"## What\n\nImplement the `--overwrite` flag on `sd plan submit` per PLAN_SPEC.md:374–391.\n\nBehavior:\n1. Re-submitting a plan for a seed that already has a non-`draft` plan WITHOUT `--overwrite` is rejected:\n   ```\n   ✗ plan pl-a1b2 already exists for seeds-9c4d (status: approved, revision: 1)\n     Use --overwrite to replace it.\n   ```\n2. With `--overwrite`:\n   a. Atomically replace the existing `plans.jsonl` row (rewrite under lock).\n   b. Increment `revision` (was 1 → 2).\n   c. Re-run validation (using the schema generator from blocking task).\n   d. Diff new `steps` against previous `children`. Match by step title (or by `step_id` if templates declare stable IDs — V1 may match by title only).\n   e. Children whose corresponding step is GONE in the new plan are flagged on stderr as **obsolete**, one suggestion line each:\n      ```\n      sd close seeds-aa03 --reason \"obsoleted by plan pl-a1b2 revision 2\"\n      ```\n   f. Do NOT auto-close obsolete children — the LLM has more context to decide.\n   g. NEW steps (no matching child by title) spawn new child seeds, wired to the parent and `plan_id`-back-pointed.\n   h. Existing matching children are kept as-is — do not duplicate or rewrite them.\n\n3. Plan revisions are NOT preserved as separate rows — only the latest plan row exists; git history of `plans.jsonl` is the audit trail (PLAN_SPEC.md:391).\n\n## Why\n\nPer PLAN_SPEC.md:374–391, plans evolve as understanding deepens. Phase 1 only supports first-time submission; revisions are how teams handle \"we learned more, the plan needs to change.\" Without this, every plan revision requires manual JSONL surgery.\n\n## Acceptance\n\n- Re-submit without `--overwrite` exits non-zero with the expected stderr format.\n- `--overwrite` rewrites the plans.jsonl row in place (no new row appended).\n- `revision` increments by exactly 1 per overwrite.\n- Validation re-runs against the current template — invalid revision is rejected even with `--overwrite`.\n- Obsolete children are listed correctly to stderr in the suggestion format above.\n- New steps spawn new children with proper `plan_id` and `blockedBy` wiring.\n- Existing matching children are preserved (same ID, untouched).\n- Git diff of `plans.jsonl` after overwrite shows ONE-LINE change (the row mutation), not append + delete.\n- Test cases:\n  (a) Overwrite without flag rejects with exit code != 0 and the expected message.\n  (b) Overwrite with flag rewrites plan, prints obsolete child suggestions correctly.\n  (c) Revision increments from 1 to 2 (and 2 to 3 on a second overwrite).\n  (d) Children whose step title is unchanged keep the same seed ID across overwrites.\n  (e) Validation failure on overwrite leaves the previous plan row untouched.\n- `bun test && bun run lint && bun run typecheck` clean.\n\n## Files\n\n- `src/commands/plan-submit.ts` — add `--overwrite` flag handling\n- `src/store.ts` — may need atomic plan-row-replace helper\n- `src/commands/plan-submit.test.ts` — extended cases\n\n## Spec references\n\n- PLAN_SPEC.md:374–391 — re-submission and --overwrite semantics\n\n## Blocked by\n\n- Task: \"AJV schema generation from PlanTemplate config\" (overwrite needs the schema generator for re-validation)","blocks":["seeds-c95d"],"closedAt":"2026-05-06T19:13:29.176Z"}
{"id":"seeds-aa81","title":"sd plan outcome <pl-id> --result <success|partial|failure> [--note <text>]","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:09:05.706Z","updatedAt":"2026-05-06T20:07:25.366Z","description":"# sd plan outcome\n\n## What\n\nImplement `sd plan outcome <pl-id> --result <success|partial|failure> [--note <text>]`. Appends a lightweight outcome to a plan record so external tooling (or future seeds commands) can build on it.\n\nSpec: PLAN_SPEC.md:393-402.\n\n> Outcome values: `success | partial | failure`. Aggregation, retros, template-evolution-from-failures are deliberately out of scope.\n\n## Why\n\nPlans need a terminal signal beyond `done` status. Outcomes capture whether the planned work actually achieved its goal — without committing seeds to opinionated retro tooling. Storage-only by design (PLAN_SPEC.md:402).\n\n## Acceptance\n\n- New subcommand `sd plan outcome <pl-id> --result <value> [--note <text>]`.\n- `--result` accepts exactly `success`, `partial`, `failure`. Reject any other value with a clear error (and exit non-zero).\n- `--note <text>` is optional, free-form string.\n- Writes `outcome` (and `outcomeNote` when `--note` given) to the plan row in `.seeds/plans.jsonl` via the standard locked atomic-rewrite path.\n- The `outcome?` and `outcomeNote?` fields were already declared on the `Plan` interface in Phase 1 task 1; this task wires the command surface (and adds them to the interface if missing).\n- Lifecycle: outcome can be set on plans in `done` status. Per PLAN_SPEC.md:431, when a plan is marked `done` with open children, recommendation is **warning, not error** — surface a stderr warning (\"plan has N open children\") but do not fail. Same applies when an outcome is set on a plan whose children are still open.\n- Tests:\n  - Golden path: `--result success` writes the field.\n  - Golden path with note: `--result partial --note \"auth provider deprecated\"` writes both fields.\n  - Rejection path: `--result wat` fails with a clear error and non-zero exit.\n  - Warning path: outcome set on a plan with open children emits a warning to stderr but succeeds.\n  - `--json` output respected.\n\n## Files\n\n- `src/commands/plan.ts` — register the `outcome` subcommand under the plan command group.\n- `src/types.ts` — confirm `outcome?: \"success\" | \"partial\" | \"failure\"` and `outcomeNote?: string` fields on `Plan` (declared in Phase 1; verify and patch if needed).\n- `src/commands/plan.test.ts` (or `src/commands/plan.outcome.test.ts`) — coverage matching acceptance.\n\n## Notes\n\n- This task is independent of tasks 2, 3, 4 in this phase. Blocks task 5 (docs).\n- Use the existing plan-store helpers from Phase 1 for read/write under file locks. Do not bypass.","blocks":["seeds-6d30","seeds-5935"],"closedAt":"2026-05-06T20:07:25.366Z","closeReason":"Phase 5 tasks 1+2: sd plan outcome and sd plan review wired up; show hint refined to gate on status=approved|active"}
{"id":"seeds-0a06","title":"Phase 4 Task 3: sd plan show recursive display + max_plan_depth config","status":"closed","type":"task","priority":2,"createdAt":"2026-05-06T18:09:09.059Z","updatedAt":"2026-05-06T19:48:53.022Z","description":"## What\n\n1. Add `max_plan_depth: <number>` to the seeds `Config` type and `.seeds/config.yaml` (default `3`, per PLAN_SPEC.md:430).\n2. Update `sd plan show` to recursively render nested plans:\n   - Human output: indent each nested plan level visually.\n   - JSON output: nest under a `children_plans: [{...recursive plan record...}]` array on each step that has a sub-plan.\n3. At `max_plan_depth`, emit a per-truncated-child hint: `\"depth limit reached — use \\`sd plan show <pl-id>\\` to drill in\"`.\n\nSpec references:\n- PLAN_SPEC.md:340 — \"`sd plan show <pl-id>` recursively displays nested plans up to a configurable depth (default 3).\"\n- PLAN_SPEC.md:425, 430 — open question (5) settled in favor of configurable `max_plan_depth`, default 3.\n\n## Why\n\nRecursive display is the read surface for the entire phase. Without it, plan authors and reviewers can't see the sub-plan topology in one place — they have to chase pl-IDs by hand. The depth limit prevents runaway output on deeply decomposed work while still keeping the data layer arbitrarily deep.\n\nNote: only display is depth-limited. Sub-plans can stack arbitrarily deep at the storage layer (Tasks 1 + 2 enforce no depth cap there).\n\n## Acceptance\n\n- `Config` interface gains `max_plan_depth?: number` with default `3` applied at load time.\n- `.seeds/config.yaml` written by `sd init` includes the field (commented or default).\n- Test: depth-1 plan (no children with sub-plans) — output unchanged from non-recursive baseline.\n- Test: depth-2 plan (parent plan has one step whose spawned child has its own approved plan) — `sd plan show` renders both, indented in human, nested in JSON.\n- Test: depth-3 nesting at `max_plan_depth: 2` — third level is truncated with the hint string `depth limit reached — use \\`sd plan show <pl-id>\\` to drill in`. Hint appears once per truncated child.\n- Test: `--json` form includes `children_plans` array (empty when no nested plans, populated when present).\n\n## Files\n\n- `src/config.ts` — add `max_plan_depth?: number` to `Config` type; default `3` in load path.\n- `src/commands/plan.ts` — show handler: implement recursion with depth tracking; render hint at boundary.\n- `src/commands/init.ts` — emit `max_plan_depth` in default `config.yaml` (or document via comment).\n- Tests: extend the plan show test suite with the four cases above.\n\n## Dependencies\n\n- Blocked by Task 1 (needs `requires_plan` flag to identify nested plans correctly) and Task 2 (needs `plan_template` step wiring to actually produce sub-plans for testing).","blocks":["seeds-f873"],"closedAt":"2026-05-06T19:48:53.022Z","closeReason":"max_plan_depth config (default 3); sd plan show recurses with truncation hint per spec; tests cover depth-1/2/3 and limit=1/2 cases"}
{"id":"seeds-4b9c","title":"sd ready / sd show / sd list plan-awareness integration","status":"closed","type":"task","priority":1,"createdAt":"2026-05-06T18:09:16.582Z","updatedAt":"2026-05-06T18:43:19.065Z","description":"## What\n\nMake the existing read commands plan-aware.\n\nPLAN_SPEC.md references:\n- `sd ready` plan surfacing: line 154\n- `sd show` plan-id awareness + child inline display: line 156\n- `sd list` indicator: line 156\n\n### `sd ready`\n\nWhen a seed has a plan in `draft`, surface a \"plan in draft\" hint and treat planning as the highest-priority work for that seed. Concretely:\n- A seed with `plan_id` whose plan is in `draft` should appear in `sd ready` output (regardless of normal blocking) with an explicit hint like `[plan in draft — run sd plan submit]`\n- Planning takes precedence over implementation: if a parent seed has a draft plan, that hint is shown instead of (or above) the seed's normal ready state\n\n### `sd show <seed>`\n\nWhen a seed has `plan_id`:\n- Display the plan id in the seed view\n- If the plan's `status` is `approved` (or `active`/`done`), list the child seeds inline (id + title + status)\n- If the plan's `status` is `draft`, show the \"plan in draft\" hint\n\n### `sd list`\n\nWhen listing seeds, show a small plan status indicator (e.g. `[draft]`/`[approved]`/`[done]`) next to seeds that have a `plan_id`. Keep it terse — one column or suffix.\n\n### `--json` parity\n\nAll three commands' `--json` outputs should include the same plan-aware data (e.g. `plan_id`, `plan_status`, `plan_children` where applicable) so downstream tools see the same view.\n\n## Why\n\nPlan-awareness in the existing commands is what closes the loop. Without it, agents have to know to run `sd plan show` separately and won't notice draft plans blocking ready work.\n\n## Acceptance\n\n- `sd ready` surfaces draft-plan hints (human + JSON) — covered by a test that creates a seed with a draft plan and asserts the hint appears\n- `sd show <seed>` with an approved plan lists children inline (human + JSON)\n- `sd list` shows the status indicator on seeds with plans (human + JSON)\n- Seeds without plans render exactly as before (regression test)\n- `bun test && bun run lint && bun run typecheck` green\n\n## Files\n\n- `src/commands/ready.ts` (edit)\n- `src/commands/show.ts` (edit)\n- `src/commands/list.ts` (edit)\n- `src/filter.ts` (edit if needed for shared plan-aware filter logic)\n- Existing tests for these commands updated; new tests for plan-aware behavior\n\n## Depends on\n\n- Task 1 (types — needs `plan_id` on Issue)\n- Task 2 (store — needs to read plans alongside issues)\n- Task 6 (submit — produces the plans/children to integrate against)\n- Task 7 (show — establishes the human display conventions for plans)","blocks":["seeds-a672"],"closedAt":"2026-05-06T18:43:19.065Z","closeReason":"ready/show/list plan-aware via shared src/plan-context.ts: ready surfaces draft-plan seeds even when blocked + emits hint; show inlines children for approved/active/done plans + draft hint; list shows [plan <status>] indicator; --json parity (plan_status, plan_children); regression test for non-plan seeds"}
{"id":"seeds-2a2d","title":"sd plan review <pl-id> --by <name> + reviewedBy + review-suggested hint","status":"closed","type":"task","priority":3,"createdAt":"2026-05-06T18:09:18.303Z","updatedAt":"2026-05-06T20:21:50.158Z","description":"# sd plan review\n\n## What\n\nWire the soft-review workflow:\n\n1. Add `reviewedBy?: string` field to the `Plan` interface (declared in Phase 1 task 1; this task wires the command surface).\n2. New subcommand `sd plan review <pl-id> --by <name>` populates `reviewedBy`. **Informational only — not a state transition.**\n3. `sd plan show <pl-id>` displays a \"review suggested\" hint when `reviewedBy` is null AND status is `approved` or `active`.\n\nSpec: PLAN_SPEC.md:404-413.\n\n> - `sd plan submit` transitions `draft -> approved` automatically on successful validation.\n> - `sd plan show` displays a \"review suggested\" hint when `reviewedBy` is null.\n> - `sd plan review <pl-id> --by <name>` populates `reviewedBy`. This is informational, not a state transition.\n\n## Why\n\nThe default optimizes for autonomous swarms; teams that want a hard gate can wrap submit in a hook. Seeds itself just exposes the data path: \"who reviewed this, if anyone\".\n\n## Acceptance\n\n- `sd plan review <pl-id> --by <name>` writes the field via the standard locked atomic-rewrite path. `--by` is required.\n- No state transition: status stays whatever it was (`draft`, `approved`, `active`, `done`).\n- `sd plan show <pl-id>` displays a clearly visible \"review suggested\" hint when **both** are true:\n  - `reviewedBy` is null/undefined AND\n  - status is `approved` or `active`\n- Hint disappears once `reviewedBy` is set, or status moves to `draft` / `done`.\n- Tests:\n  - Setting `reviewedBy` via the command works and persists.\n  - Hint appears in `show` output when status=approved + reviewedBy=null.\n  - Hint appears in `show` output when status=active + reviewedBy=null.\n  - Hint does NOT appear when status=draft (review not relevant yet) or status=done (already past).\n  - Hint does NOT appear when `reviewedBy` is set, regardless of status.\n  - `--json` output of `show` includes `reviewedBy` (null or string) but does NOT need the cosmetic hint.\n\n## Files\n\n- `src/commands/plan.ts` — new `review` subcommand + edit to `show` handler to emit the hint.\n- `src/types.ts` — confirm `reviewedBy?: string` on `Plan`.\n- `src/commands/plan.test.ts` (or dedicated file) — coverage matching acceptance.\n\n## Notes\n\n- This task is independent of tasks 1, 3, 4 in this phase. Blocks task 5 (docs).\n- The hint should be human-readable (e.g. printed to stdout in non-JSON mode, italic/dim if convenient via chalk). Stay consistent with existing seeds output style.","blocks":["seeds-6d30","seeds-5935"],"closedAt":"2026-05-06T20:21:50.158Z","closeReason":"Work bundled with seeds-aa81 in commit 8e0c9e0 (Phase 5 outcome + review). Status flip was missed; closing now."}
{"id":"seeds-0189","title":"Built-in bug plan template","status":"closed","type":"task","priority":3,"createdAt":"2026-05-06T18:09:36.306Z","updatedAt":"2026-05-06T20:10:11.013Z","description":"# Built-in `bug` plan template\n\n## What\n\nShip a built-in `bug` template that adds defect-specific sections to a feature-like base. Per PLAN_SPEC.md:268:\n\n> `bug` — Defect fix. Adds `reproduction` and `root_cause` sections.\n\nConcrete sections (six total):\n\n| Name              | Required | Kind  | Notes                                  |\n| ----------------- | -------- | ----- | -------------------------------------- |\n| `context`         | yes      | text  | Why the work matters.                  |\n| `reproduction`    | yes      | text  | `min_length: 50`. How to trigger it.   |\n| `root_cause`      | yes      | text  | `min_length: 50`. What's actually wrong. |\n| `approach`        | yes      | text  | Chosen fix with rationale.             |\n| `steps`           | yes      | steps | `min: 1`.                              |\n| `acceptance`      | yes      | list  | `min: 1`. Verifiable conditions.       |\n\nWire into the default-template-inference mapping. Per PLAN_SPEC.md:429 (open question 4), recommended hard-coded mapping is `task->feature, bug->bug, feature->feature, epic->feature` with `--template` always overriding. Use that mapping here unless Phase 2 already chose differently — in which case, follow Phase 2's choice and only add the `bug -> bug` row.\n\n## Why\n\n`feature` is overkill for defect work and `feature`'s `risks` section misframes the conversation. `bug` requires the LLM to surface reproduction steps and root cause separately from the fix approach — these are the high-leverage fields for defect work.\n\n## Acceptance\n\n- `sd plan templates` (Phase 2 surface) lists `bug`.\n- `sd plan prompt <bug-seed>` defaults to the `bug` template when seed type is `bug` and `--template` is not passed.\n- `sd plan submit` validates the template end-to-end:\n  - Missing `reproduction` -> validation error.\n  - `reproduction` shorter than 50 chars -> validation error mentioning min_length.\n  - Same for `root_cause`.\n  - Empty `steps` -> validation error.\n  - Empty `acceptance` -> validation error.\n- `sd plan show` renders the bug-specific sections.\n- Built-in template loads even when the project's `.seeds/config.yaml` does not redeclare it (per PLAN_SPEC.md:261 — built-in defaults loaded if not redeclared).\n- A project can override the built-in by declaring `plan_templates.bug:` in config; the override wins.\n- Tests:\n  - Inference: `sd plan prompt <seed>` where seed.type=bug picks `bug` template.\n  - Validation goldens for each required section + min_length.\n  - Override test: project config redeclares `bug` -> custom shape wins.\n\n## Files\n\n- `src/plan-templates/bug.ts` (or wherever Phase 2 puts built-ins — match its convention).\n- Edit to template-inference table (probably in `src/plan-templates/index.ts` or similar Phase 2 module).\n- Test file alongside the implementation.\n\n## Notes\n\n- This task is independent of tasks 1, 2, 4 in this phase. Blocks task 5 (docs).\n- The `bug` template re-uses the same `kind`s (`text`, `list`, `steps`) as `feature`. No new `kind` machinery needed.\n- Match the structure Phase 2 uses for the `feature` built-in. Do not invent a new pattern.","blocks":["seeds-6d30","seeds-5935"],"closedAt":"2026-05-06T20:10:11.013Z","closeReason":"Phase 5 tasks 3+4: built-in bug + refactor templates added; bug → bug type inference; refactor opt-in only"}
{"id":"seeds-6730","title":"Built-in refactor plan template","status":"closed","type":"task","priority":3,"createdAt":"2026-05-06T18:09:50.653Z","updatedAt":"2026-05-06T20:10:11.013Z","description":"# Built-in `refactor` plan template\n\n## What\n\nShip a built-in `refactor` template. Per PLAN_SPEC.md:269:\n\n> `refactor` — Internal restructuring. Adds `behavior_invariant` (must stay equal).\n\nConcrete sections (five total):\n\n| Name                 | Required | Kind  | Notes                                              |\n| -------------------- | -------- | ----- | -------------------------------------------------- |\n| `context`            | yes      | text  | Why the refactor.                                  |\n| `behavior_invariant` | yes      | text  | `min_length: 50`. The contract that MUST remain equal. |\n| `approach`           | yes      | text  | Chosen restructuring strategy.                     |\n| `steps`              | yes      | steps | `min: 1`.                                          |\n| `acceptance`         | yes      | list  | `min: 1`. How we'll verify behavior is preserved.  |\n\nNo `risks` by default — the framing is \"what stays the same\", which subsumes risk in the refactor case.\n\n## Inference choice (document in implementation)\n\n`refactor` is **not currently a seed type** in seeds (`task | bug | feature | epic` per src/types.ts). Two options:\n\n- **(a)** Add a new seed type `refactor` and wire `refactor -> refactor` into the inference mapping.\n- **(b)** Leave inference as `--template refactor` only — opt-in via the explicit flag.\n\n**Choose option (b).** Rationale: avoid type-taxonomy churn; the seed type system is shared with the rest of seeds (`sd list --type`, etc.) and adding a new type has ripple effects beyond planning. Document this choice in a comment at the top of the template module so future readers understand the asymmetry vs. `bug`.\n\n## Why\n\nRefactors have a unique structural concern: the *invariant*. The point of a refactor plan is to make the LLM commit, in writing, to a contract that the change is supposed to preserve. That commitment is what acceptance tests should verify.\n\n## Acceptance\n\n- `sd plan templates` lists `refactor`.\n- `sd plan prompt --template refactor <seed>` works for any seed type.\n- Inference: when `--template` is omitted, seed type does NOT route to `refactor` automatically (option b above).\n- Validation:\n  - Missing `behavior_invariant` -> error.\n  - `behavior_invariant` shorter than 50 chars -> error.\n  - Empty `steps` / empty `acceptance` -> errors.\n  - No `risks` section is added by default.\n- `sd plan submit` and `sd plan show` work end-to-end with the template.\n- Override test: project config redeclares `refactor` -> custom shape wins.\n- Tests:\n  - Goldens for each required section + min_length on `behavior_invariant`.\n  - Explicit `--template refactor` flow for a `task`-typed seed.\n  - Inference test: `task`-typed seed without `--template` picks `feature`, NOT `refactor`.\n\n## Files\n\n- `src/plan-templates/refactor.ts` (match Phase 2 convention).\n- Edit to template-registry / inference mapping.\n- Test file alongside.\n\n## Notes\n\n- This task is independent of tasks 1, 2, 3 in this phase. Blocks task 5 (docs).\n- Document the option-b choice in a header comment in `refactor.ts`.","blocks":["seeds-6d30","seeds-5935"],"closedAt":"2026-05-06T20:10:11.013Z","closeReason":"Phase 5 tasks 3+4: built-in bug + refactor templates added; bug → bug type inference; refactor opt-in only"}
{"id":"seeds-5935","title":"Docs: README, CLAUDE.md, sd prime / sd onboard plan-aware context","status":"closed","type":"task","priority":3,"createdAt":"2026-05-06T18:10:20.658Z","updatedAt":"2026-05-06T20:21:50.158Z","description":"# Docs: README, CLAUDE.md, sd prime / sd onboard\n\n## What\n\nUpdate user-facing and agent-facing docs so the shipped `sd plan` surface is discoverable.\n\n### 1. `README.md` — new \"Planning\" section\n\nDemonstrate the spawn loop (PLAN_SPEC.md:200-256) end-to-end:\n\n- Quick pitch: structured planning that spawns child seeds.\n- Walkthrough:\n  - `sd plan prompt <seed-id>` -> shows the prompt-request JSON shape (sample from PLAN_SPEC.md:200-234).\n  - LLM produces the submission JSON (sample from PLAN_SPEC.md:236-255, including `blocks: [step_index]` translation).\n  - `sd plan submit <seed-id> --plan plan.json` validates, spawns children, links them.\n  - `sd plan show <pl-id>`.\n  - Outcome: `sd plan outcome <pl-id> --result success`.\n- Mention built-in templates (`feature`, `bug`, `refactor`) with the one-liner from PLAN_SPEC.md:265-269.\n- Brief mention: nested plans + `requires_plan` (PLAN_SPEC.md:327-342).\n- Brief mention: review is suggested, not required (PLAN_SPEC.md:404-413).\n\n### 2. `seeds/CLAUDE.md`\n\n- Add `sd plan` command reference under **CLI Command Reference** with subcommands: `prompt`, `submit`, `show`, `validate`, `templates`, `outcome`, `review`. One-line description per subcommand. Match the existing style (see the Issue Commands / Template Commands blocks).\n- Add a short **\"Planning\"** subsection under **Conventions** explaining when to plan vs. when to ticket directly. Rough heuristic: plan when the work is large/ambiguous enough that the LLM benefits from structured decomposition; create plain seeds directly for small, well-scoped tasks.\n\n### 3. `src/commands/prime.ts` and `src/commands/onboard.ts`\n\n- `sd prime` should emit plan-aware context: a one-line workflow hint (e.g. \"Use `sd plan prompt <seed>` for ambiguous/large work; structured plan spawns child seeds with deps wired\") and the new commands appear in any command-list section.\n- `sd onboard` writes a \"Planning\" section to `CLAUDE.md` / `AGENTS.md` (using the marker-delimited helpers in `src/markers.ts`). The section must be **idempotent** — re-running `sd onboard` does not duplicate the section. Use the existing markers convention.\n\n## Why\n\nPhases 1-4 ship the surface; Phase 5 makes it discoverable. Without these docs, agents will not know `sd plan` exists or when to use it.\n\n## Acceptance\n\n- `README.md` has a \"Planning\" section with the prompt-request and submission JSON examples from the spec.\n- `seeds/CLAUDE.md` lists `sd plan` subcommands under CLI Command Reference and has a Planning conventions blurb.\n- `sd prime` output (both default and `--compact`) includes plan-aware context. Existing snapshots updated.\n- `sd onboard` writes the new section to a fresh fixture cleanly. Re-running on the same file is a no-op (no duplicate sections).\n- Existing prime/onboard tests pass with snapshot updates that reflect the new content.\n- New tests:\n  - prime emits the plan workflow hint.\n  - onboard writes the planning marker section idempotently.\n\n## Files\n\n- `README.md`\n- `seeds/CLAUDE.md` (i.e. `/Users/jayminwest/Projects/os-eco/seeds/CLAUDE.md`)\n- `src/commands/prime.ts`\n- `src/commands/onboard.ts`\n- `src/commands/prime.test.ts`, `src/commands/onboard.test.ts` (snapshot updates + new cases)\n- Any test fixtures under `src/commands/__fixtures__/` or wherever existing onboard tests keep them\n\n## Cross-tool note\n\nIf any of the planning prompts are ever to be managed via Canopy, that's out of scope here (PLAN_SPEC.md:419 — \"Canopy integration is not in V1\"). For this task, the planning examples in `README.md` are illustrative markdown only — they are NOT canopy-emitted prompts. Reminder that **`cn emit` regenerates emitted prompts and emitted files must NOT be edited manually** — irrelevant here only because this task does not touch any emitted prompt files; calling it out so future doc edits do not accidentally hand-edit emitted output.\n\n## Notes\n\n- This task depends on tasks 1-4 of this phase landing first. Blocks the phase feature.\n- Keep README \"Planning\" section reasonably scoped — link out to PLAN_SPEC.md for the full spec; do not re-paste the whole document.","blocks":["seeds-6d30"],"closedAt":"2026-05-06T20:15:16.810Z","closeReason":"Phase 5 task 5: README Planning section, CLAUDE.md plan command reference + Planning conventions, sd prime + sd onboard plan-aware (idempotent)"}
{"id":"seeds-25a5","title":"sd plan: resolve punted spec decisions during implementation","status":"closed","type":"task","priority":3,"createdAt":"2026-05-06T18:11:16.831Z","updatedAt":"2026-05-06T20:21:59.109Z","description":"Tracking seed for the open implementation questions in PLAN_SPEC.md §424–432 that were deliberately deferred from up-front planning. Resolve each as the relevant phase implementer encounters it; close this seed once all five are decided and recorded.\n\nParent epic: seeds-8fdb.\n\n## Decisions to resolve\n\n1. **Section schema vs. mulch custom_types reuse** (PLAN_SPEC.md:426)\n   - Question: Reuse mulch's AJV schema-generation code (extract shared package) or maintain a parallel impl in seeds?\n   - **Default during impl:** parallel impl in seeds (`src/plan-schema.ts` per Phase 2 task 14). Keeps seeds dependency-free of mulch. Revisit if drift becomes painful.\n\n2. **Step ID stability for --overwrite** (PLAN_SPEC.md:427)\n   - Question: Should templates be able to declare stable `step_id` keys that survive title edits?\n   - **Default during impl:** title-based diffing only in Phase 2 task 5. Add `step_id` later if title-edit churn shows up in real use. File a follow-up if this bites.\n\n3. **Mulch query interface** (PLAN_SPEC.md:428)\n   - **Locked during planning:** shell out to `ml query --json`. Phase 3 implements this. No revisit needed.\n\n4. **Default template inference mapping** (PLAN_SPEC.md:429)\n   - **Locked during planning:** hardcoded mapping `task→feature, bug→bug, feature→feature, epic→feature`, `--template` always overrides. Document in Phase 1 task 5 + Phase 5 tasks 3/4.\n\n5. **Sub-plan depth** (PLAN_SPEC.md:430)\n   - **Locked during planning:** configurable `max_plan_depth` in config.yaml, default 3. Phase 4 task 3 implements.\n\n6. **Children lifecycle when plan moves to `done`** (PLAN_SPEC.md:431)\n   - **Locked during planning:** warning, not error. Phase 5 task 1 (`sd plan outcome`) and Phase 2 task 4 (lifecycle transitions) implement.\n\n## Definition of done\n\nAll six items above either implemented per the locked default OR a deliberate alternative chosen and recorded (commit message + ml record decision). Close this seed with reason linking to the relevant commits/PRs.","closedAt":"2026-05-06T20:21:59.109Z","closeReason":"All six punted/locked decisions resolved per locked defaults during implementation:\n1. Schema vs mulch reuse → parallel impl in seeds (src/plan-schema.ts via seeds-6bd8/315d817).\n2. Step ID stability → title-based diffing only in v1 (Phase 2 task seeds-9bd4); add stable step_id later if title-edit churn shows up.\n3. Mulch query interface → shell-out to ml query --json (Phase 3 seeds-f170/5315449).\n4. Default template inference → hardcoded task→feature, bug→bug, feature→feature, epic→feature (Phase 1 + 5).\n5. Sub-plan depth → max_plan_depth in config.yaml, default 3 (Phase 4 task seeds-0a06/33c1fb9).\n6. Plan-done with open children → warning, not error (Phase 2 lifecycle + Phase 5 outcome)."}
{"id":"seeds-48a0","title":"output.ts treats issues with closed blockers as still [blocked]","status":"closed","type":"bug","priority":1,"createdAt":"2026-05-06T19:36:10.400Z","updatedAt":"2026-05-06T19:57:15.520Z","description":"## Bug\n\n`formatIssueOneLine` (src/output.ts:35) and `formatIssueOneLineCompact` (src/output.ts:53) compute `isBlocked` from the length of `blockedBy` alone:\n\n```ts\nconst isBlocked = (issue.blockedBy?.length ?? 0) > 0;\n```\n\nThis treats any issue that has ever had a blocker as `[blocked]`, even after every blocker has been closed. The `[blocked]` tag and yellow `!` status icon stick around indefinitely, miscommunicating actual readiness.\n\nSymptom: `sd list`, `sd show`, and any command that flows through these formatters display `[blocked]` on issues that are in fact unblocked. Quietly wrong — the user has to cross-check via `sd ready` or `sd dep list` to know what's really actionable.\n\n## Correct semantic (already implemented in `sd blocked`)\n\n`src/commands/blocked.ts:28–33` does this correctly:\n\n```ts\nconst closedIds = new Set(issues.filter((i) => i.status === \"closed\").map((i) => i.id));\nconst blocked = issues.filter((i) => {\n  if (i.status === \"closed\") return false;\n  const blockers = i.blockedBy ?? [];\n  return blockers.some((bid) => !closedIds.has(bid));\n});\n```\n\nAn issue is *effectively* blocked when at least one of its `blockedBy` entries is not closed. The output formatters need the same logic.\n\n## Repro\n\n```bash\nsd init\nA=$(sd create --title \"blocker\" --json | jq -r .id)\nB=$(sd create --title \"blocked\" --json | jq -r .id)\nsd dep add \"$B\" \"$A\"\nsd close \"$A\"\nsd list                 # B still shows [blocked] — should not\nsd show \"$B\"            # also implies blocked via the formatter\n```\n\n## Fix options\n\n- **(a) Pass `closedBlockerIds` (or `openBlockerIds`) into the formatters.** Callers compute the set once from the full issue list before iterating. Cheap; localized; keeps formatter pure.\n- **(b) Compute an effective-blocked flag at read time** inside store.ts after dedup, attaching it as a transient field. Centralised but adds a non-persistent field to Issue.\n- **(c) Strip closed blocker IDs from `blockedBy` when `sd close` runs.** Rejected: destroys dependency history; merge-conflict-prone.\n\nRecommend (a). `formatIssueOneLine(issue, closedBlockerIds?)` defaults to the current behavior when the set isn't passed (backwards-compatible at the function-signature level), and command handlers in `list.ts`, `show.ts`, `ready.ts`, `blocked.ts` pass the precomputed set.\n\n## Acceptance\n\n- `formatIssueOneLine` and `formatIssueOneLineCompact` no longer report `[blocked]` when every entry in `blockedBy` references a closed issue.\n- All call sites (`list`, `show`, `ready`, `blocked`, `stats` if it uses formatters, anywhere else `formatIssueOneLine*` is called) pass the closed-set.\n- New test in `src/output.test.ts` (or wherever output tests live): build an Issue with `blockedBy: [closedId]`, assert formatter does not include `[blocked]`; build with `blockedBy: [openId, closedId]`, assert it does.\n- `bun test && bun run lint && bun run typecheck` green.\n\n## Files\n\n- `src/output.ts` (edit — both formatters)\n- Call sites: `src/commands/list.ts`, `src/commands/show.ts`, `src/commands/ready.ts`, `src/commands/blocked.ts` (already computes the set; just pass it to the formatter), and any other callers of `formatIssueOneLine*`\n- New test file or extend existing one","closedAt":"2026-05-06T19:57:15.520Z","closeReason":"Threaded closedBlockerIds set through formatIssueOneLine/Compact/printIssueOneLine; updated callers in list, ready, blocked, show (compact), search, dep, tpl. New tests in src/output.test.ts cover the closed-blocker logic. All quality gates green."}
{"id":"seeds-7d17","title":"sd plan show renders structured list items as raw JSON","status":"closed","type":"bug","priority":1,"createdAt":"2026-05-06T21:36:31.686Z","updatedAt":"2026-05-06T21:46:18.682Z","description":"## Bug\n\n`sd plan show <pl-id>` falls back to `JSON.stringify(entry)` for any list entry that isn't a plain string. This affects the `steps` and `alternatives` sections of every plan — the user-facing review surface that the PLAN_SPEC explicitly positions as \"the artifact reviewed\" (PLAN_SPEC.md:7).\n\n### Symptom\n\nSubmitting a feature plan and then viewing it produces:\n\n```\nsteps\n  1. {\"title\":\"Add `auth` config namespace to src/config.ts\",\"blocks\":[],\"requires_plan\":false}\n  2. {\"title\":\"Implement device-flow polling client\",\"blocks\":[]}\nalternatives\n  1. {\"name\":\"Personal access tokens\",\"rejected_because\":\"Plaintext storage\"}\n```\n\ninstead of formatting the title, blocks, requires_plan, and rejection reasons in a human-readable way.\n\n### Location\n\n`src/commands/plan.ts:857-863` — the renderer's array branch:\n\n```ts\nvalue.forEach((entry, i) => {\n    if (typeof entry === \"string\") {\n        console.log(`    ${i + 1}. ${entry}`);\n    } else {\n        console.log(`    ${i + 1}. ${JSON.stringify(entry)}`);  // ← fallback bites here\n    }\n});\n```\n\n### Repro\n\n```bash\nSEED=$(sd create --title 'Test plan rendering' --type feature --json | jq -r .id)\nsd plan prompt $SEED --json > /tmp/p.json\n# Fill /tmp/p.json with any valid feature plan, then:\nsd plan submit $SEED --plan /tmp/p.json\nsd plan show <pl-id>   # observe JSON-stringified steps + alternatives\n```\n\n### Fix sketch\n\nRender each section based on its template `kind` rather than runtime type. The plan already knows which template was used; `renderPlanSections` can look up the section spec and dispatch:\n\n- `kind: steps` → `${i+1}. <title>` plus secondary lines for non-empty `blocks`, `requires_plan: true`, `plan_template` overrides.\n- `kind: list` with `item` schema (e.g. alternatives `{name, rejected_because}`) → render each named field on its own indented line.\n- `kind: list` with `item: text` → unchanged (already correct).\n\nThe renderer must tolerate plans whose template is no longer in config (custom templates may be removed). Fall back to today's JSON.stringify only when the spec is unavailable.\n\n### Acceptance\n\n- `sd plan show` for a built-in feature plan prints step titles directly with secondary annotations (blocks, requires_plan) on indented sub-lines.\n- Same for alternatives: `name` on the first line, `rejected_because` indented underneath.\n- Custom templates with declared item schemas render via the same path.\n- Plans whose template is no longer registered still render (fall back to JSON dump rather than crashing).\n- New tests in `src/commands/plan.test.ts` covering steps with all of {plain, blocks set, requires_plan, plan_template} and alternatives with the {name, rejected_because} item shape.\n\n### Files\n\n- `src/commands/plan.ts` (renderer)\n- `src/commands/plan.test.ts` (new cases)","closedAt":"2026-05-06T21:46:18.682Z","closeReason":"Renderer dispatches by template kind: kind=steps formats title with annotations, kind=list with item schema renders named fields per line, fallback to JSON when template missing. Tests added in src/commands/plan.test.ts."}
{"id":"seeds-76af","title":"Plan-spawned child seeds inherit no context from parent plan","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-06T21:36:50.393Z","updatedAt":"2026-05-06T21:57:46.848Z","description":"## Problem\n\nWhen `sd plan submit` spawns child seeds, each child gets only the step `title` as its title. The child's `description` is empty. An agent picking up a child seed cold has to:\n\n1. Notice the `plan_id` field on the issue.\n2. Run `sd show <plan-parent-seed>` to find the parent.\n3. Run `sd plan show <pl-id>` to read the `context`, `approach`, and surrounding steps.\n\nThat's three navigations before the agent has the framing the plan author already wrote down. Worse, every child seed needs the same orientation — so the cost multiplies across the plan.\n\n## Proposed fix\n\nWhen spawning a child, populate `description` with a backref block that gives an agent enough to start without forcing a navigation:\n\n```\nStep <N> of plan pl-9d0f.\n\nParent seed: seeds-7c84 — Add OAuth2 device-flow authentication\nPlan template: feature\nPlan approach: <first ~200 chars of plan.sections.approach>\n\nRun `sd plan show pl-9d0f` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n```\n\nThe exact format is up to implementation, but the constraint is: a child seed's `sd show` output must contain enough context to begin work without explicitly navigating to the plan. The full plan stays the source of truth.\n\n## Considerations\n\n- **Mutability.** If the plan is revised via `--overwrite`, kept children should have their backref refreshed — otherwise the snippet drifts from the live plan. Cheap to recompute on overwrite.\n- **Step-author content.** If a step ever grows a free-form `description` field on the template (not in V1), prefer that over the parent approach summary.\n- **Don't duplicate the whole plan.** Excerpts only. The `sd plan show` link is the escape hatch.\n\n## Acceptance\n\n- A child seed's `description` after `sd plan submit` includes: step index, plan id, parent seed id+title, plan template, and a short excerpt of `sections.approach` (or whichever section the template designates as \"the why\").\n- `sd plan submit --overwrite` updates the backref block on retained child seeds when the plan content changes.\n- Existing seed fields (assignee, labels) on retained children are preserved across overwrite.\n- New tests in `src/commands/plan.test.ts` covering both submit and overwrite paths.\n\n## Files\n\n- `src/commands/plan.ts` (spawn + overwrite logic)\n- `src/commands/plan.test.ts`","closedAt":"2026-05-06T21:57:46.848Z","closeReason":"Child seeds spawned by sd plan submit now carry a backref block (step index, plan id, parent seed id+title, template, approach excerpt, sd plan show link). --overwrite refreshes the marker section in place; assignee/labels on retained children survive."}
{"id":"seeds-b2d7","title":"sd show on plan-spawned seed labels siblings as 'Children'","status":"closed","type":"bug","priority":3,"createdAt":"2026-05-06T21:36:59.964Z","updatedAt":"2026-05-06T22:02:45.214Z","description":"## Bug\n\nWhen viewing a child seed of a plan, `sd show` displays the entire plan's children list under a heading labeled \"Children (N)\". That heading is misleading: the items are *siblings* of the seed being inspected, not its children.\n\n### Repro\n\n```bash\n# Submit any plan with multiple steps\nsd plan submit <seed> --plan plan.json   # spawns seeds-AAAA, seeds-BBBB, seeds-CCCC\nsd show seeds-AAAA\n```\n\nOutput ends with:\n\n```\nPlan: pl-xxxx  [approved]\nChildren (3):\n  seeds-AAAA  [open]  Step 1\n  seeds-BBBB  [open]  Step 2\n  seeds-CCCC  [open]  Step 3\n```\n\n\"Children (3)\" reads as if seeds-AAAA has 3 children. It actually has zero — the list is the plan's children.\n\n### Fix\n\nEither:\n- Re-label the section to \"Plan steps\" or \"Sibling steps\" when displayed via `sd show <child-seed>`.\n- Or omit the current seed from the list when it's the seed being viewed and rename to \"Other steps\".\n\nRecommend \"Plan steps\" — accurate framing without needing to detect self-membership.\n\n### Acceptance\n\n- `sd show <child-seed>` no longer uses \"Children\" as the heading for the plan's step list.\n- `sd plan show <pl-id>` retains its existing \"Children\" framing (it's the right word from the plan's perspective).\n- A test asserts the `sd show` heading text on a plan-spawned seed.\n\n### Files\n\n- `src/commands/show.ts` or wherever the plan-aware section is rendered\n- corresponding test","closedAt":"2026-05-06T22:02:45.214Z","closeReason":"Renamed sd show plan-step heading from 'Children' to 'Plan steps'. sd plan show retains 'Children'. Added regression tests."}
{"id":"seeds-d6cb","title":"sd plan submit success output: nudge toward next action","status":"closed","type":"feature","priority":3,"createdAt":"2026-05-06T21:37:12.664Z","updatedAt":"2026-05-06T22:11:55.671Z","description":"## Problem\n\n`sd plan submit` success output today:\n\n```\n✓ plan pl-9d0f created (status: approved)\n✓ 6 child seeds: seeds-7a81, seeds-d8e4, seeds-c5c1, seeds-de2b, seeds-94a0, seeds-b9ab\n✓ seeds-7c84 now blocked by 6 children\n```\n\nThat confirms what happened but doesn't tell the agent what's next. After submit, an agent is most likely to want to (a) review the plan they just submitted, (b) pick up the first ready child, or (c) post the plan id somewhere for human review.\n\n## Proposed addition\n\nAppend a small \"Next\" block:\n\n```\nNext:\n  sd plan show pl-9d0f          # review the plan as a unit\n  sd ready                      # pick up the first child step\n  sd plan review pl-9d0f --by <name>   # record approval (optional)\n```\n\nSame pattern `sd plan submit` already uses on the `--overwrite` failure path with the suggested `sd close` lines on stderr — keep next-step hints to stderr so they don't pollute --json output.\n\n## Considerations\n\n- Only emit hints in human (non-JSON) output. JSON consumers don't need them.\n- Only emit `sd plan review` line when reviewedBy is null AND the plan is in approved/active.\n- If the plan was submitted with `--record-decision`, surface the resulting mulch id (`✓ recorded mulch decision mx-xxxx`) — currently silent on success.\n\n## Acceptance\n\n- Plain `sd plan submit` success prints the three Next lines on stderr.\n- `sd plan submit --overwrite` continues to print obsolete-child suggestions; the Next block follows.\n- `sd plan submit --record-decision` prints the new mulch confirmation line when ml record succeeds.\n- `sd plan submit --json` output is unchanged (Next block goes to stderr, not stdout).\n- Tests assert stderr content for at least one of the success paths.\n\n### Files\n\n- `src/commands/plan.ts` (submit success path)\n- `src/commands/plan.test.ts`","closedAt":"2026-05-06T22:11:55.671Z","closeReason":"Implemented Next-block hints + mulch confirmation. Plan submit success now suggests 'sd plan show', 'sd ready', and (when reviewable) 'sd plan review' on stderr; --record-decision surfaces the recorded mx-id on stdout. JSON output unchanged."}
{"id":"seeds-51bc","title":"sd plan show/validate/outcome/review should accept a seed id","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-06T22:02:40.332Z","updatedAt":"2026-05-06T22:19:23.495Z","description":"## Problem\n\n`sd plan show` (and the sibling commands `validate`, `outcome`, `review`) only accept a plan id (`pl-xxxx`). When an agent already knows the *seed* id — which is usually what they have in hand from `sd ready`, `sd show`, or recent context — they hit a confusing dead-end:\n\n```\n$ sd plan show mulch-7233\nError: Plan not found: mulch-7233. Run 'sd plan list' to see available plans.\n$ sd plan list | head -1\npl-e01a  approved  rev 1  feature  seed=mulch-7233  children=5  ...\n```\n\nThe seed knows its plan via the `plan_id` field — the lookup is a one-line dereference, but the user has to round-trip through `sd plan list` and copy/paste a different id. This fires every time someone tries the natural shortcut, and erodes the \"plans are the artifact reviewed\" pitch by adding friction at the review surface.\n\n## Proposed fix\n\nIn the argument resolver for `sd plan show <id>`, `sd plan validate <id>`, `sd plan outcome <id>`, and `sd plan review <id>`:\n\n1. If the id starts with the plan prefix (`pl-`), look it up as a plan (current behavior).\n2. Otherwise, look it up as a seed and resolve through `seed.plan_id`.\n3. If the seed has no `plan_id`, fail with a clear message: `Seed mulch-7233 has no plan. Submit one with 'sd plan submit mulch-7233 --plan <file>'.`\n4. If the seed id doesn't exist, fail with the existing not-found phrasing.\n\nUse a shared helper (e.g. `resolvePlanIdArg(arg, store)`) so the four commands stay consistent.\n\n### Edge case: seeds with both incoming and outgoing plan links\n\nA seed that is a sub-plan parent (e.g. seeds-6e0c in the stress test) has `plan_id` pointing to the plan *it spawns*, not the plan *it is a step of*. That's the right semantic for `sd plan show <seed>` — \"show the plan attached to this seed\" reads as the plan rooted at this seed. No special-casing needed; the existing `plan_id` field already encodes the right preference.\n\nFor *step* children (seeds with `plan_step_index` set), `plan_id` points to the parent plan — also the right answer for `sd plan show <child-seed>` (\"show the plan I'm a step of\").\n\nSo the resolver doesn't need to disambiguate; it just trusts `seed.plan_id`.\n\n## Acceptance\n\n- `sd plan show <pl-id>` continues to work unchanged.\n- `sd plan show <seed-id>` resolves to the plan via `seed.plan_id` and prints it.\n- Same resolution for `sd plan validate`, `sd plan outcome`, `sd plan review`.\n- Seed without a plan: error message includes the suggested `sd plan submit` command.\n- Nonexistent id: existing not-found phrasing.\n- Tests in `src/commands/plan.test.ts` covering all four commands × {pl-id, seed-id with plan, seed-id without plan, unknown id}.\n\n## Files\n\n- `src/commands/plan.ts` (resolver + four command handlers)\n- `src/commands/plan.test.ts`","closedAt":"2026-05-06T22:19:23.495Z"}
{"id":"seeds-3da2","title":"sd onboard: surface project version in snippet (replace opaque schema integer)","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-06T22:23:40.270Z","updatedAt":"2026-05-06T23:06:44.218Z","description":"## Problem\n\nThe `sd onboard` snippet injected into AGENTS.md / CLAUDE.md currently includes only an opaque schema-version marker:\n\n```\n## Issue Tracking (Seeds)\n<!-- seeds-onboard-v:3 -->\n```\n\nDefined at `src/commands/onboard.ts:8-9`:\n\n```ts\nconst ONBOARD_VERSION = 3;\nconst VERSION_MARKER = `<!-- seeds-onboard-v:${String(ONBOARD_VERSION)} -->`;\n```\n\nUsers / agents reading AGENTS.md see \"V3\" (or whatever the next bump produces) and have no way to map that to a meaningful seeds release. The number is decoupled from `package.json` (`0.4.0`) and only changes when someone manually bumps the snippet schema. Goal: the snippet should show the actual project version (e.g. `0.4.0`) so consumers can correlate it with installed seeds.\n\nNote: `src/index.ts:2` already exposes `const VERSION = \"0.4.0\"` for the CLI banner; that constant can be reused.\n\n## Desired behavior\n\nThe visible/identifying version in the emitted snippet should be the package version (`0.4.0` today). One concrete rendering:\n\n```\n## Issue Tracking (Seeds)\n<!-- seeds-onboard:v0.4.0 -->\n\nThis project uses [Seeds](https://github.com/jayminwest/seeds) v0.4.0 for git-native issue tracking.\n```\n\n## Design tradeoff to resolve\n\nThe current `ONBOARD_VERSION` integer drives **outdated-snippet detection** on re-run (see version-bump test at `src/commands/onboard.test.ts:125-138` updating v0 → v3, and the inclusion assertion at lines 149-154). If we replace the schema integer with the package version, every patch release would mark every existing snippet as outdated, even when the snippet body did not change.\n\nTwo reasonable resolutions — pick one in implementation:\n\n1. **Replace** — make the marker `<!-- seeds-onboard:v<package.version> -->` and accept that any version bump prompts an update. Simplest, but noisy.\n2. **Decouple display from detection** — keep an internal schema-version marker for change detection (renamed e.g. `<!-- seeds-onboard-schema:N -->`) AND render the package version in user-visible body text and/or a separate `<!-- seeds-onboard:v0.4.0 -->` line that is informational only. Recommended.\n\n## Implementation pointers\n\n- `src/commands/onboard.ts:8` — `ONBOARD_VERSION` constant\n- `src/commands/onboard.ts:9` — `VERSION_MARKER` template\n- `src/commands/onboard.ts:13-49` — `onboardSnippet()` template body\n- `src/index.ts:2` — existing `VERSION = \"0.4.0\"` constant (already exported; reuse rather than re-reading package.json)\n- Outdated detection logic — find every callsite that compares the current marker against the marker found in AGENTS.md/CLAUDE.md and adapt to whichever scheme above is chosen.\n\n## Tests to update\n\n`src/commands/onboard.test.ts`:\n- Lines 125-138: version-bump detection (v0 → current)\n- Lines 149-154: marker inclusion assertion (currently `seeds-onboard-v:3`)\n- Lines 156-167: planning section coverage\n- Lines 177-184: idempotency on re-run\n\nAdd a test that the rendered snippet contains the package version verbatim.\n\n## Acceptance criteria\n\n- [ ] Running `sd onboard` (or `--print`) on a fresh AGENTS.md emits a snippet whose visible identifier is the package version (e.g. `0.4.0`).\n- [ ] Re-running `sd onboard` on an already-injected snippet that matches the current version is a no-op (idempotent).\n- [ ] Bumping the snippet **content** still triggers an update prompt on existing AGENTS.md (whichever detection scheme is chosen, the change-detection signal must continue to work).\n- [ ] Tests covering snippet content + version display pass.\n- [ ] Cross-repo: `mulch` has a parallel issue for `ml onboard`; consider keeping the two in sync stylistically.","labels":["onboard","dx"],"closedAt":"2026-05-06T23:06:44.218Z","closeReason":"Implemented option 2 (decouple display from detection): seeds-onboard-schema:N drives outdated detection; seeds-onboard:v$VERSION renders the package version in marker + body. Legacy seeds-onboard-v:N markers auto-upgrade. VERSION extracted to src/version.ts to avoid index.ts side-effects on import. Tests + lint + typecheck all green."}
{"id":"seeds-4a54","title":"sd plan submit inverts step.blocks dependency direction vs PLAN_SPEC","status":"closed","type":"bug","priority":1,"createdAt":"2026-05-08T15:30:29.903Z","updatedAt":"2026-05-08T15:44:59.680Z","description":"**Symptom**\nLLMs writing plans per PLAN_SPEC.md (forward 'blocks' semantics) end up with their dependency chain wired in reverse on submit. `sd ready` surfaces the LAST step of a sequential plan first.\n\n**Concrete repro (observed in ../burrow pl-5b40)**\nPlan steps (LLM-authored, forward semantics):\n  step 0 blocks=[1], step 1 blocks=[2], ..., step 6 blocks=[7], step 7 blocks=[]\n\nAfter submit, child seeds:\n  c0 (Phase 0): blocks=[parent],     blockedBy=[c1]\n  c1 (Phase 1): blocks=[c0, parent], blockedBy=[c2]\n  ...\n  c7 (Phase 7): blocks=[c6, parent], blockedBy=undefined\n\n`sd ready` returns c7 (the last phase) as the ready step, not c0. The chain runs backwards.\n\n**Root cause**\nPLAN_SPEC.md:248-257 documents forward semantics:\n  > step 0 with blocks: [1] means step 0 blocks step 1\n  > example: 'Add OAuth provider config' blocks: [1] → 'Wire callback handler'\n\nBut src/commands/plan.ts:542-552 treats step.blocks as blocked-by indices:\n```\nconst deps = step.blocks ?? [];\n...\nissue.blockedBy = blockedByIds;  // step i's own blockedBy = step.blocks targets\n```\n\nsrc/commands/plan.test.ts:87-90,260 enshrines the inverted semantics: Step B with blocks: [0] → B.blockedBy=[A].\n\nBidirectional integrity is maintained internally (the inverse loop at plan.ts:559-563 is inverted to match), so `sd doctor` does NOT catch this. The chain is just semantically backwards.\n\n**Fix direction**\nFlip plan.ts to match the spec — when step i has blocks=[j], spawn child i with i.blocks containing child j (and child j.blockedBy containing child i). Update plan.test.ts assertions and add an INSTRUCTIONS line clarifying 'blocks = step indices this step blocks (i.e. that depend on it)'. Existing inverted plans in the wild need a migration or a one-time `sd doctor --fix` pass.\n\n**Surfaced by**\nburrow-e0f6 (../burrow). Reporter mis-described the data (cited a non-existent mismatch in pl-5b40 and conflated 37 doctor warnings from older plans pl-a253/pl-2085 with a new bug), but the underlying inversion is real and confirmed by tracing pl-5b40 + running `sd ready` in burrow.\n\n**Impact**\nAny plan an LLM writes following PLAN_SPEC will have child seeds blocked in reverse order. `sd ready` will surface trailing steps first; agents pick them up out of order. Doctor stays silent because internal directionality is consistent.","closedAt":"2026-05-08T15:44:59.680Z","closeReason":"Fixed in src/commands/plan.ts: step.blocks now uses forward semantics (step i with blocks=[j] means step i blocks step j, so child j.blockedBy gains child i and child i.blocks gains child j). Both fresh-submit and overwrite paths handle bidirectional edges including matched-child sources/targets, with dedupe. PLAN_SPEC.md:257, prompt INSTRUCTIONS, onboard.ts and prime.ts updated. Existing inverted plans tracked in seeds-1c38 for migration."}
{"id":"seeds-1c38","title":"Migrate plans submitted before seeds-4a54 fix to forward step.blocks semantics","status":"closed","type":"task","priority":2,"createdAt":"2026-05-08T15:44:54.443Z","updatedAt":"2026-05-10T18:07:50.974Z","description":"seeds-4a54 flipped step.blocks from inverted to forward semantics. Plans submitted before the fix have child issues whose blockedBy/blocks edges run in reverse vs the plan row's step.blocks. sd ready returns trailing steps first on those plans.\n\nMigration approach: scan plans.jsonl rows; for each non-draft plan with steps that declare blocks=[j], inspect the corresponding child issues. If child i.blockedBy contains child j (the inverted form), swap so child j.blockedBy contains child i and child i.blocks contains child j. Could be a sd doctor --fix branch or a one-shot script.\n\nDetection signal: for any plan, walk step.blocks edges and check whether the spawned children's blockedBy direction matches the spec. Older revisions are wrong; new ones (post seeds-4a54) are right.\n\nNote: the plan row's sections.steps[i].blocks values themselves are unchanged by the fix — they were the source-of-truth all along. Only the spawned-issue dep edges need rotation.","closedAt":"2026-05-10T18:07:50.974Z"}
{"id":"seeds-4eba","title":"sd show: accept multiple IDs with clear separation in all output formats","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-08T20:56:08.229Z","updatedAt":"2026-05-10T18:01:41.334Z","description":"Currently `sd show` only accepts a single issue ID, forcing agents and users to run one command per issue:\n\n```\n$ sd show burrow-7ba7 burrow-fbdf burrow-0fab\nerror: too many arguments for 'show'. Expected 1 argument but got 3.\n```\n\nThis causes verbose, repeated tool calls when reviewing related issues (e.g. an agent inspecting a set of blockers, deps, or a plan's children).\n\n## Proposal\n\nAllow `sd show` to accept one or more IDs, like `sd close`:\n\n```\nsd show <id> [<id2> ...]\n```\n\n## Output requirements\n\n- **Human format (default):** Render each issue using the existing single-issue layout, separated by a clear visual divider (e.g. a horizontal rule `────` or blank lines + a header line) so issues don't visually bleed into each other.\n- **JSON (`--json` / `--format json`):** Emit an array of issue objects (not a stream of separate objects).\n- **compact / markdown / plain / ids:** Apply the same per-format separator conventions used elsewhere (markdown: `---` between issues; plain: blank line; ids: one ID per line).\n- Preserve current single-ID behavior exactly when only one ID is passed (no array wrapping in JSON for the single case is debatable — pick whichever matches `sd close`'s precedent for consistency).\n- Unknown IDs should report a per-ID error but not abort the rest (mirror `sd close` semantics).\n\n## Acceptance\n\n- `sd show a b c` prints all three with clear separation in human output\n- `sd show a b c --format json` returns a parseable array\n- Tests cover: multi-ID success, mixed valid/invalid IDs, each output format\n- README + `sd prime` reference updated","closedAt":"2026-05-10T18:01:41.334Z"}
{"id":"seeds-deea","title":"Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-10T16:50:14.270Z","updatedAt":"2026-05-10T17:27:33.130Z","description":"## Context\n\nWarren V2 (see warren/ROADMAP.md item R-01) is going to use seeds as the source of truth for issues — no separate warren issues table. To do this, warren needs to attach runtime metadata to seeds: assigned agent role, scheduled-for timestamp, trigger source, last-run pointer, queued-but-don't-dispatch flag.\n\nToday seeds has fixed fields (status, type, priority, labels, plan_id, etc.) and no structured extension point. Labels (`role:refactor-bot`, `trigger:cron`) work as a hack but lose their structure on read and don't round-trip cleanly. Other ecosystem tools (greenhouse, overstory) will eventually want similar runtime annotations.\n\nThe persistence rule for warren is: \"DB only holds runtime state (runs, events, schedule fire-history). Definitions, expertise, issues all live as git-tracked files in the project repo.\" Seeds needs a clean place for warren-specific issue metadata that travels with the seed.\n\n## Sketch\n\nOne-line schema addition in `src/types.ts`:\n\n    export interface Issue {\n      // ... existing fields ...\n      extensions?: Record<string, unknown>;\n    }\n\n- **Validation:** none. Each consumer (warren, greenhouse, etc.) owns the shape of its own keys; seeds treats `extensions` as opaque JSON.\n- **Storage:** round-trips through `.seeds/issues.jsonl` like any other field.\n- **Display:** `sd show` adds an \"Extensions:\" line summarizing the keys present (e.g., `Extensions: role=refactor-bot trigger=cron scheduledFor=2026-05-12T03:00:00Z`).\n\nCLI surface — extend `sd update` with `--extensions <json>` (merge semantics, not replace), so partial updates don't clobber other consumers' keys. Optionally a `sd extensions <set|show|remove>` subcommand for per-key writes that don't require composing JSON on the command line.\n\nWarren's namespaced keys, agreed by convention:\n\n    extensions: {\n      role: \"refactor-bot\",                    // canopy role name\n      scheduledFor: \"2026-05-12T03:00:00Z\",    // ISO8601\n      trigger: \"cron\",                          // manual | cron | webhook | comment\n      queued: true,                             // \"created but don't run yet\"\n      lastRunId: \"wr-...\",                      // most recent warren run\n      lastRunAt: \"2026-05-10T03:00:00Z\"\n    }\n\n## Acceptance bar\n\n1. `Issue.extensions?: Record<string, unknown>` declared in `src/types.ts`.\n2. Round-trips through `.seeds/issues.jsonl` — write, read, write preserves the field byte-for-byte.\n3. `sd update <id> --extensions '{\"role\":\"refactor-bot\"}'` merges (does NOT replace) into existing extensions.\n4. `sd show <id>` displays a human-readable extensions summary.\n5. `sd list --format json` includes the extensions field on each item.\n6. (Optional, nice-to-have) `sd ready` learns to exclude items with `extensions.queued: true` or `extensions.scheduledFor > now`, behind an opt-in flag.\n\n## Open questions for planning\n\n- Whether to add a runtime validator (JSON Schema per consumer's namespace) or leave it to consumers. Lean leave-it.\n- Whether `sd ready`'s default semantics should change to respect queued/scheduledFor, or only via opt-in flag. Lean opt-in flag for V1 of this feature.\n- Whether the `sd extensions` subcommand is worth adding now or defer. Probably defer — `sd update --extensions` covers the use case.\n- Migration: existing `.seeds/issues.jsonl` files have no extensions field; reads must treat missing field as `undefined`, never throw.\n\n## Related\n\n- warren ROADMAP R-01 (consumer)\n- warren ROADMAP R-04 (issue UI that depends on this)\n- warren ROADMAP R-06 (cron scheduler that reads `extensions.scheduledFor`)\n\nThis seed is intentionally scoped as a single feature for an agent to expand into a full plan via `sd plan submit`.","labels":["warren-integration","extensibility"],"plan_id":"pl-c195","closedAt":"2026-05-10T17:27:33.130Z"}
{"id":"seeds-f35a","title":"Add Issue.extensions field + JSONL round-trip test","status":"closed","type":"feature","priority":1,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-c195.\n\nParent seed: seeds-deea — Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata\nPlan template: feature\nPlan approach: Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON,…\n\nRun `sd plan show pl-c195` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:00:29.674Z","plan_id":"pl-c195","blocks":["seeds-e7ea","seeds-be14","seeds-614b","seeds-56ff","seeds-27d1","seeds-deea"],"closedAt":"2026-05-10T17:00:29.674Z","closeReason":"Added Issue.extensions?: Record<string, unknown> to types.ts and round-trip test in store.test.ts (mixed scalars, ISO8601, nested object, array, null). 561 tests pass, lint+typecheck clean."}
{"id":"seeds-e7ea","title":"Render extensions in sd show output","status":"closed","type":"feature","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-c195.\n\nParent seed: seeds-deea — Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata\nPlan template: feature\nPlan approach: Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON,…\n\nRun `sd plan show pl-c195` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:05:47.340Z","plan_id":"pl-c195","blocks":["seeds-27d1","seeds-deea"],"closedAt":"2026-05-10T17:05:47.340Z","closeReason":"Added formatExtensionsLine in src/output.ts; rendered in formatIssueFull (default + plain via stripAnsi), JSON mode already serializes the issue. Tests in src/commands/format.test.ts cover present/missing/empty/json. bun test (564), bun run lint, bun run typecheck all pass."}
{"id":"seeds-be14","title":"Add sd update --extensions <json> with shallow merge semantics","status":"closed","type":"feature","priority":1,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-c195.\n\nParent seed: seeds-deea — Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata\nPlan template: feature\nPlan approach: Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON,…\n\nRun `sd plan show pl-c195` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:09:47.188Z","plan_id":"pl-c195","blocks":["seeds-27d1","seeds-deea"],"closedAt":"2026-05-10T17:09:47.188Z","closeReason":"Added sd update --extensions <json> with shallow-merge into existing extensions and --clear-extensions. parseExtensionsPatch rejects non-object JSON (arrays, null, scalars) with 'must be a JSON object' error; malformed JSON yields '--extensions must be valid JSON: <reason>'. Mutually exclusive with --clear-extensions. Empty merge result drops the field. 11 new tests in src/commands/update.test.ts. bun test 575 pass · lint clean · typecheck clean."}
{"id":"seeds-614b","title":"Add opt-in sd ready --respect-schedule flag","status":"closed","type":"feature","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-c195.\n\nParent seed: seeds-deea — Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata\nPlan template: feature\nPlan approach: Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON,…\n\nRun `sd plan show pl-c195` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:13:23.873Z","plan_id":"pl-c195","blocks":["seeds-27d1","seeds-deea"],"closedAt":"2026-05-10T17:13:23.873Z","closeReason":"Implemented sd ready --respect-schedule opt-in flag. Filters issues where extensions.queued === true (strict) or extensions.scheduledFor parses to a future ISO8601 timestamp. Default ready behavior unchanged. Tests cover queued, future/past scheduledFor, malformed timestamps, and truthy-but-not-strict queued."}
{"id":"seeds-56ff","title":"Doctor schema check for malformed extensions","status":"closed","type":"feature","priority":3,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-c195.\n\nParent seed: seeds-deea — Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata\nPlan template: feature\nPlan approach: Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON,…\n\nRun `sd plan show pl-c195` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:20:49.438Z","plan_id":"pl-c195","blocks":["seeds-27d1","seeds-deea"],"closedAt":"2026-05-10T17:20:49.438Z"}
{"id":"seeds-27d1","title":"Update README, CHANGELOG, CLAUDE.md to document extensions","status":"closed","type":"task","priority":2,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-c195.\n\nParent seed: seeds-deea — Add `extensions: Record<string, unknown>` field to Issue type for runtime metadata\nPlan template: feature\nPlan approach: Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON,…\n\nRun `sd plan show pl-c195` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:26:27.345Z","plan_id":"pl-c195","blocks":["seeds-deea"],"closedAt":"2026-05-10T17:26:27.345Z","closeReason":"Documented Issue.extensions in README (new Extensions section + table flag updates), CHANGELOG ([Unreleased] entry covering all 5 prior steps), and CLAUDE.md (Issue type field, CLI reference for --extensions/--clear-extensions/--respect-schedule, new Extensions subsection under Coding Conventions). Quality gates: 587 tests pass, lint clean, typecheck clean."}
{"id":"seeds-ac83","title":"Add `sd config schema` and `sd config set` CLI for warren UI consumption","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-10T17:08:27.416Z","updatedAt":"2026-05-10T17:45:29.220Z","description":"## Context\n\nWarren V2 (see warren/ROADMAP.md item R-10) wraps each os-eco tool's config surface in a generic schema-driven UI. Each tool publishes its config JSON Schema; warren renders forms automatically and writes back via per-knob CLI commands.\n\nSeeds' config surface is small but real: `max_plan_depth`, `plan_templates` (nested), `project`, `version`. The headline knob is `plan_templates` — a nested editor for custom plan templates that's the natural counterpart to mulch's `custom_types` editor. Both tools sharing this nested-schema-editor pattern in warren's UI is the leverage point.\n\n## Sketch\n\nTwo new CLI surfaces:\n\n### 1. `sd config schema [--json]`\n\nEmits the JSON Schema for `.seeds/config.yaml`. Covers `max_plan_depth`, `plan_templates`, `project`, `version`. Each property has title/description/default. The `plan_templates` nested structure (sections with kind/required/min/min_length/item/mulch_source/prompt) gets full schema coverage so warren's UI can render a structured editor for it.\n\nSchema is the wire-format contract; lock with golden tests.\n\n### 2. `sd config set <path> <value>`\n\nDot-path writes: `sd config set max_plan_depth 5`, `sd config set plan_templates.spike.sections.context.kind text`. Value is YAML-parsed. Validation runs against the JSON Schema before write.\n\n`sd config show [--path <p>] [--json]` for reads; `sd config unset <path>` for deletes.\n\n## Acceptance bar\n\n1. `sd config schema --json` emits a complete JSON Schema for `.seeds/config.yaml`.\n2. Golden test locks the schema shape.\n3. `sd config set <path> <value>` validates + writes atomically.\n4. `sd config show --path <p> --json` reads any path.\n5. Nested writes into `plan_templates.<name>.sections.<section>.<field>` work cleanly.\n\n## Open questions for planning\n\n- Whether `plan_templates` editing is better via `sd plan templates set` (template-aware command) or `sd config set plan_templates.<name>` (generic config command). Either works; lean generic for V1 so warren's UI uses one CLI surface.\n- Migration: AJV-validated plan templates have stricter rules than the surrounding config. Make sure validation errors are clear when a template entry violates plan schema rules.\n- Whether to expose the built-in templates as discoverable config (so warren's UI can show the user \"here are the built-ins; copy one to start\"). Lean yes — `sd config schema` includes built-in template defaults in `examples`.\n\n## Related\n\n- warren ROADMAP R-10 (consumer)\n- seeds-deea (extensions field — precedent for warren-integration cross-repo work)\n- mulch's parallel seed (`ml config schema` / `ml config set`)\n\nThis seed is intentionally scoped as a single feature for an agent to expand into a full plan via `sd plan submit`.","labels":["warren-integration","cli","config"],"closedAt":"2026-05-10T17:45:29.220Z","closeReason":"sd config schema/show/set/unset shipped: JSON Schema for .seeds/config.yaml emitted via sd config schema, sd config set/unset validates against the schema before writing under the config.yaml advisory lock, sd config show reads at any dot-path. 21 new tests; CHANGELOG/README/CLAUDE.md updated."}
{"id":"seeds-66de","title":"sd show pl-xxxx should show plan details, not error 'Issue not found'","status":"closed","type":"bug","priority":2,"createdAt":"2026-05-10T17:31:53.572Z","updatedAt":"2026-05-10T17:50:57.528Z","description":"Running `sd show pl-fd71` (or any plan ID) errors:\n\n```\nError: Issue not found: pl-fd71\n```\n\n`sd show` only looks up issues by ID. When the ID prefix is `pl-`, it should detect this is a plan and route to plan display logic (equivalent to `sd plan show <pl-id>`), or at minimum return a clearer error directing the user to `sd plan show`.\n\nRepro:\n1. `sd plan submit <seed-id> --plan <file>` to create a plan\n2. `sd show pl-xxxx` → errors with 'Issue not found'\n\nExpected: shows plan sections, children, and nested sub-plans (same output as `sd plan show`).","closedAt":"2026-05-10T17:50:57.528Z","closeReason":"sd show now routes pl-* ids to plan show (markdown + json)"}
{"id":"seeds-185f","title":"Switch sd plan submit step indices from 0-based to 1-based","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-11T00:24:49.908Z","updatedAt":"2026-05-14T00:35:42.615Z","description":"## Problem\n\nThe `steps[].blocks` field in `sd plan submit` payloads uses **0-based** indices into the steps array. LLMs (the primary author of plan files) consistently get this wrong — they default to 1-based, producing off-by-one wiring or `blocks: [N]` references that overflow the array.\n\nSymptoms:\n- Plans submit cleanly but spawn the wrong dependency edges (silent off-by-one)\n- \"First step blocks second\" written as `blocks: [1]` on step 0 — correct by accident\n- \"First step blocks second\" written as `blocks: [2]` on step 1 — wrong, but validator catches it\n- LLMs writing `blocks: [0]` to mean \"the first step depends on this\" — currently a self-reference error, would be a clear \"1-based\" error post-change\n\nThe `blocks` field is an LLM/human authoring surface, not a code-level array index. If the primary author keeps misreading it, the wire format is wrong.\n\n## Proposal\n\nSwitch `steps[].blocks` to **1-based**. Step 1 is the first step, step N is the last. `blocks: [2]` on step 1 means \"step 1 blocks step 2\".\n\n`plan_step_index` on spawned child issues stays **0-based** (it's an internal back-link, not author-facing). Document this asymmetry explicitly.\n\n## Surface area\n\n- `src/commands/plan.ts:217` — INSTRUCTIONS string: clarify \"1-based step indices (step 1 is the first step)\"\n- `src/commands/plan.ts:545-552` — fresh-submit forward-edge translation: `newChildIds[j - 1]`\n- `src/commands/plan.ts:562-566` — fresh-submit reverse-edge wiring: `newIssues[j - 1]`\n- `src/commands/plan.ts:847-860` (approx) — overwrite-path edge wiring (mirrors fresh-submit)\n- Plan validator — bounds become `1..steps.length`; reject `0` with explicit \"blocks uses 1-based step indices\" error; self-reference check becomes \"i+1 in step i.blocks\"\n- `PLAN_SPEC.md:248-249` — example\n- `PLAN_SPEC.md:257` — semantics paragraph (\"step 0 with blocks: [1]\" → \"step 1 with blocks: [2]\")\n- `PLAN_SPEC.md:279` — table row referencing step_index\n- `PLAN_SPEC.md:367` — validator bullet\n- `src/commands/plan.test.ts` — ~15 fixture sites using `blocks: [1]`, `[2]`, `[3]` (each needs +1 and the expected wiring re-verified)\n- `src/commands/plan-lifecycle.test.ts` — audit for same patterns\n\n## Migration\n\nOne existing plan row in this repo's `.seeds/plans.jsonl` has `sections.steps[].blocks` values. Either:\n1. One-shot script to `+1` every entry in `blocks` arrays across `plans.jsonl` (small, scoped)\n2. Accept staleness on closed/historical plans (the stored numbers are display-only after spawning; children already have their resolved blocks/blockedBy edges)\n\nOption 2 is simpler and lower-risk; the plan row's `sections.steps[].blocks` is informational once children are spawned. Recommend option 2 unless we discover a consumer that re-reads those numbers.\n\nNo CHANGELOG entry needed for in-flight data because seeds isn't yet relied on outside this ecosystem; document the format change loudly in CHANGELOG under the next minor release.\n\n## Acceptance\n\n- [ ] `sd plan submit` accepts 1-based `blocks` values; rejects `0` with a clear error\n- [ ] Self-reference detection uses 1-based position (step at index 0 cannot have `blocks: [1]`)\n- [ ] Out-of-range error message reads \"step indices are 1-based; valid range 1..N\"\n- [ ] PLAN_SPEC.md examples, prose, and validator bullets updated\n- [ ] INSTRUCTIONS in `buildPlanRequest` mentions \"1-based\" explicitly\n- [ ] All tests pass after fixture migration\n- [ ] CHANGELOG notes the breaking change to the submit wire format\n\n## Notes\n\n- Considered alternatives (rejected):\n  - Reference steps by `key` field instead of position — more verbose, larger surface change\n  - Reference by title — fragile, breaks on title edits\n  - Keep 0-based and beef up the prompt — band-aid; doesn't fix the ergonomic mismatch\n- The internal `plan_step_index` on children stays 0-based intentionally; that field is consumed by code (`resolveStepPlanTemplate` at `plan.ts:333-346`), not authored by LLMs.","closedAt":"2026-05-14T00:35:42.615Z"}
{"id":"seeds-5640","title":"Add names/titles to plans for human + agent readability","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-14T00:13:57.993Z","updatedAt":"2026-05-14T00:26:35.797Z","description":"## Problem\n\n`sd plan list` output is unreadable when scanning multiple plans:\n\n```\npl-a31c  done      rev 1  feature  seed=warren-0a83  children=5 (success)  2026-05-13T22:53:52.957Z\npl-9ba1  approved  rev 1  feature  seed=warren-6747  children=10           2026-05-12T01:50:00.213Z\npl-4374  done      rev 1  feature  seed=warren-39c1  children=7            2026-05-12T01:49:09.923Z\n```\n\nEvery plan looks identical — opaque IDs, template name, parent seed ID, child count, timestamp. Nothing tells you what the plan is *about*. Agents (and humans) have to `sd plan show <id>` each one to figure out which is which. This breaks the agent loop: pick a plan from a list → can't, no semantic signal.\n\nThe parent seed has a title, but it's not surfaced in plan listings, and a plan can diverge from its seed's title (especially after revisions or when one seed spawns multiple plans).\n\n## Proposal\n\nAdd a `name` (or `title`) field to plans:\n\n1. **Schema:** `Plan.name?: string` — short human-readable label (e.g. \"Schema-driven config editor\", \"Fix race in sync lock\").\n2. **Populated at `sd plan submit` time:**\n   - Inferred from the parent seed's title by default (good enough most of the time).\n   - Overrideable via `--name <text>` flag.\n   - The structured plan JSON itself could carry a top-level `name` field that submit reads.\n3. **Surfaced in:**\n   - `sd plan list` — column between status and template, truncated to ~40 chars.\n   - `sd plan show` — header line.\n   - `--json` / `--format compact` output — new field, additive.\n\n## Example after\n\n```\npl-a31c  done      Schema-driven config editor       feature  seed=warren-0a83  ...\npl-9ba1  approved  Plan revision UX                   feature  seed=warren-6747  ...\npl-4374  done      Wire greenhouse → overstory hand…  feature  seed=warren-39c1  ...\n```\n\n## Open questions\n\n- Required vs optional? Optional is safer (back-compat with existing plans), but a required name would force the discipline. Probably: optional in schema, but `sd plan submit` auto-derives one from the parent seed title if not provided, so in practice it's always populated going forward.\n- Mutable after submit? Probably yes — `sd plan update <pl-id> --name <text>` or fold into existing update path. Out of scope for v1.\n- Should revisions inherit the name? Yes, default; allow override on overwrite/resubmit.\n\n## Related\n\n- mx records on plan workflow: planning UX is increasingly the bottleneck for agent loops (overstory dispatches → which plan? warren reviews → which plan?).\n- Compare to seed titles: seeds got this right from day one because they're the primary work unit. Plans are second-class in display but first-class in workflow.","closedAt":"2026-05-14T00:26:35.797Z","closeReason":"Implemented Plan.name field with --name flag, JSON-level name, and seed-title fallback. Surfaced in plan show header, plan list column, and --json payloads. Tests in src/commands/plan.test.ts."}
{"id":"seeds-8f1c","title":"Test extension","status":"closed","type":"task","priority":1,"createdAt":"2026-05-14T05:33:17.445Z","updatedAt":"2026-05-14T05:33:22.627Z","extensions":{"queued":true,"scheduledFor":"2026-05-20T10:00:00Z"},"closedAt":"2026-05-14T05:33:22.627Z","closeReason":"testing"}
{"id":"seeds-f0a2","title":"Merge test","status":"closed","type":"task","priority":1,"createdAt":"2026-05-14T05:33:25.086Z","updatedAt":"2026-05-14T14:14:18.408Z","closedAt":"2026-05-14T14:14:18.408Z"}
{"id":"seeds-3c89","title":"Allow sd plan to adopt and release existing seeds","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-14T05:45:33.932Z","updatedAt":"2026-05-14T14:14:35.913Z","description":"Plans currently only spawn new child seeds. We need bidirectional linking so currently-open seeds can be adopted into plans (new or existing) and released back out.\n\n## Motivation\n\n`sd plan submit` always generates fresh children from `steps[]`. There's no path to:\n- adopt an already-open seed as a step at submit time\n- attach an open seed to an already-submitted plan after the fact\n- detach a seed from a plan without closing it\n\nThe seed↔plan link is just three fields (`plan_id`, `plan_step_index`, `requires_plan`) plus edges (`blocks` / `blockedBy`). Adoption = setting those and wiring; release = unsetting and unwiring. Backref management already exists (`src/plan-backref.ts:applyPlanBackref`).\n\n## Surfaces\n\n1. **`sd plan submit` step schema gains `existing_seed?: string`** — per-step \"adopt or spawn\"\n2. **`sd plan adopt <plan-id> <seed-id...>`** — post-submit adoption (`--step <i>` for slotting)\n3. **`sd plan release <plan-id> <seed-id...>`** — inverse: detach without closing\n\n## Code anchors\n\n- Fresh-submit spawn loop: src/commands/plan.ts:537-588\n- applyOverwrite (closest analogue to adopt — matches old children by title): src/commands/plan.ts:751-947\n- Backref helpers (handles preserve-existing-description): src/plan-backref.ts\n- Issue plan fields: src/types.ts:14-16\n- Plan template AJV schema: src/plan-schema.ts\n- Lock order (mx-f29e43): outer plansPath, inner issuesPath\n\n## Edge cases to settle in the plan\n\n- Reject adopting closed seeds; reject adopting a seed that already has a plan_id unless explicit reassign flag.\n- On existing_seed + step.title mismatch: seed.title wins, warn on stderr.\n- plan_template + existing_seed: if adopted seed already has a plan, that's the sub-plan; else set requires_plan=true.\n- Overwrite path must match by existing_seed id first, then by title.\n- Release: strip backref block via marker regex (do not touch surrounding description); drop parent seed from seed.blocks; drop seed from parent blockedBy; clear plan_id and plan_step_index.\n- sd plan show rendering: tag adopted children with (adopted) in human output (optional).\n\n## Out of scope\n\n- Cross-plan moves in a single command (do release then adopt for now)\n- Auto-bumping plan revision on adopt/release (decide in plan; lean \"yes\" for diff visibility)","plan_id":"pl-43ff","closedAt":"2026-05-14T14:14:35.913Z"}
{"id":"seeds-7002","title":"Extend Step JSON schema with existing_seed in src/plan-schema.ts","status":"closed","type":"task","priority":2,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T05:50:13.148Z","plan_id":"pl-43ff","blocks":["seeds-24c6","seeds-99ae","seeds-2b93","seeds-2b8a","seeds-3c89"],"closedAt":"2026-05-14T05:50:13.148Z","closeReason":"Extended STEP_SCHEMA in src/plan-schema.ts with optional existing_seed (string, minLength 1); mirrored in SubmittedStep TS interface; added accept/reject schema tests. Wiring is for seeds-24c6."}
{"id":"seeds-24c6","title":"Wire existing_seed into fresh-submit path in runSubmit (src/commands/plan.ts)","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:05:30.446Z","plan_id":"pl-43ff","blocks":["seeds-99ae","seeds-a3ab","seeds-e198","seeds-aef6","seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:05:30.446Z","closeReason":"Wired existing_seed into fresh-submit path: validateAdoptions runs pre-write, fresh+adopted paths share unified edge-wiring via updateChildField/appendUnique, adopted seeds get backref + plan_id/plan_step_index but keep title/type/priority/assignee/status, plan.children references adopted ids. 10 new tests cover happy path, link-only preservation, all rejection cases (self/missing/closed/already-attached/duplicate/plan_template-conflict), title-mismatch warning, and rollback on validation failure."}
{"id":"seeds-99ae","title":"Update applyOverwrite to match by existing_seed id first, then by title","status":"closed","type":"task","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:14:20.583Z","plan_id":"pl-43ff","blocks":["seeds-e198","seeds-aef6","seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:14:20.583Z","closeReason":"applyOverwrite now matches step.existing_seed id first, then step.title; external adoptions via overwrite link a new seed into the plan (plan_id, plan_step_index, backref, parent-blocks edge); validateAdoptions takes optional allowedCurrentPlanId so existing plan-children can be pinned by id"}
{"id":"seeds-2b93","title":"Add sd plan adopt <plan-id> <seed-id...> [--step <i>] subcommand","status":"closed","type":"feature","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:25:40.960Z","plan_id":"pl-43ff","blocks":["seeds-a3ab","seeds-e198","seeds-aef6","seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:25:40.960Z","closeReason":"Implemented sd plan adopt <plan-id> <seed-ids...> [--step <i>]: validation pass + link-only mutations + revision bump under outer plans / inner issues locks. 14 new tests pass; full suite 679 pass."}
{"id":"seeds-2b8a","title":"Add sd plan release <plan-id> <seed-id...> subcommand","status":"closed","type":"feature","priority":2,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:33:44.901Z","plan_id":"pl-43ff","blocks":["seeds-e198","seeds-aef6","seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:33:44.901Z"}
{"id":"seeds-a3ab","title":"Tag adopted children with (adopted) marker in sd plan show human output","status":"closed","type":"task","priority":3,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:44:05.626Z","plan_id":"pl-43ff","blocks":["seeds-e198","seeds-aef6","seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:44:05.626Z"}
{"id":"seeds-e198","title":"Update PLAN_SPEC.md to document adoption + release semantics, edge cases, lock order","status":"closed","type":"task","priority":2,"plan_step_index":6,"description":"<!-- seeds:plan-backref:start -->\nStep 7 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:48:12.152Z","plan_id":"pl-43ff","blocks":["seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:48:12.152Z","closeReason":"PLAN_SPEC.md updated with Adoption and Release section: lifecycle, validation rejection tables, lock order, edge cases; plus existing_seed in Step schema/submission examples, id-first overwrite precedence, and adoptedChildren on the plan row."}
{"id":"seeds-aef6","title":"Update README.md + CHANGELOG.md with new commands, JSON shape, and an end-to-end adoption example","status":"closed","type":"task","priority":3,"plan_step_index":7,"description":"<!-- seeds:plan-backref:start -->\nStep 8 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:51:06.808Z","plan_id":"pl-43ff","blocks":["seeds-72a6","seeds-3c89"],"closedAt":"2026-05-14T06:51:06.808Z","closeReason":"README.md gains 'adopt'/'release' rows in the Plan Commands table, existing_seed in the submission JSON example, and an 'Adopting existing seeds' subsection with an end-to-end example covering submit-time existing_seed, sd plan adopt (loose + --step), sd plan release, and the rejection list. CHANGELOG [Unreleased] documents the new Step.existing_seed field, sd plan adopt/release subcommands, applyOverwrite id-first precedence, and the Plan.adoptedChildren row field with the (adopted) tag in sd plan show."}
{"id":"seeds-72a6","title":"Run quality gates: bun test && bun run lint && bun run typecheck","status":"closed","type":"task","priority":2,"plan_step_index":8,"description":"<!-- seeds:plan-backref:start -->\nStep 9 of plan pl-43ff.\n\nParent seed: seeds-3c89 — Allow sd plan to adopt and release existing seeds\nPlan template: feature\nPlan approach: Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's…\n\nRun `sd plan show pl-43ff` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:53:48.012Z","plan_id":"pl-43ff","blocks":["seeds-3c89"],"closedAt":"2026-05-14T06:53:48.012Z","closeReason":"Quality gates pass: 702 tests, biome clean, tsc clean"}
{"id":"seeds-e445","title":"Richer JSON output for sd prime","status":"closed","type":"feature","priority":3,"createdAt":"2026-05-15T20:12:16.742Z","updatedAt":"2026-05-15T20:36:15.028Z","description":"Today `sd prime --json` returns a single envelope:\n\n```json\n{ \"success\": true, \"command\": \"prime\", \"content\": \"<entire markdown blob>\" }\n```\n\nThis is fine for shoveling the markdown into an LLM system prompt, but it's opaque to any structured consumer (TUI rendering, slash-command palettes, config-UI surfaces).\n\n## Proposal\n\nSplit the prime output into structured sections in JSON mode while keeping the existing markdown output unchanged for the default mode:\n\n```json\n{\n  \"success\": true,\n  \"command\": \"prime\",\n  \"sections\": {\n    \"closeProtocol\": [...],\n    \"rules\": [...],\n    \"commands\": { \"findingWork\": [...], \"creating\": [...], ... },\n    \"workflows\": [{ \"name\": \"Starting work\", \"steps\": [...] }, ...]\n  },\n  \"content\": \"...\"   // keep for backwards compat\n}\n```\n\nExact shape TBD — mirror whatever the markdown headings already are so the source of truth stays single.\n\n## Motivation\n\nPi extension investigation (os-eco/docs/pi-extensions-investigation.md, Appendix A) found this gap while spec'ing `@os-eco/pi-seeds`. The extension wants to render rules/commands/workflows in a TUI panel and drive slash-command discovery from the structured data, rather than dump the whole markdown blob into context every session.\n\nSame pattern warren consumes for `ml/sd config schema`. This is one more 'config-UI consumer' surface.\n\n## Non-goals\n\n- No change to the human-readable markdown output.\n- No change to the default (non-`--json`) output.","labels":["cli","json","integration"],"closedAt":"2026-05-15T20:36:15.028Z"}
{"id":"seeds-7164","title":"Build pi-seeds extension — issue queue integration for pi","status":"closed","type":"feature","priority":2,"createdAt":"2026-05-15T20:20:07.094Z","updatedAt":"2026-05-27T00:17:44.202Z","description":"Ship the pi extension for seeds inside this repo's existing `@os-eco/seeds-cli` npm package. Design tracked in `os-eco/docs/pi-extensions-investigation.md` §2.\n\n## Shape\n\n- Add `extensions/pi/index.ts` (+ `autocomplete.ts`, `tools.ts`, `lib/`).\n- Add `pi` manifest + `peerDependencies` (optional) to root `package.json` per §Distribution.\n- Need to add an `sd setup` command first (parity with mulch/canopy) OR drop the pi recipe into an existing init/onboard path. Decide during build.\n- Update the `<!-- seeds:start -->` onboarding marker so it shortens when pi extension is detected.\n\n## Build checklist\n\n- [ ] `session_start` → `sd prime` raw markdown injection; preload `sd ready --json` for autocomplete\n- [ ] Status widget: `sd: <n> ready / <n> in-progress / <n> blocked` via `ctx.ui.setStatus`\n- [ ] Custom tools: `sd_create`, `sd_ready`, `sd_show`, `sd_update`, `sd_close`, `sd_dep`\n- [ ] `#sd-*` autocomplete provider (port of pi's `github-issue-autocomplete.ts`)\n- [ ] `#sd-...` reference expansion in `input` event → inline `sd show` blob for LLM\n- [ ] Cache invalidation: refresh `sd ready` list on `agent_end` if any tool call wrote to `.seeds/`\n- [ ] `currentIssueId` state via `pi.appendEntry('seeds-state', ...)`\n- [ ] Commands: `/sd`, `/sd:ready`, `/sd:create`, `/sd:show <id>`, `/sd:close <id>`, `/sd:claim <id>`\n- [ ] Worktree-aware: use `ctx.cwd`, not `process.cwd()`\n- [ ] Tests against mock `ExtensionAPI`\n- [ ] README in `extensions/pi/`\n\n## Dependencies / nice-to-haves\n\n- `seeds-e445` (this repo): richer `sd prime --json` sections. Non-blocking — extension treats `sd prime` as opaque markdown for V1 and uses `sd list`/`sd ready`/`sd show` JSON for structured data.\n\n## Estimate\n\n3–5 days focused work.","labels":["pi-extension","integration"],"plan_id":"pl-a1d4","closedAt":"2026-05-27T00:17:44.202Z"}
{"id":"seeds-2774","title":"Foundation: extension skeleton, peer deps, pi.* config schema, sd setup scaffolding","status":"closed","type":"task","priority":2,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T21:07:55.362Z","plan_id":"pl-a1d4","blocks":["seeds-3a2a","seeds-adb1","seeds-e9d0","seeds-5103","seeds-89d2","seeds-7164"],"closedAt":"2026-05-15T21:07:55.362Z"}
{"id":"seeds-3a2a","title":"Session_start hooks: auto-prime + status widget + agent_end cache invalidation","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T21:15:51.458Z","plan_id":"pl-a1d4","blocks":["seeds-17a8","seeds-7164"],"closedAt":"2026-05-15T21:15:51.458Z"}
{"id":"seeds-adb1","title":"Custom tools: sd_create, sd_ready, sd_show, sd_update, sd_close, sd_dep, sd_search","status":"closed","type":"task","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T21:25:18.890Z","plan_id":"pl-a1d4","blocks":["seeds-17a8","seeds-7164"],"closedAt":"2026-05-15T21:25:18.890Z","closeReason":"Added 7 custom tools (sd_create, sd_ready, sd_show, sd_update, sd_close, sd_dep, sd_search) as thin shims over sd ... --json via pi.exec. Tools registered at extension load, resolve ctx.cwd per call. Includes 12 unit tests."}
{"id":"seeds-e9d0","title":"Autocomplete: #sd-* completion provider + input-event reference expansion","status":"closed","type":"task","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T21:37:10.889Z","plan_id":"pl-a1d4","blocks":["seeds-17a8","seeds-7164"],"closedAt":"2026-05-15T21:37:10.889Z","closeReason":"Autocomplete provider for #sd-<id> + input-event reference expansion landed in extensions/pi/lib/autocomplete.ts with 27 tests; index.ts wires session_start population + agent_end mtime refresh."}
{"id":"seeds-5103","title":"Commands: /sd, /sd:ready, /sd:create, /sd:show, /sd:close, /sd:claim + currentIssueId state","status":"closed","type":"task","priority":2,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T21:49:16.730Z","plan_id":"pl-a1d4","blocks":["seeds-17a8","seeds-7164"],"closedAt":"2026-05-15T21:49:16.730Z"}
{"id":"seeds-89d2","title":"sd setup pi recipe + pi-aware onboarding marker variant","status":"closed","type":"task","priority":2,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T22:00:49.763Z","plan_id":"pl-a1d4","blocks":["seeds-17a8","seeds-7164"],"closedAt":"2026-05-15T22:00:49.763Z","closeReason":"sd setup pi recipe + pi-aware onboarding marker variant landed. ONBOARD_SCHEMA bumped 4→5; :pi suffix on schema marker doubles as install-state detection. Built-in pi recipe writes .pi/settings.json idempotently and flips onboard snippet to the short pi-aware variant. runOnboard exported with variant/silent options so setup recipe owns user-facing messages. Tests + lint + typecheck green (792 tests pass)."}
{"id":"seeds-17a8","title":"Tests + README","status":"closed","type":"task","priority":2,"plan_step_index":6,"description":"<!-- seeds:plan-backref:start -->\nStep 7 of plan pl-a1d4.\n\nParent seed: seeds-7164 — Build pi-seeds extension — issue queue integration for pi\nPlan template: feature\nPlan approach: Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` +…\n\nRun `sd plan show pl-a1d4` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T22:07:01.270Z","plan_id":"pl-a1d4","blocks":["seeds-7164"],"closedAt":"2026-05-15T22:07:01.270Z","closeReason":"Tests added for config/prime/status lib modules; extensions/pi/README.md written with install, config, tools, commands, autocomplete, troubleshooting sections. bun test (822 pass) / lint / typecheck all green."}
{"id":"seeds-045e","title":"pi-seeds extension audit follow-ups","status":"closed","type":"feature","priority":1,"createdAt":"2026-05-15T22:43:39.806Z","updatedAt":"2026-05-27T02:58:39.337Z","description":"Audit of the newly-shipped @os-eco/pi-seeds extension surfaced 12 findings across bugs, doc mismatches, LLM tool-surface gaps, and lifecycle UX rough edges. This umbrella tracks the planned resolution.\n\nSource: agent dogfood audit, 2026-05-15. Full findings list in the plan body.","labels":["pi-extension","audit"],"plan_id":"pl-23f6","blockedBy":["seeds-f029","seeds-1bdd","seeds-6f43","seeds-19bd","seeds-52ac","seeds-c830","seeds-c969","seeds-ab15","seeds-46be","seeds-560e","seeds-f9af"],"closedAt":"2026-05-27T02:58:38.835Z","closeReason":"Rolled back pi extension; see seeds-2f21"}
{"id":"seeds-f029","title":"Fix `sd config show <path>` doc/CLI mismatch — accept positional path","status":"open","type":"task","priority":2,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-1bdd","seeds-6f43","seeds-045e"]}
{"id":"seeds-1bdd","title":"Make `sd config show --path <unset>` return null instead of erroring","status":"open","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blockedBy":["seeds-f029"],"blocks":["seeds-045e"]}
{"id":"seeds-6f43","title":"Garbage-collect empty parent objects on `sd config unset`","status":"open","type":"task","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blockedBy":["seeds-f029"],"blocks":["seeds-045e"]}
{"id":"seeds-19bd","title":"Drop Bun-specific commands from `sd prime` default closeProtocol","status":"open","type":"task","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-52ac","title":"Add `sd_sync` LLM tool (and consider sd_list, sd_blocked, sd_stats, sd_label)","status":"open","type":"task","priority":2,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-c830","title":"Accept multiple ids in `sd_close` and `sd_show` tools","status":"open","type":"task","priority":2,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-c969","title":"Make `sd_update` with no fields return a validation error","status":"open","type":"task","priority":2,"plan_step_index":6,"description":"<!-- seeds:plan-backref:start -->\nStep 7 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-ab15","title":"Surface plan-aware fields in sd_ready/sd_search tool descriptions","status":"open","type":"task","priority":2,"plan_step_index":7,"description":"<!-- seeds:plan-backref:start -->\nStep 8 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-02f6","title":"Notify (not silently no-op) when slash commands disabled via pi.commands=false","status":"closed","type":"task","priority":2,"plan_step_index":8,"description":"<!-- seeds:plan-backref:start -->\nStep 9 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-27T02:58:39.337Z","plan_id":"pl-23f6","blocks":["seeds-045e"],"closedAt":"2026-05-27T02:58:39.337Z","closeReason":"Rolled back pi extension; see seeds-2f21"}
{"id":"seeds-46be","title":"Add `#sd-<4hex>` shorthand to reference regex + document","status":"open","type":"task","priority":2,"plan_step_index":9,"description":"<!-- seeds:plan-backref:start -->\nStep 10 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-560e","title":"Refresh status widget on before_agent_start (not just agent_end)","status":"open","type":"task","priority":2,"plan_step_index":10,"description":"<!-- seeds:plan-backref:start -->\nStep 11 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-f9af","title":"Document auto-prime visibility + #sd reference gotcha in extension README","status":"open","type":"task","priority":2,"plan_step_index":11,"description":"<!-- seeds:plan-backref:start -->\nStep 12 of plan pl-23f6.\n\nParent seed: seeds-045e — pi-seeds extension audit follow-ups\nPlan template: feature\nPlan approach: Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight: • 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial…\n\nRun `sd plan show pl-23f6` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","plan_id":"pl-23f6","blocks":["seeds-045e"]}
{"id":"seeds-2f21","title":"Roll back pi-seeds extension experiment (revert, not reset)","status":"closed","type":"task","priority":2,"createdAt":"2026-05-17T18:50:36.404Z","updatedAt":"2026-05-27T02:58:58.271Z","description":"Sibling rollback to mulch-18e4 (mulch repo) and a separate cancellation in canopy. After auditing the mulch pi extension and discussing philosophy with the owner, the conclusion across all three primitives (mulch / seeds / canopy) is to remove pi extensions entirely and rely on CLAUDE.md + CLI as the universal integration surface.\n\n## Why (decision rationale)\n\nThe pi-seeds extension landed in 6 commits between 28e997f (foundation) and HEAD. Audit work in the mulch repo (this session, 2026-05-15) surfaced two classes of issues that apply equally here:\n\n1. **Recipe distribution is fragile.** The mulch audit found `ml setup pi` writes a bare scoped package name to `.pi/settings.json` that pi's resolver silently drops — the extension never loaded for any user. `sd setup pi` uses the same pattern (`@os-eco/seeds-cli` bare, no `npm:` prefix) — I have NOT empirically verified the seeds recipe is broken in the same way, but the code path is identical. **First action: verify whether `sd setup pi` produces a loading extension or a silent no-op like mulch's.** Either way the verdict below holds.\n\n2. **Extensions aren't needed.** `CLAUDE.md` is auto-discovered by pi, Claude Code, Cursor, Codex, every coding agent. `sd prime` works fine as a tool call at session start. `sd create` / `sd ready` / `sd close` work fine via bash. The qualitative differentiator extensions offer (systemPrompt-cached prime + structured tool wrappers) doesn't justify the maintenance burden:\n   - Multiple sources of truth (extension shells out to `sd`, version drift is a real failure mode)\n   - pi-specific code path that doesn't help Claude Code / Cursor / Codex users\n   - Distribution complexity (npm: prefix, auto-install, package resolution)\n   - The CLI is already JSON-shaped (`--json` everywhere), so structured tools are thin wrappers that the agent could call directly via bash\n\nExisting audit followups (seeds-045e, seeds-02f6) become moot once the extension is removed.\n\n## The 6 commits to revert (reverse chronological)\n\n```\ncc690ba test(pi): config/prime/status unit tests + README (seeds-17a8)\n8af2100 feat(pi): sd setup pi recipe + pi-aware onboarding marker variant (seeds-89d2)\n423404c feat(pi): slash commands + currentIssueId state (seeds-5103)\n593a7ca feat(pi): #sd-* autocomplete + input-event reference expansion\nd698ef2 feat(pi): custom sd_* tools (create/ready/show/update/close/dep/search)\nd83598f feat(pi): session_start auto-prime + status widget + agent_end cache invalidation\n28e997f feat(pi): foundation for @os-eco/pi-seeds extension\n```\n\n(That's 7 commits if you count the foundation. Confirm exact list at execution time with `git log --oneline -- extensions/pi`.)\n\n## Commits to preserve (non-pi work in the same range)\n\nOnly one — the release bump:\n\n```\nd270b13 chore: release v0.4.6\n```\n\nStrategy: revert the 7 pi commits; the release bump revert is fine (or hand-edit package.json afterward). Net result: package.json moves to 0.4.7 for the rollback release.\n\n## Strategy: revert, not reset\n\nSame rationale as mulch-18e4. `git revert` preserves history; the existing seeds (seeds-7164 plan, seeds-045e, seeds-02f6) already document the experiment. A revert commit pointing at them is honest engineering record-keeping; `git reset --hard` rewrites history and forces everyone with checkouts to reset.\n\n## Execution sequence\n\n```bash\n# 0. EMPIRICALLY VERIFY whether sd setup pi is broken like mulch's\n#    (the recipe code path is identical so this is mostly a formality)\nmkdir /tmp/seeds-pi-probe && cd /tmp/seeds-pi-probe\nsd init && sd setup pi\ncat .pi/settings.json                        # should show '@os-eco/seeds-cli' bare\npi -p 'list tools' --thinking off --mode text\n# expect: only read/bash/edit/write — confirms extension doesn't load via the recipe\n\n# 1. preflight (from the seeds repo)\ncd ~/Projects/os-eco/seeds\ngit worktree list\ngit remote -v\nsd setup pi --remove                          # clean THIS repo's .pi/\n\n# 2. revert in reverse-chronological order\ngit revert --no-commit cc690ba 8af2100 423404c 593a7ca d698ef2 d83598f 28e997f\n# expect manual conflict resolution in: package.json, src/schemas/*,\n# src/commands/setup.ts, src/commands/onboard.ts (~30 min)\n\n# 3. land it\ngit commit -m 'revert: remove pi-seeds extension experiment\n\nBuilt seeds-pi (28e997f..cc690ba) as a pi-coding-agent integration.\nDecision (2026-05-15, with mulch and canopy in parallel): the CLI plus\nCLAUDE.md onboarding cover the same ground for every agent runtime\nuniformly, without a pi-specific code path. Reverting to consolidate\non that single surface.\n\nRefs: seeds-045e, seeds-02f6 (superseded), <this-seed-id>,\nsibling: mulch-18e4, canopy-<TBD>'\n\n# 4. bump version, publish patch (0.4.6 → 0.4.7), changelog entry, npm publish\n\n# 5. close superseded seeds\nsd close seeds-045e seeds-02f6 --reason 'rolled back pi extension; see <this-seed-id>'\n# also any open children of pl-a1d4 / seeds-7164 plan; verify with:\nsd plan show pl-a1d4\n```\n\n## Gotchas / risks\n\n1. **`sd setup pi --remove` correctness** — the remove path must work; if it has the same bare-vs-npm: mismatch bug as the install path, it may not actually clear `.pi/settings.json` cleanly. Verify on a throwaway directory before running on real projects.\n\n2. **Force-push not required** (this is why we use revert) — confirm worktree list first.\n\n3. **npm 0.4.6 stays published forever.** `npm deprecate @os-eco/seeds-cli@0.4.6 \"Rolled back — use 0.4.7+ (CLI-only integration)\"` adds an install-time warning. No consumers in os-eco pin to it currently (the only known pinner is users themselves).\n\n4. **`<!-- seeds:start -->` onboarding snippets across projects** — any project that ran `sd setup pi` or `sd onboard` has pi-aware prose. Self-updates on next run. Cosmetic.\n\n5. **Conflict resolution** — the 6 pi commits build on each other (foundation → auto-prime + widget → tools → autocomplete → commands → recipe → tests). Reverting in reverse chronological order minimizes conflicts; expect manual work in: package.json (pi manifest + peerDeps), src/schemas/config*.ts, src/commands/setup.ts (piRecipe), src/commands/onboard.ts (PI_PACKAGE_NAME + :pi marker suffix), src/state/ (currentIssueId state if it was added).\n\n6. **The status widget integration may have left state-management code in src/state/ or similar** — confirm the revert removes ALL of it; the widget added `currentIssueId` state which may have plumbing in unexpected places. `git diff 28e997f^..HEAD -- 'src/**' '!extensions/**'` to see the full bleed.\n\n7. **seeds mulch records referencing pi-seeds extension** — if any exist in this repo's `.mulch/expertise/*.jsonl` they describe code that won't exist. Same advice as mulch-18e4: leave them, let anchor-validity decay handle it. Hand-archive only if cosmetics matter.\n\n8. **Sibling rollbacks coordinated** — mulch-18e4 (mulch repo) and canopy-<TBD> (canopy repo, which is a *prevention* not a revert since no code has landed yet). Doing all three in the same week keeps the ecosystem message coherent for downstream users.\n\n## Acceptance\n\n- All 7 pi commits reverted; `git log --oneline -- extensions/pi` shows only the revert.\n- `extensions/pi/` directory deleted; pi-related tests deleted.\n- `package.json` has no `pi` manifest field and no pi peerDependencies.\n- `src/schemas/config*.ts` has no `PiConfig` type; snapshots updated.\n- `src/commands/setup.ts` has no `piRecipe`; `sd setup --list` (if it exists) no longer shows pi.\n- `src/commands/onboard.ts` has no `PI_PACKAGE_NAME`, no `:pi` marker suffix, no pi-aware snippet variant.\n- `currentIssueId` state / widget cache machinery (whatever the d83598f and 423404c commits added) is fully removed.\n- `bun test && bun run lint && bun run typecheck` all clean.\n- 0.4.7 published; 0.4.6 deprecated on npm.\n- seeds-045e, seeds-02f6 closed with cross-reference; any pl-a1d4 plan children closed similarly.\n\n## Out of scope (file separately if needed)\n\n- README update describing the CLAUDE.md-only integration pattern.\n- Pruning any pi-referencing mulch records — let natural decay handle.","labels":["pi-extension","rollback","audit-followup"],"closedAt":"2026-05-27T02:58:58.271Z","closeReason":"Reverted pi-seeds extension: extensions/pi/ removed, setup command removed, pi config schema removed, onboard restored to standard-only variant"}
{"id":"seeds-6c8e","title":"Auto-compact issues.jsonl on sd sync (dedup before commit)","status":"open","type":"task","priority":2,"createdAt":"2026-05-17T20:16:49.053Z","updatedAt":"2026-05-17T20:17:12.147Z","description":"## Problem\n\nAfter a `merge=union` pull, `.seeds/issues.jsonl` can carry duplicate lines for the same issue id until something dedups them. Today nothing does — the file just keeps the duplicates committed, and dedup happens only on read. Over time the file bloats and diffs get noisy.\n\n`sd doctor --fix duplicate-ids` already does the right cleanup (`src/commands/doctor.ts:538`, calls `fixDuplicates`) but it's a manual command — most users never run it.\n\n## Proposal\n\n`sd sync` should compact (dedup-write) before staging. Pseudocode in `src/commands/sync.ts` right before the `git add`:\n\n```ts\n// dedup any duplicate-id lines accumulated since the last sync\nfor (const file of [ISSUES_FILE, TEMPLATES_FILE, PLANS_FILE]) {\n  const items = readJsonl(...); // already dedups via deduplicateById\n  writeJsonl(..., items);        // rewrite the file with one line per id\n}\n```\n\nThis reuses existing store helpers — `loadIssues` already returns the deduped set, `writeIssues` already does atomic rewrite. The cost is one re-read + re-write per sync, which is cheap.\n\n## Acceptance\n\n- After a `merge=union` merge that produces duplicate lines, `sd sync` commits a file with one line per id (verified with `wc -l` and `jq -s 'group_by(.id) | map(length)'`).\n- If there are no duplicates, the sync is a no-op for the dedup pass (the diff is unchanged).\n- `sd sync --dry-run` reports whether compaction would change anything.\n- Existing tests pass; new test covers \"two branches modify different issues, merge, sync, file has one line per id.\"\n\n## Related\n\n- Surfaced in warren PR 15 post-mortem (warren repo): merge worked but the question of \"what happens when union accumulates duplicates\" came up. Mulch doesn't have this problem because it shards by domain into separate files; seeds intentionally stays monolithic, which is fine — but auto-compaction closes the only real downside.\n- See also the field-level merge follow-up (filed separately) — that one handles the *semantic* case where two branches touched the same issue.","blocks":["seeds-a493"]}
{"id":"seeds-a493","title":"Field-level merge when dedup finds duplicate ids (don't lose concurrent edits)","status":"open","type":"feature","priority":2,"createdAt":"2026-05-17T20:17:08.164Z","updatedAt":"2026-05-17T20:17:12.147Z","description":"## Problem\n\nToday's dedup is **last-line-wins for the whole record** (`src/store.ts:82–88`):\n\n```ts\nfunction deduplicateById<T extends { id: string }>(items: T[]): T[] {\n  const map = new Map<string, T>();\n  for (const item of items) map.set(item.id, item); // last occurrence wins\n  return Array.from(map.values());\n}\n```\n\nCombined with `merge=union`, this silently loses edits when two branches touch the same issue:\n\n- Branch A: `sd close warren-0c8e` → writes `{...,status:\\\"closed\\\",closedAt:\\\"...\\\"}`\n- Branch B: `sd update warren-0c8e --description \\\"new context\\\"` → writes `{...,status:\\\"open\\\",description:\\\"new context\\\"}`\n- Merge with `merge=union`: both lines land in the file.\n- Dedup-on-read: whichever line is physically later wins. The other side's edits are gone.\n\nOrder in a union-merged file isn't deterministic across operating systems / git versions, so which side wins is effectively a coin flip.\n\n## Proposal\n\nWhen dedup finds duplicates for the same id, **merge fields per-record** instead of picking one whole record. Policy:\n\n- Per-field: take the value from the record with the newer `updatedAt`.\n- Monotonic fields (`closedAt`, `labels`, `blockedBy`, `blocks`): union/most-recent wins as appropriate.\n  - `closedAt`: if any duplicate has it set, keep it (status closed is sticky).\n  - `labels`, `blockedBy`, `blocks`: union the sets.\n- `updatedAt` on the merged record: max of all duplicates.\n\nThis is local logic — no git custom merge driver needed (custom drivers require per-user `git config` and don't ship via `.gitattributes`, so they're a non-starter for portability).\n\nImplementation lives next to existing dedup:\n\n- `src/store.ts`: new `mergeById<T>(items, mergePolicy)` replacing the `last wins` line.\n- `src/commands/doctor.ts:732` (`fixDuplicates`): use the new merger so `sd doctor --fix` also recovers full state.\n- Issues, plans, templates each declare their merge policy (most fields are \\\"newer updatedAt wins\\\"; only a few need special handling).\n\n## Acceptance\n\n- Two branches modifying disjoint fields of the same issue (e.g., A closes it, B updates description) produce a merged record containing **both** changes after `git merge` + `sd sync`.\n- `sd doctor --fix duplicate-ids` on a file with conflicting duplicates produces the same merged result.\n- Existing tests pass; new tests cover the close-vs-update, label-union, blockedBy-union cases.\n- No behavior change for the common case (no duplicates).\n\n## Related\n\n- seeds-6c8e: auto-compact on sd sync (this issue depends on it — compaction is where the merge runs).\n- Surfaced via warren PR 15 conflict investigation: today this is a theoretical data-loss bug because most agent PRs touch disjoint issues, but it'll bite as parallel agent work scales (warren's whole point is many agents in flight at once).","blockedBy":["seeds-6c8e"]}
{"id":"seeds-5583","title":"extend 'sd plan submit' to accept existing-seed children (warren §11.Q upstream blocker)","status":"closed","type":"task","priority":2,"createdAt":"2026-05-18T23:20:10.234Z","updatedAt":"2026-05-18T23:30:01.103Z","description":"Cross-repo upstream blocker for warren SPEC §11.Q (warren-a4b7, pl-5310 step 4). Originally tracked as warren-d519 in warren's .seeds/; refiled here because the work lives in this repo. See cross-repo-design-lock-seed pattern (mx-bffcf1).\n\n**Upstream change required.** Today 'sd plan submit <seed-id> --plan <file>' spawns NEW child seeds. There is no way to make existing seeds children of a new plan. Warren SPEC §11.Q's synthesis step needs that capability: given N attached seeds_issue ids on a Plot, mint a parent seed + plan whose children ARE those N existing seeds, no new spawns.\n\n**Contract assumed by §11.Q.**\n- New optional field in the plan JSON's 'steps' array (or sibling top-level field — seeds-cli's call): existing_seeds: [\"warren-X\", ...]\n- Children become refs, not new spawns\n- Validation: every named seed must exist in the same .seeds/issues.jsonl; cross-repo seed ids rejected; closed seeds rejected at plan-submit time\n- Output plan row in plans.jsonl byte-compatible with today's plans (same children: [seedId, ...] projection so warren's plan-run coordinator needs zero changes)\n\n**Once shipped upstream:** warren double-pins per the burrow-cli rule (warren CLAUDE.md 'Relationship to burrow'): @os-eco/seeds-cli in package.json + bun.lock + Dockerfile global install layer, all three locations bumped in lockstep. Then warren-a4b7 sub-plan steps (synthesis module, POST /plot-plan-runs, UI button, acceptance scenario 30) can proceed.\n\nDownstream blocks (in warren repo): warren-a4b7, warren-3c8a, warren-99b2, warren-bce0, warren-af97.\nOriginally filed: 2026-05-18, warren design-lock commit (warren-d519, now closed as moved).","closedAt":"2026-05-18T23:30:01.103Z","closeReason":"step.title is now optional when step.existing_seed is set; synthesis-style plans can submit {existing_seed: <id>} per child with no titles. Children projection in plans.jsonl is byte-compatible with prior adoption submits."}
{"id":"seeds-19a9","title":"outputJson truncates large payloads when stdout is piped (Bun buffered I/O)","status":"closed","type":"bug","priority":2,"createdAt":"2026-05-25T23:18:44.177Z","updatedAt":"2026-05-25T23:28:34.106Z","description":"## Problem\n\n`outputJson` in `src/output.ts:17` uses `console.log(JSON.stringify(data, null, 2))`. In Bun, `console.log` uses buffered I/O. When stdout is a pipe (not a TTY) — the typical case for agent consumption (`sd list --json | jq ...`) — large writes (>64KB) may not fully flush before the process exits.\n\nThe entry point uses `process.exitCode` (not `process.exit()`), which should let the event loop drain, but Bun's pipe buffering can still cause truncation.\n\n## Affected code\n\n**Primary:** `src/output.ts:17` — `outputJson()`, called from ~66 sites across 24 command files.\n\n**Secondary:** 4 direct `console.log(JSON.stringify(...))` calls that bypass `outputJson`:\n- `src/index.ts:182` (error handler in main catch block)\n- `src/commands/config.ts:125,128,148` (config schema/show)\n\n## Proposed fix\n\nMake `outputJson` async and use `Bun.write(Bun.stdout, ...)` which awaits the full write:\n\n```typescript\nexport async function outputJson(data: unknown): Promise<void> {\n    const str = JSON.stringify(data, null, 2) + \"\\n\";\n    await Bun.write(Bun.stdout, str);\n}\n```\n\nAll callers are already in async Commander action handlers and the entry point does `await program.parseAsync()`, so adding `await` at each call site is mechanical.\n\n## Implementation notes\n\n1. Change `outputJson` signature to `async` returning `Promise<void>`\n2. Replace `console.log` body with `await Bun.write(Bun.stdout, str + \"\\n\")`\n3. Add `await` at all ~66 call sites (grep for `outputJson(` in `src/commands/`)\n4. Fix the 4 direct `console.log(JSON.stringify(...))` calls to also use `Bun.write`\n5. TypeScript will catch any missed `await` sites since the return type changes to `Promise<void>`\n\n## Alternative considered\n\nAdding a flush guard at the end of `main()`:\n```typescript\nawait Bun.write(Bun.stdout, \"\");\n```\nThis is less invasive but not guaranteed to work — a zero-length write may not actually flush the buffer. The async `outputJson` approach is more reliable.\n\n## Reproduction\n\nPipe a command that produces large JSON output (e.g., a project with many issues):\n```bash\nsd list --json | cat  # may truncate vs sd list --json (direct TTY)\n```","closedAt":"2026-05-25T23:28:34.106Z","closeReason":"Fixed: outputJson now uses async Bun.write(Bun.stdout, ...) instead of console.log to prevent truncation on piped stdout"}
{"id":"seeds-a2de","title":"sd plan edit: in-place plan field editing","status":"open","type":"feature","priority":1,"createdAt":"2026-05-26T18:33:13.828Z","updatedAt":"2026-05-26T18:34:14.072Z","description":"Add an 'sd plan edit' subcommand that allows targeted, field-level edits to plans without requiring full re-submission via --overwrite. Covers: plan name, section text, and step metadata (title, priority, type) with child-seed propagation.","plan_id":"pl-dee8","blockedBy":["seeds-9b12","seeds-21f2","seeds-64cf","seeds-d457"]}
{"id":"seeds-9b12","title":"Register sd plan edit subcommand and implement --name editing","status":"closed","type":"task","priority":1,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-dee8.\n\nParent seed: seeds-a2de — sd plan edit: in-place plan field editing\nPlan template: feature\nPlan approach: Add a new sd plan edit subcommand with field-level flags: --name for the plan label, --section <name> <text> for replacing section content, and --step <i> combined with --title/--priority/--type for step metadata. The implementation…\n\nRun `sd plan show pl-dee8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T18:34:14.072Z","updatedAt":"2026-05-26T18:54:35.365Z","plan_id":"pl-dee8","blocks":["seeds-21f2","seeds-64cf","seeds-a2de"],"closedAt":"2026-05-26T18:54:35.365Z","closeReason":"Implemented sd plan edit subcommand with --name; tests + lint + typecheck all green"}
{"id":"seeds-21f2","title":"Implement --section editing with approach backref refresh","status":"closed","type":"task","priority":1,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-dee8.\n\nParent seed: seeds-a2de — sd plan edit: in-place plan field editing\nPlan template: feature\nPlan approach: Add a new sd plan edit subcommand with field-level flags: --name for the plan label, --section <name> <text> for replacing section content, and --step <i> combined with --title/--priority/--type for step metadata. The implementation…\n\nRun `sd plan show pl-dee8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T18:34:14.072Z","updatedAt":"2026-05-26T19:19:57.608Z","plan_id":"pl-dee8","blockedBy":["seeds-9b12"],"blocks":["seeds-a2de"],"closedAt":"2026-05-26T19:19:57.608Z","closeReason":"Implemented --section editing with approach backref refresh"}
{"id":"seeds-64cf","title":"Implement --step metadata editing with child-seed propagation","status":"open","type":"task","priority":1,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-dee8.\n\nParent seed: seeds-a2de — sd plan edit: in-place plan field editing\nPlan template: feature\nPlan approach: Add a new sd plan edit subcommand with field-level flags: --name for the plan label, --section <name> <text> for replacing section content, and --step <i> combined with --title/--priority/--type for step metadata. The implementation…\n\nRun `sd plan show pl-dee8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T18:34:14.072Z","updatedAt":"2026-05-26T18:34:14.072Z","plan_id":"pl-dee8","blockedBy":["seeds-9b12"],"blocks":["seeds-a2de"]}
{"id":"seeds-d457","title":"Update docs: CLAUDE.md, README, PLAN_SPEC, prime, onboard","status":"open","type":"task","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-dee8.\n\nParent seed: seeds-a2de — sd plan edit: in-place plan field editing\nPlan template: feature\nPlan approach: Add a new sd plan edit subcommand with field-level flags: --name for the plan label, --section <name> <text> for replacing section content, and --step <i> combined with --title/--priority/--type for step metadata. The implementation…\n\nRun `sd plan show pl-dee8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T18:34:14.072Z","updatedAt":"2026-05-26T18:34:14.072Z","plan_id":"pl-dee8","blocks":["seeds-a2de"]}
{"id":"seeds-a3bd","title":"Optimize test suite: eliminate subprocess spawning bottleneck","status":"open","type":"feature","priority":1,"createdAt":"2026-05-26T19:09:16.594Z","updatedAt":"2026-05-26T19:11:06.266Z","description":"The test suite takes ~99s for 825 tests. Root cause: ~22 command test files shell out via Bun.spawn for every CLI invocation (~2,000-3,000 spawns total). Unit tests that import functions directly run at 130+ tests/sec vs ~5 tests/sec for subprocess tests. Refactor command implementations to be callable as functions, convert tests to direct-import, keep thin CLI smoke tests. Target: <15s total suite time.","plan_id":"pl-86aa","blockedBy":["seeds-4770","seeds-efd7","seeds-807e","seeds-c709","seeds-d1ca","seeds-f78d"]}
{"id":"seeds-4770","title":"Build in-process test harness for capturing command output","status":"closed","type":"task","priority":2,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-86aa.\n\nParent seed: seeds-a3bd — Optimize test suite: eliminate subprocess spawning bottleneck\nPlan template: feature\nPlan approach: Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning…\n\nRun `sd plan show pl-86aa` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T20:21:54.385Z","plan_id":"pl-86aa","blocks":["seeds-efd7","seeds-807e","seeds-c709","seeds-d1ca","seeds-f78d","seeds-a3bd"],"closedAt":"2026-05-26T20:21:54.385Z","closeReason":"Built src/test-harness.ts with runCli/runCliJson (4-channel capture, chdir+exitCode isolation, chalk forced off, run()/register() dispatch). 21 harness tests pass; full suite 875 pass."}
{"id":"seeds-efd7","title":"Convert plan.test.ts to in-process (38s → ~2-3s target)","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-86aa.\n\nParent seed: seeds-a3bd — Optimize test suite: eliminate subprocess spawning bottleneck\nPlan template: feature\nPlan approach: Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning…\n\nRun `sd plan show pl-86aa` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T20:43:31.554Z","plan_id":"pl-86aa","blockedBy":["seeds-4770"],"blocks":["seeds-a3bd"],"closedAt":"2026-05-26T20:43:31.554Z","closeReason":"Converted plan.test.ts to in-process harness; 127s → 9s. Fixed harness to propagate exitOverride to subcommands so --help no longer process.exit(0)s and kills bun:test."}
{"id":"seeds-807e","title":"Convert label, create, unblock, dep, list tests to in-process (~27s → ~2s target)","status":"closed","type":"task","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-86aa.\n\nParent seed: seeds-a3bd — Optimize test suite: eliminate subprocess spawning bottleneck\nPlan template: feature\nPlan approach: Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning…\n\nRun `sd plan show pl-86aa` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T20:51:25.493Z","plan_id":"pl-86aa","blockedBy":["seeds-4770"],"blocks":["seeds-a3bd"],"closedAt":"2026-05-26T20:51:25.493Z","closeReason":"Converted label/create/unblock/dep/list tests to in-process harness. 75s → 0.44s for these 5 files (115 tests). Full suite 875 tests pass, lint+typecheck clean."}
{"id":"seeds-c709","title":"Convert remaining command tests to in-process (~30s → ~2s target)","status":"closed","type":"task","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-86aa.\n\nParent seed: seeds-a3bd — Optimize test suite: eliminate subprocess spawning bottleneck\nPlan template: feature\nPlan approach: Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning…\n\nRun `sd plan show pl-86aa` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T21:00:53.431Z","plan_id":"pl-86aa","blockedBy":["seeds-4770"],"blocks":["seeds-a3bd"],"closedAt":"2026-05-26T21:00:53.431Z","closeReason":"Converted show, ready, update, tpl, search, prime, onboard, format, doctor, config, setup, init test files (plus the lone sd-CLI spawn in sync.test.ts) to the in-process harness. Kept completions.test.ts subprocess (full-program enumeration), the 'sd --help' setup test (top-level commander), the worktree-branch sync test (seedsDir-undefined branch), and the stdin/PATH-env subprocess calls in plan tests; suggestions.test.ts and timing.test.ts remain subprocess as top-level CLI smoke. Full suite: 875 tests pass in ~16.9s (down from ~99s). Lint + typecheck clean."}
{"id":"seeds-d1ca","title":"Add CLI smoke test file for subprocess-level coverage","status":"closed","type":"task","priority":2,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-86aa.\n\nParent seed: seeds-a3bd — Optimize test suite: eliminate subprocess spawning bottleneck\nPlan template: feature\nPlan approach: Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning…\n\nRun `sd plan show pl-86aa` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T21:05:33.755Z","plan_id":"pl-86aa","blockedBy":["seeds-4770"],"blocks":["seeds-a3bd"],"closedAt":"2026-05-26T21:05:33.755Z","closeReason":"Added src/cli-smoke.test.ts: 7 subprocess-level smoke tests covering commander boot, --help formatter, --version (+ --json), unknown command/typo suggester, main().catch JSON wrapper, and end-to-end init→create→list. Full suite 882 pass / 0 fail in ~18s."}
{"id":"seeds-f78d","title":"Validate full suite performance and clean up","status":"closed","type":"task","priority":2,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-86aa.\n\nParent seed: seeds-a3bd — Optimize test suite: eliminate subprocess spawning bottleneck\nPlan template: feature\nPlan approach: Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning…\n\nRun `sd plan show pl-86aa` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T21:11:36.225Z","plan_id":"pl-86aa","blockedBy":["seeds-4770"],"blocks":["seeds-a3bd"],"closedAt":"2026-05-26T21:11:36.225Z","closeReason":"Validated: 882 tests pass in ~18s (5.5x improvement from ~99s). Lint + typecheck green. CHANGELOG entry added."}
{"id":"seeds-576c","title":"Add labels field to plan step definitions for child seed labeling at spawn time","status":"open","type":"feature","priority":1,"createdAt":"2026-05-26T19:22:30.916Z","updatedAt":"2026-05-26T19:23:11.958Z","description":"Warren patrol agents (nightwatch, bugwatch) need to tag spawned child seeds with agent-name labels. Today SubmittedStep has no labels field, so the only way is post-hoc sd label add calls. Add an optional string[] labels field to SubmittedStep and STEP_SCHEMA, normalize labels at spawn time (lowercase/trim/dedup), merge labels additively on adoption, and validate non-empty strings.","labels":["plan","warren"],"plan_id":"pl-e5a8","blockedBy":["seeds-7561","seeds-745e","seeds-bac9","seeds-9f86","seeds-2848"]}
{"id":"seeds-7561","title":"Add labels to STEP_SCHEMA and SubmittedStep interface","status":"closed","type":"task","priority":1,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-e5a8.\n\nParent seed: seeds-576c — Add labels field to plan step definitions for child seed labeling at spawn time\nPlan template: feature\nPlan approach: Add an optional labels: string[] field to SubmittedStep and the STEP_SCHEMA in plan-schema.ts. Reuse the same normalization logic as sd label add (lowercase, trim, dedup). In the fresh-spawn path of runSubmit, set issue.labels from the…\n\nRun `sd plan show pl-e5a8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:23:11.958Z","updatedAt":"2026-05-26T21:16:52.708Z","plan_id":"pl-e5a8","blocks":["seeds-745e","seeds-bac9","seeds-9f86","seeds-576c"],"closedAt":"2026-05-26T21:16:52.708Z","closeReason":"Added optional labels: string[] to STEP_SCHEMA and SubmittedStep interface. Schema rejects empty/whitespace items (minLength:1 + pattern \\\\S). No wiring yet — fresh-spawn (seeds-745e) and adoption (seeds-bac9) paths land in subsequent plan steps."}
{"id":"seeds-745e","title":"Wire labels into fresh-spawn path of runSubmit","status":"closed","type":"task","priority":1,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-e5a8.\n\nParent seed: seeds-576c — Add labels field to plan step definitions for child seed labeling at spawn time\nPlan template: feature\nPlan approach: Add an optional labels: string[] field to SubmittedStep and the STEP_SCHEMA in plan-schema.ts. Reuse the same normalization logic as sd label add (lowercase, trim, dedup). In the fresh-spawn path of runSubmit, set issue.labels from the…\n\nRun `sd plan show pl-e5a8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:23:11.958Z","updatedAt":"2026-05-26T21:21:52.095Z","plan_id":"pl-e5a8","blockedBy":["seeds-7561"],"blocks":["seeds-9f86","seeds-576c"],"closedAt":"2026-05-26T21:21:52.095Z","closeReason":"Wired step.labels through both runSubmit fresh-spawn branches (initial + overwrite); normalizeLabels exported from label.ts for reuse by seeds-bac9. All 882 tests pass."}
{"id":"seeds-bac9","title":"Wire labels into adoption paths (submit-time and runAdopt)","status":"closed","type":"task","priority":1,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-e5a8.\n\nParent seed: seeds-576c — Add labels field to plan step definitions for child seed labeling at spawn time\nPlan template: feature\nPlan approach: Add an optional labels: string[] field to SubmittedStep and the STEP_SCHEMA in plan-schema.ts. Reuse the same normalization logic as sd label add (lowercase, trim, dedup). In the fresh-spawn path of runSubmit, set issue.labels from the…\n\nRun `sd plan show pl-e5a8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:23:11.958Z","updatedAt":"2026-05-26T21:25:33.242Z","plan_id":"pl-e5a8","blockedBy":["seeds-7561"],"blocks":["seeds-9f86","seeds-576c"],"closedAt":"2026-05-26T21:25:33.242Z","closeReason":"Wired step.labels into all three adoption paths via mergeAdoptedLabels() helper in plan.ts: (1) runSubmit fresh-submit adoption branch, (2) applyOverwrite usedOldIds + adoptedExternalIds branches, (3) runAdopt post-submit when --step <i> is given (reads plan.sections.steps[i].labels). Merge is additive (Set union with normalizeLabels) so user-added labels survive. All 882 tests pass; lint + typecheck clean."}
{"id":"seeds-9f86","title":"Add tests for step labels across all paths","status":"closed","type":"task","priority":1,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-e5a8.\n\nParent seed: seeds-576c — Add labels field to plan step definitions for child seed labeling at spawn time\nPlan template: feature\nPlan approach: Add an optional labels: string[] field to SubmittedStep and the STEP_SCHEMA in plan-schema.ts. Reuse the same normalization logic as sd label add (lowercase, trim, dedup). In the fresh-spawn path of runSubmit, set issue.labels from the…\n\nRun `sd plan show pl-e5a8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:23:11.958Z","updatedAt":"2026-05-26T21:31:40.867Z","plan_id":"pl-e5a8","blockedBy":["seeds-7561","seeds-745e","seeds-bac9"],"blocks":["seeds-2848","seeds-576c"],"closedAt":"2026-05-26T21:31:40.867Z","closeReason":"Added 16 tests covering all step.labels paths (schema validation, fresh-spawn, submit-time adoption, --overwrite matched/spawn/external, runAdopt --step). All 228 plan tests pass; full suite 899 pass; lint+typecheck clean."}
{"id":"seeds-2848","title":"Update CLAUDE.md and sd plan prompt output to document labels field","status":"closed","type":"task","priority":2,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-e5a8.\n\nParent seed: seeds-576c — Add labels field to plan step definitions for child seed labeling at spawn time\nPlan template: feature\nPlan approach: Add an optional labels: string[] field to SubmittedStep and the STEP_SCHEMA in plan-schema.ts. Reuse the same normalization logic as sd label add (lowercase, trim, dedup). In the fresh-spawn path of runSubmit, set issue.labels from the…\n\nRun `sd plan show pl-e5a8` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-26T19:23:11.958Z","updatedAt":"2026-05-26T21:36:05.415Z","plan_id":"pl-e5a8","blockedBy":["seeds-9f86"],"blocks":["seeds-576c"],"closedAt":"2026-05-26T21:36:05.415Z","closeReason":"Documented step.labels across plan prompt instructions, submit help text example, README, PLAN_SPEC, sd prime Planning notes, CLAUDE.md, and CHANGELOG."}
{"id":"seeds-76cf","title":"Accept --label as alias for --labels on sd create","status":"open","type":"bug","priority":2,"createdAt":"2026-05-27T00:19:18.732Z","updatedAt":"2026-05-27T00:19:18.732Z","description":"Agents (and humans) frequently guess --label instead of --labels on sd create, causing a failure + retry cycle. Example: sd create --label \"foo,bar\" fails with 'unknown option --label (Did you mean --labels?)'. Since sd list/ready/search already use --label for filtering, the inconsistency is confusing. Fix: add --label as a hidden alias for --labels on sd create so both forms work."}
{"id":"seeds-c1db","title":"nightwatch patrol: 2026-05-27","status":"open","type":"task","priority":3,"createdAt":"2026-05-27T11:04:35.551Z","updatedAt":"2026-05-27T11:05:56.380Z","labels":["patrol","nightwatch"],"plan_id":"pl-09b0","blockedBy":["seeds-d6dd","seeds-92ef","seeds-c273","seeds-9d2a","seeds-7000","seeds-fd19","seeds-206f","seeds-b1d2","seeds-ce1c"]}
{"id":"seeds-d6dd","title":"Fix stripAnsi regex in src/format.ts to include the ESC byte","status":"closed","type":"bug","priority":1,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:09:25.724Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:09:25.724Z","closeReason":"Replaced literal ESC byte in ANSI_REGEX with explicit \\x1b escape for source readability; added src/format.test.ts coverage proving stripAnsi removes real ANSI sequences and leaves bracket-only text untouched."}
{"id":"seeds-92ef","title":"Annotate sd search --json with plan_status/plan_children like list/ready","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:12:23.761Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:12:23.761Z","closeReason":"sd search --json now annotates seeds with plan_status/plan_children, matching list/ready."}
{"id":"seeds-c273","title":"Route printWarning to stderr and honor _quiet","status":"closed","type":"bug","priority":2,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:15:12.907Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:15:12.907Z","closeReason":"printWarning now writes to stderr and honors -q/--quiet; added output.test.ts coverage and updated sync worktree-guard test."}
{"id":"seeds-9d2a","title":"Validate --limit input in list/ready/search","status":"closed","type":"bug","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:19:01.553Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:19:01.553Z","closeReason":"Validated --limit input in list/ready/search via shared parseLimitFlag helper."}
{"id":"seeds-7000","title":"Reject empty/whitespace --title on sd update","status":"closed","type":"bug","priority":3,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:21:26.362Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:21:26.362Z","closeReason":"Empty/whitespace --title now rejected with a clear error; commander wrapper also passes empty strings through so they're caught instead of silently dropped."}
{"id":"seeds-fd19","title":"Reject self-block in sd block and sd dep add","status":"closed","type":"bug","priority":3,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:24:12.910Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:24:12.910Z","closeReason":"Implemented self-block rejection in sd block and sd dep add with tests"}
{"id":"seeds-206f","title":"Surface onboard outdated-branch silent failure when replaceMarkerSection returns falsy","status":"closed","type":"bug","priority":3,"plan_step_index":6,"description":"<!-- seeds:plan-backref:start -->\nStep 7 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:27:34.496Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:27:34.496Z","closeReason":"Surface failure when replaceMarkerSection returns null: print error (or --json {success:false, action:failed, error}) and exit 1 instead of silently no-op'ing. Also strengthened replaceMarkerSection to return null when END_MARKER appears before START_MARKER (out-of-order markers), making the defensive branch reachable + testable. Added 3 tests (1 marker unit, 2 onboard end-to-end)."}
{"id":"seeds-b1d2","title":"Reconcile CLAUDE.md lock-constants doc with src/types.ts values","status":"closed","type":"task","priority":3,"plan_step_index":7,"description":"<!-- seeds:plan-backref:start -->\nStep 8 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:29:49.115Z","plan_id":"pl-09b0","blocks":["seeds-ce1c","seeds-c1db"],"closedAt":"2026-05-27T11:29:49.115Z","closeReason":"Updated CLAUDE.md lock-constants line to match src/types.ts (100ms retry, 30s timeout)"}
{"id":"seeds-ce1c","title":"Release: run /release per .claude/commands/release.md","status":"closed","type":"task","priority":3,"plan_step_index":8,"description":"<!-- seeds:plan-backref:start -->\nStep 9 of plan pl-09b0.\n\nParent seed: seeds-c1db — nightwatch patrol: 2026-05-27\nPlan template: refactor\nPlan approach: Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON,…\n\nRun `sd plan show pl-09b0` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:32:24.317Z","plan_id":"pl-09b0","blockedBy":["seeds-d6dd","seeds-92ef","seeds-c273","seeds-9d2a","seeds-7000","seeds-fd19","seeds-206f","seeds-b1d2"],"blocks":["seeds-c1db"],"closedAt":"2026-05-27T11:32:24.317Z","closeReason":"Released v0.5.2"}
{"id":"seeds-3222","title":"Add hidden --label alias to sd create (commander + run parser)","status":"closed","type":"task","priority":2,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-f4dc.\n\nParent seed: seeds-76cf — Accept --label as alias for --labels on sd create\nPlan template: bug\nPlan approach: Two-file change, no public API break: (1) In `src/commands/create.ts`, register a hidden `--label <labels>` option using commander's `Option(...).hideHelp(true)` so `--help` output stays clean, and forward `opts.label` into the internal…\n\nRun `sd plan show pl-f4dc` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T16:02:03.480Z","updatedAt":"2026-05-27T16:04:27.848Z","plan_id":"pl-f4dc","blocks":["seeds-354a","seeds-76cf"],"closedAt":"2026-05-27T16:04:27.848Z","closeReason":"Hidden --label alias added to sd create; --labels wins on conflict; --help omits alias. Manually verified; automated tests follow in seeds-354a."}
{"id":"seeds-354a","title":"Add test coverage for --label alias on sd create","status":"closed","type":"task","priority":2,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-f4dc.\n\nParent seed: seeds-76cf — Accept --label as alias for --labels on sd create\nPlan template: bug\nPlan approach: Two-file change, no public API break: (1) In `src/commands/create.ts`, register a hidden `--label <labels>` option using commander's `Option(...).hideHelp(true)` so `--help` output stays clean, and forward `opts.label` into the internal…\n\nRun `sd plan show pl-f4dc` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T16:02:03.480Z","updatedAt":"2026-05-27T16:08:15.933Z","plan_id":"pl-f4dc","blockedBy":["seeds-3222"],"blocks":["seeds-a1e6","seeds-76cf"],"closedAt":"2026-05-27T16:08:15.933Z","closeReason":"Added test coverage for --label alias on sd create in create.test.ts (alias vs canonical parity, normalization parity, --labels wins) and a hidden-help subprocess assertion in cli-smoke.test.ts."}
{"id":"seeds-678b","title":"Note --label alias under Unreleased in CHANGELOG.md","status":"closed","type":"task","priority":3,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-f4dc.\n\nParent seed: seeds-76cf — Accept --label as alias for --labels on sd create\nPlan template: bug\nPlan approach: Two-file change, no public API break: (1) In `src/commands/create.ts`, register a hidden `--label <labels>` option using commander's `Option(...).hideHelp(true)` so `--help` output stays clean, and forward `opts.label` into the internal…\n\nRun `sd plan show pl-f4dc` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T16:02:03.480Z","updatedAt":"2026-05-27T16:11:05.354Z","plan_id":"pl-f4dc","blocks":["seeds-a1e6","seeds-76cf"],"closedAt":"2026-05-27T16:11:05.354Z","closeReason":"CHANGELOG entry added under Unreleased"}
{"id":"seeds-a1e6","title":"Release: run /release per .claude/commands/release.md","status":"closed","type":"task","priority":2,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-f4dc.\n\nParent seed: seeds-76cf — Accept --label as alias for --labels on sd create\nPlan template: bug\nPlan approach: Two-file change, no public API break: (1) In `src/commands/create.ts`, register a hidden `--label <labels>` option using commander's `Option(...).hideHelp(true)` so `--help` output stays clean, and forward `opts.label` into the internal…\n\nRun `sd plan show pl-f4dc` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-27T16:02:03.480Z","updatedAt":"2026-05-27T16:14:07.297Z","plan_id":"pl-f4dc","blockedBy":["seeds-354a","seeds-678b"],"blocks":["seeds-76cf"],"closedAt":"2026-05-27T16:14:07.297Z"}
{"id":"seeds-464f","title":"nightwatch patrol: 2026-05-28","status":"open","type":"task","priority":3,"createdAt":"2026-05-28T11:05:40.569Z","updatedAt":"2026-05-28T11:06:53.834Z","labels":["patrol","nightwatch"],"plan_id":"pl-ca00","blockedBy":["seeds-455c","seeds-5f5c","seeds-a665","seeds-c9cc","seeds-8aa5","seeds-545e","seeds-6ac8"]}
{"id":"seeds-455c","title":"Extract duplicated issueJsonWithPlan helper into plan-context.ts","status":"closed","type":"task","priority":3,"plan_step_index":0,"description":"<!-- seeds:plan-backref:start -->\nStep 1 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:10:00.860Z","plan_id":"pl-ca00","blocks":["seeds-6ac8","seeds-464f"],"closedAt":"2026-05-28T11:10:00.860Z","closeReason":"Extracted issueJsonWithPlan into plan-context.ts; list/ready/search now share the helper."}
{"id":"seeds-5f5c","title":"Annotate sd blocked --json with plan_status/plan_children for parity with list/ready/search","status":"closed","type":"task","priority":3,"plan_step_index":1,"description":"<!-- seeds:plan-backref:start -->\nStep 2 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:12:48.874Z","plan_id":"pl-ca00","blocks":["seeds-6ac8","seeds-464f"],"closedAt":"2026-05-28T11:12:48.874Z","closeReason":"Annotated sd blocked --json with plan_status/plan_children via shared issueJsonWithPlan helper; added parity test."}
{"id":"seeds-a665","title":"Use VERSION constant from src/version.ts in sd upgrade instead of re-reading package.json","status":"closed","type":"task","priority":3,"plan_step_index":2,"description":"<!-- seeds:plan-backref:start -->\nStep 3 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:14:46.857Z","plan_id":"pl-ca00","blocks":["seeds-6ac8","seeds-464f"],"closedAt":"2026-05-28T11:14:46.857Z","closeReason":"Replaced async package.json read with VERSION import from src/version.ts."}
{"id":"seeds-c9cc","title":"Use brand/printSuccess/printWarning helpers in sd upgrade output","status":"closed","type":"task","priority":3,"plan_step_index":3,"description":"<!-- seeds:plan-backref:start -->\nStep 4 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:17:13.824Z","plan_id":"pl-ca00","blocks":["seeds-6ac8","seeds-464f"],"closedAt":"2026-05-28T11:17:13.824Z","closeReason":"Replaced inline chalk usage in src/commands/upgrade.ts with brand/printSuccess/printWarning from src/output.ts."}
{"id":"seeds-8aa5","title":"Emit JSON error on unknown-command branch in src/index.ts when --json is present","status":"open","type":"task","priority":3,"plan_step_index":4,"description":"<!-- seeds:plan-backref:start -->\nStep 5 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:06:53.834Z","plan_id":"pl-ca00","blocks":["seeds-6ac8","seeds-464f"]}
{"id":"seeds-545e","title":"Append missing merge=union .gitattributes lines per-file in sd init","status":"open","type":"bug","priority":3,"plan_step_index":5,"description":"<!-- seeds:plan-backref:start -->\nStep 6 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:06:53.834Z","plan_id":"pl-ca00","blocks":["seeds-6ac8","seeds-464f"]}
{"id":"seeds-6ac8","title":"Release: run /release per .claude/commands/release.md","status":"open","type":"task","priority":3,"plan_step_index":6,"description":"<!-- seeds:plan-backref:start -->\nStep 7 of plan pl-ca00.\n\nParent seed: seeds-464f — nightwatch patrol: 2026-05-28\nPlan template: refactor\nPlan approach: Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or…\n\nRun `sd plan show pl-ca00` for the full plan (context, alternatives, sibling steps, acceptance criteria).\n<!-- seeds:plan-backref:end -->","createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:06:53.834Z","plan_id":"pl-ca00","blockedBy":["seeds-455c","seeds-5f5c","seeds-a665","seeds-c9cc","seeds-8aa5","seeds-545e"],"blocks":["seeds-464f"]}
