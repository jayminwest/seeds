{"id":"pl-c195","seed":"seeds-deea","template":"feature","status":"done","revision":1,"sections":{"context":"Warren V2 (warren/ROADMAP R-01) is making seeds the source of truth for issues — there will be no parallel warren issues table. Warren needs to attach runtime metadata to seeds (assigned agent role, scheduledFor timestamp, trigger source, last-run pointer, queued-but-don't-dispatch flag) and seeds today has fixed fields plus labels, with no structured extension point. Labels can fake `role:refactor-bot` or `trigger:cron` but lose their structure on read, can't carry timestamps, and don't round-trip cleanly. Other ecosystem tools (greenhouse, overstory) will eventually want similar runtime annotations. The persistence rule is `DB only holds runtime state; definitions, expertise, issues all live as git-tracked files in the project repo` — so seeds needs a clean place for warren-specific metadata that travels with the seed and merges cleanly across branches.","approach":"Add a single optional, opaque-to-seeds field `extensions?: Record<string, unknown>` to the `Issue` type. Each consumer (warren, greenhouse, overstory) owns the shape of its own namespaced keys; seeds treats the value as opaque JSON, performs no schema validation, and lets it round-trip through `.seeds/issues.jsonl` like any other field. Surface it on output (`sd show` adds a human-readable Extensions line; JSON modes already serialize the whole issue) and add a single write path on `sd update --extensions <json>` with shallow merge semantics so partial updates from different consumers do not clobber each other. Add a single, opt-in `sd ready --respect-schedule` consumer of two well-known keys (`extensions.queued`, `extensions.scheduledFor`) so warren's cron use case works on day one without changing default semantics for everyone else. This matches the established `new-issue-field-checklist` pattern (mx-c9a8e4): types.ts → output.ts → update.ts → docs, plus doctor.ts when the field has structural constraints.","alternatives":[{"name":"Inline first-class fields on Issue (scheduledFor, role, queued, lastRunId, lastRunAt at the top level)","rejected_because":"Leaks consumer-specific concerns into seeds core, locks the schema to warren's vocabulary, and provides no path for greenhouse/overstory to add their own keys without further first-class fields. Fails the seeds principle of staying minimal and consumer-agnostic."},{"name":"Sidecar file (.seeds/runtime.jsonl) keyed by issue id","rejected_because":"Violates the 'JSONL file IS the database' invariant — introduces a parallel store, a sync step, and the same merge-conflict surface that we already solved for issues.jsonl via `merge=union` + dedup-on-read. Doubles the lock surface for any cross-cutting write."},{"name":"Encode metadata in labels (role:refactor-bot, trigger:cron, sched:2026-05-12T03:00:00Z)","rejected_because":"Lossy round-trip: a label is a flat lowercased string with no structured types. Cannot carry ISO8601 timestamps as parsable values, cannot represent booleans, and consumer code has to re-parse colon-prefixed strings on every read. Conflates runtime state with discoverability tags."},{"name":"Per-consumer top-level keys on Issue (Issue.warren, Issue.greenhouse, Issue.overstory)","rejected_because":"Hardcodes the consumer set into seeds; every new tool requires a seeds release. A single opaque `extensions` map gets the same isolation through namespaced keys without the coupling."}],"steps":[{"title":"Add Issue.extensions field + JSONL round-trip test","type":"feature","priority":1,"blocks":[1,2,3,4,5],"description":"Phase 1 (foundation). Add `extensions?: Record<string, unknown>;` to the `Issue` interface in src/types.ts (alongside other optional fields, before the timestamp block). No changes needed to src/store.ts — append/write/read paths use JSON.stringify/JSON.parse on the whole record, so the field round-trips automatically. Add a focused test in src/store.test.ts that writes an issue with extensions = { role: 'refactor-bot', scheduledFor: '2026-05-12T03:00:00Z', queued: true, nested: { a: 1 } }, reads it back, and asserts deep equality. Add a second test that writes an issue WITHOUT extensions and confirms reading a record with no extensions key returns issue.extensions === undefined (never throws). Acceptance: bun test src/store.test.ts passes; bun run typecheck passes; the field is present on the type and survives a write-read-write cycle."},{"title":"Render extensions in sd show output","type":"feature","priority":2,"blocks":[5],"description":"Phase 2 (display). In src/output.ts formatIssueFull, after the Labels line and before the description, append an `Extensions:` line when issue.extensions is a non-empty object. Format as space-separated key=value pairs at the top level: scalars (string/number/boolean) are stringified directly; objects render as [obj]; arrays as [array(n)]; null as null. Keep it on a single line — this is a summary, not a dump. JSON modes (--format json) already serialize the whole issue, so no change needed in show.ts/list.ts/ready.ts JSON branches; verify that path with a test. Add a test in src/commands/show.test.ts (create if it doesn't exist) that calls run(['<id>']) on an issue with extensions and asserts the rendered string contains 'Extensions:' and the expected key=value summary. Watch out for the plain-format path in show.ts — it strips ANSI, so the Extensions line must work after stripAnsi as well."},{"title":"Add sd update --extensions <json> with shallow merge semantics","type":"feature","priority":1,"blocks":[5],"description":"Phase 3 (write surface). In src/commands/update.ts: register `--extensions <json>` (and matching commander option). Parse the value with JSON.parse; require it to be a plain object (not array, not null, not primitive) — throw a clear error otherwise. Merge top-level keys into the existing issue.extensions with spread: `patch.extensions = { ...(issue.extensions ?? {}), ...parsed }`. Do NOT deep-merge nested objects — V1 is shallow on purpose; document this. Also accept `--clear-extensions` boolean to set extensions to undefined (drops the field). The merge happens inside the existing `inner` closure under the issues.jsonl lock; no plan-lifecycle interaction. Add tests in src/commands/update.test.ts (create the file if it doesn't exist; follow the colocated real-I/O pattern from other *.test.ts files): (a) merging a new key preserves existing keys; (b) merging an existing key overwrites just that key; (c) --clear-extensions drops the field; (d) --extensions with a JSON array errors out; (e) --extensions with malformed JSON errors out with a useful message. Do NOT change `sd create` in this step — defer create-time extensions to a follow-up."},{"title":"Add opt-in sd ready --respect-schedule flag","type":"feature","priority":2,"blocks":[5],"description":"Phase 4 (consumer surface). Extend src/filter.ts with `respectSchedule?: boolean` on IssueFilterOptions, and inside applyIssueFilters when the flag is true, exclude any issue where `extensions.queued === true` OR `extensions.scheduledFor` is a string that ISO-parses to a future Date relative to Date.now(). Robust to missing/malformed values: any non-string scheduledFor is ignored (not future). Wire the flag through filterOptionsFromFlags (read flags['respect-schedule']) and through src/commands/ready.ts (register `--respect-schedule` on the commander command, push it through to the args/flags pipeline). Default behavior MUST be unchanged: without the flag, ready ignores extensions completely. Tests in src/filter.test.ts (create if missing) or src/commands/ready.test.ts: (a) without flag, queued/scheduled-future issues remain in ready output; (b) with flag, queued===true is excluded; (c) with flag, scheduledFor in the future is excluded; (d) with flag, scheduledFor in the past is INCLUDED (it's already due); (e) malformed scheduledFor doesn't throw and doesn't filter. Do NOT touch sd list — only sd ready learns this flag in V1."},{"title":"Doctor schema check for malformed extensions","type":"feature","priority":3,"blocks":[5],"description":"Phase 5 (integrity). In src/commands/doctor.ts, add a new check named `extensions-schema` that flags any issue where `extensions` is present and is NOT a plain object (e.g., array, primitive, null). Place it next to the existing `label-schema` check; mirror its structure. Implement the matching --fix branch that drops the malformed extensions field (sets it to undefined). Update the hardcoded pass count in doctor.test.ts (mx-957e8f convention — bumping the doctor check count requires updating the expected-pass count in the test). Add focused tests for: malformed cases (extensions = []), passing case (extensions = {role:'x'}), and --fix behavior."},{"title":"Update README, CHANGELOG, CLAUDE.md to document extensions","type":"task","priority":2,"blocks":[],"description":"Phase 6 (docs). Update README.md: in the data model section, document Issue.extensions as an opaque consumer-owned bag; document the warren namespacing convention (top-level keys: role, scheduledFor, trigger, queued, lastRunId, lastRunAt) as an example, not a contract. Document `sd update --extensions <json>` (shallow merge) and `--clear-extensions`. Document `sd ready --respect-schedule` and the two well-known keys it reads. Update CHANGELOG.md [Unreleased] with a `### Added` entry for Issue.extensions, sd update --extensions, sd ready --respect-schedule, and the doctor extensions-schema check. Update CLAUDE.md (project root) data model section to list the new field. Bump src/commands/onboard.ts ONBOARD_VERSION (mx-c97ad9 convention) only if the new field is mentioned in the onboard snippet — otherwise leave it. Run `bun test && bun run lint && bun run typecheck` from seeds/ as the final gate. Do NOT bump the package version in this step — release is a separate /release flow."}],"risks":["Future first-class field collision: if seeds later promotes (e.g.) scheduledFor to a top-level Issue field, it will collide with consumer-namespaced extensions.scheduledFor. Mitigation: document the warren keys as a convention pinned to extensions.* — promotion to first-class would require a migration. Out of scope for this plan.","Shallow-merge surprises: a consumer expecting deep-merge for nested objects (e.g., extensions.lastRun.id vs extensions.lastRun.at) will overwrite the entire nested value when patching. Mitigation: document shallow semantics explicitly; consumers that need deeper structure should keep keys flat at the top level (lastRunId, lastRunAt) per the warren example.","Default-behavior drift on sd ready: if --respect-schedule were on by default, agents would silently lose visibility into queued items. Mitigation: opt-in flag for V1 (per the seed's open question); revisit default behavior only after warren ships and we have real usage data.","JSONL union-merge across branches with concurrent extension writes: two branches merging `extensions: {role:'a'}` and `extensions: {scheduledFor:'...'}` will produce two issue records that dedup-on-read collapses to last-occurrence-wins, losing one branch's update. Mitigation: same as today for label edits — acceptable per the existing merge model; document it. Consumers requiring lock-step writes should serialize through a single agent.","Doctor false-positives if a consumer ever writes a JSON array intentionally: we explicitly reject arrays at the top level to keep the merge model coherent. Mitigation: document `extensions must be a plain object` in README; this is a deliberate constraint, not a bug."],"acceptance":["Issue.extensions?: Record<string, unknown> declared in src/types.ts; bun run typecheck passes.","Round-trip test in src/store.test.ts: write an issue with non-trivial extensions (mix of scalars, ISO8601 strings, nested object), read it back, assert deep equality. Confirms the field is preserved byte-for-byte through .seeds/issues.jsonl.","sd update <id> --extensions '{\"role\":\"refactor-bot\"}' shallow-merges into existing extensions (other keys preserved). --clear-extensions removes the field. Malformed JSON or non-object input fails with a clear error. Covered by tests in src/commands/update.test.ts.","sd show <id> displays an Extensions: <key=value ...> line when present (default + plain formats). --format json includes extensions on the issue payload.","sd list --format json and sd ready --format json include extensions on each issue (no code change required; verify in a smoke test).","sd ready --respect-schedule excludes issues where extensions.queued === true or extensions.scheduledFor parses to a future Date. Default sd ready (no flag) is byte-identical to today.","sd doctor includes an extensions-schema check that flags non-object extensions; sd doctor --fix drops malformed values. Doctor test pass count is updated.","README.md, CHANGELOG.md ([Unreleased]), and root CLAUDE.md document the new field, the warren namespacing convention (as example), the shallow-merge contract, and the --respect-schedule flag.","bun test && bun run lint && bun run typecheck all pass from seeds/."]},"children":["seeds-f35a","seeds-e7ea","seeds-be14","seeds-614b","seeds-56ff","seeds-27d1"],"createdAt":"2026-05-10T16:57:24.830Z","updatedAt":"2026-05-10T17:26:27.345Z"}
{"id":"pl-43ff","seed":"seeds-3c89","template":"feature","status":"done","revision":1,"sections":{"context":"sd plan submit always spawns brand-new child seeds from steps[]. There is no way to adopt an already-open seed into a new or existing plan, nor to detach a seed from a plan without closing it. The seed↔plan link is just three Issue fields (plan_id, plan_step_index, requires_plan) plus the blocks/blockedBy edges between parent seed and children. Backref text in description is managed by src/plan-backref.ts:applyPlanBackref(), which already preserves surrounding content. The closest existing analogue is applyOverwrite (src/commands/plan.ts:751-947), which matches old children to new steps by title; we extend this with a more reliable match-by-seed-id path. Need bidirectional linking so agents can stage related open issues into a coherent plan instead of duplicating them.","approach":"Add three surfaces, all additive to the existing data model: (1) extend the submitted Step JSON schema with an optional existing_seed: string field so a step at submit time either adopts the named open seed or spawns a fresh child (today's behavior); (2) add `sd plan adopt <plan-id> <seed-id...> [--step <i>]` for post-submit adoption; (3) add `sd plan release <plan-id> <seed-id...>` as the inverse (detach without closing). Reuse applyPlanBackref for description handling and existing lock order (outer plansPath, inner issuesPath, mx-f29e43). Adoption is link-only: don't mutate status, assignee, labels, priority — those stay with the seed. Release strips the backref block via the seeds:plan-backref markers, clears plan_id + plan_step_index, and removes the parent seed↔child edges. The overwrite path is extended to match by existing_seed id first, then by step.title (preserves today's behavior for plans that don't use adoption).","alternatives":[{"name":"Adoption via sd update only (no plan-aware command)","rejected_because":"Setting plan_id with `sd update --extensions` is opaque and doesn't wire blocks/blockedBy edges or apply the backref block. Plans would have inconsistent children lists."},{"name":"Single sd plan add/remove command instead of adopt + release","rejected_because":"Verbs matter for agent ergonomics — 'adopt' and 'release' convey the link-only contract; 'add/remove' suggests creation/deletion."},{"name":"Always auto-adopt seeds whose title matches a step","rejected_because":"Title collisions are common and silent adoption is dangerous. Explicit existing_seed id is the safe contract."}],"steps":[{"title":"Extend Step JSON schema with existing_seed in src/plan-schema.ts","type":"task","priority":2,"blocks":[2,3,4,5]},{"title":"Wire existing_seed into fresh-submit path in runSubmit (src/commands/plan.ts)","type":"task","priority":2,"blocks":[3,6,7,8,9]},{"title":"Update applyOverwrite to match by existing_seed id first, then by title","type":"task","priority":2,"blocks":[7,8,9]},{"title":"Add sd plan adopt <plan-id> <seed-id...> [--step <i>] subcommand","type":"feature","priority":2,"blocks":[6,7,8,9]},{"title":"Add sd plan release <plan-id> <seed-id...> subcommand","type":"feature","priority":2,"blocks":[7,8,9]},{"title":"Tag adopted children with (adopted) marker in sd plan show human output","type":"task","priority":3,"blocks":[7,8,9]},{"title":"Update PLAN_SPEC.md to document adoption + release semantics, edge cases, lock order","type":"task","priority":2,"blocks":[9]},{"title":"Update README.md + CHANGELOG.md with new commands, JSON shape, and an end-to-end adoption example","type":"task","priority":3,"blocks":[9]},{"title":"Run quality gates: bun test && bun run lint && bun run typecheck","type":"task","priority":2}],"risks":["Adopting a seed that already has plan_id silently corrupts the source plan's children list — must reject without an explicit --reassign-from <pl> flag (deferred to a followup).","Overwrite path: if a step lists existing_seed but its id is already in plan.children as a fresh-spawned title-matched child, the matching precedence matters. Match by id first prevents this; tests must cover the precedence explicitly.","Release: stripping the backref block must use the exact seeds:plan-backref markers — string-based search/replace will eat user-edited content otherwise.","Title mismatch on adoption: seed.title wins over step.title. Agents writing plans by hand may be surprised; stderr warning is required."],"acceptance":["sd plan submit accepts a step shape with existing_seed; the named seed is linked into the plan without a new seed being created.","sd plan adopt <plan-id> <seed-id...> attaches the seeds, applies backrefs, wires edges, appends to plan.children, and bumps plan.revision.","sd plan release <plan-id> <seed-id...> detaches the seeds, strips backrefs, unwires edges, removes from plan.children, and bumps plan.revision; the seed itself remains open.","Adopting a closed seed is rejected with a clear error; adopting a seed that already has plan_id is rejected unless explicitly reassigned (out of scope here — reject for now).","applyOverwrite matches existing children by existing_seed id first, then by step.title; pre-existing overwrite-by-title behavior is unchanged when no step uses existing_seed.","sd plan show distinguishes adopted children from spawned children in human output (e.g. 'seeds-abcd (adopted)').","PLAN_SPEC.md documents the adoption + release lifecycle, the schema addition, and the edge-case decisions.","All quality gates pass: bun test && bun run lint && bun run typecheck."]},"children":["seeds-7002","seeds-24c6","seeds-99ae","seeds-2b93","seeds-2b8a","seeds-a3ab","seeds-e198","seeds-aef6","seeds-72a6"],"createdAt":"2026-05-14T05:46:33.505Z","updatedAt":"2026-05-14T06:53:48.012Z","name":"Adopt and release existing seeds in plans"}
{"id":"pl-a1d4","seed":"seeds-7164","template":"feature","status":"done","revision":1,"sections":{"context":"Seeds integration with pi (the coding-agent harness becoming the daily-driver runtime) is conventional today: the seeds onboarding snippet in CLAUDE.md tells the LLM to run `sd prime`, `sd ready`, `sd close` and we hope it remembers. It skips `sd close` on completion, never claims issues with `sd update --status in_progress`, and parses human output when it does shell out instead of using `--json`. A pi extension hard-wires the ritual into pi lifecycle events — session_start prime + status widget, structured `sd_*` tools so the LLM stops re-parsing human output, `#sd-*` autocomplete + reference expansion so issue IDs round-trip naturally in chat, and a cached ready list invalidated on writes. Investigation in os-eco/docs/pi-extensions-investigation.md §2; pi extension API surface validated against /opt/homebrew/lib/node_modules/@earendil-works/pi-coding-agent@0.74.0. The recently-closed seeds-e445 (commit 3187ba1) added typed sections to `sd prime --json` (`closeProtocol`, `rules`, `commandGroups`, `workflows`), so this extension can render structured panels and budget injection per-section instead of shoveling the markdown blob.","approach":"Ship inside @os-eco/seeds-cli (Option A from the investigation doc) — adds extensions/pi/index.ts + tools.ts + autocomplete.ts + lib/ files, plus a `pi` manifest, the `pi-package` keyword, and `peerDependencies` + `peerDependenciesMeta.optional: true` in package.json. Mirror mulch's distribution shape exactly (pl-5563) so the two extensions feel like siblings. Add a new `sd setup` command for parity with `ml setup` / future `cn setup` — `sd setup pi` becomes the first recipe, writes `.pi/settings.json` idempotently, validates `pi` is on PATH, and flips the onboarding marker variant. Config knobs live under a `pi.*` namespace inside `.seeds/config.yaml` via a new `pi` block in `src/config-schema.ts` (one source of truth, file-locked atomic writes, AJV-validated; warren's UI inherits it for free). Auto-prime consumes the new typed `sections` from `sd prime --json` (not the legacy `content` blob) so the extension can selectively inject `rules` + `closeProtocol` always and budget-gate `commandGroups` / `workflows`. The onboarding marker stays on the existing three-marker scheme (`<!-- seeds:start -->`, `<!-- seeds-onboard-schema:N -->`, `<!-- seeds-onboard:v<ver> -->`) — bump `ONBOARD_SCHEMA` from 4 to 5 and emit a `:pi` suffix on the schema marker when the recipe is installed, so existing outdated-detection logic doubles as install-state detection. The seven `sd_*` custom tools are thin shims over `sd <cmd> --json`; cache invalidation watches `.seeds/issues.jsonl` mtime (cheap, no JSONL diffing) and refreshes the autocomplete list on `agent_end` only when it changed.","alternatives":[{"name":"Inject the legacy `sd prime --json` `content` blob into systemPrompt","rejected_because":"seeds-e445 just landed typed sections precisely so consumers stop treating prime as opaque markdown. Using `content` would mean re-parsing markdown headings to drive any TUI panel and would burn the budget on sections the extension already knows are low-value (e.g. workflows during a short focused session). Sections-driven injection costs nothing extra and lets `pi.prime.sections` config gate what gets injected."},{"name":"Add `sd onboard --with-pi` instead of a new `sd setup` command","rejected_because":"mulch and (soon) canopy both have a `setup` command with per-runtime recipes (claude/cursor/codex/pi). Bolting pi into `sd onboard` diverges from that pattern and forces every future runtime recipe (sapling, overstory) into the same overloaded command. A dedicated `sd setup` is a small file, parallel structure across the ecosystem, and idiomatic for `--check` / `--remove` subflags. The onboarding marker logic stays in onboard.ts — setup calls into it."},{"name":"Sibling workspace package via bun workspaces (Option B from investigation doc)","rejected_because":"Version-locking with the CLI is the whole point. The extension is a thin shim over `sd ... --json`; if the CLI changes a JSON shape, the extension must change in the same PR. One tarball, one version, cannot drift. Option B is reserved for warren in the investigation doc."},{"name":"New `<!-- seeds-pi-onboard:v -->` marker parallel to the existing three","rejected_because":"Doubles detection logic and forces a migration. A `:pi` suffix on the existing schema marker (and an `ONBOARD_SCHEMA` bump) is a one-line change to onboard.ts, reuses `detectStatus`, and means a single source of truth for install state. Same idiom mulch chose in pl-5563."},{"name":"Per-tool-call write detection (track which `sd_*` tool ran)","rejected_because":"Misses writes from bash `sd close`, hand edits to .seeds/issues.jsonl, and merges from sibling worktrees. Stat-ing `.seeds/issues.jsonl` mtime on `agent_end` is one syscall, catches everything, and matches how seeds' own `sd sync` detects pending changes."},{"name":"Ship sd_label and sd_plan_* tools in V1","rejected_because":"Label management is rare enough that bash `sd label add <id> <label>` is fine for V1. Plan tools (sd_plan_prompt / sd_plan_submit / sd_plan_show) carry the multi-step plan JSON contract; wrapping them as tools deserves its own design pass to decide whether the LLM should be authoring plan JSON via tool args or via a slash-command-driven editor. Deferred to v0.2 — the V1 tool surface is the core CRUD + dep + search."},{"name":"Shadow config in .pi/seeds.json","rejected_because":"Would lose file-locking, schema validation, and multi-agent concurrency safety that `.seeds/config.yaml` already provides. `pi.*` namespace in the existing config inherits all of it, and warren's schema-driven config UI (R-10) gets pi knobs for free."}],"steps":[{"title":"Foundation: extension skeleton, peer deps, pi.* config schema, sd setup scaffolding","description":"Create `extensions/pi/` directory with index.ts, tools.ts, autocomplete.ts, lib/. Add `pi` manifest, `pi-package` keyword, `files: [\"src\", \"extensions\"]`, and `peerDependencies` + `peerDependenciesMeta.optional: true` for `@earendil-works/pi-coding-agent` and `typebox` to package.json — mirror mulch's pl-5563 step 1 exactly. Add a `pi` block to `src/config-schema.ts` covering `pi.auto_prime` (bool), `pi.status_widget` (bool), `pi.prime.sections` (array of section names to inject), `pi.cache.invalidate_on_write` (bool), with sensible defaults and titles for warren's UI. Bump `config.test.ts` golden snapshot. Add `src/commands/setup.ts` + `setup.test.ts` with the command scaffolding (parses `--check`, `--remove`, dispatches to a recipe map) but no recipes registered yet — the `pi` recipe lands in step 6. Wire `setup` into `src/index.ts` command router and `sd prime` command list.","blocks":[2,3,4,5,6]},{"title":"Session_start hooks: auto-prime + status widget + agent_end cache invalidation","description":"On `session_start` (events: startup | reload | new | resume | fork), shell `sd prime --json` and cache the parsed `sections` object. In `before_agent_start`, append a rendered subset (per `pi.prime.sections` config — default `[\"closeProtocol\", \"rules\"]`) to `event.systemPrompt`. System-prompt edits chain — append, never replace. Preload `sd ready --json` + `sd stats --json` and set the status widget via `ctx.ui.setStatus` to `sd: <n> ready / <n> in-progress / <n> blocked`. On `agent_end`, stat `<ctx.cwd>/.seeds/issues.jsonl` mtime; if changed since session_start (or last refresh), re-run `sd ready --json` + `sd stats --json` and update the widget. Persist the cached ready list via `pi.appendEntry(\"seeds-ready\", { ids, refreshedAt })` so `/reload` restores from `ctx.sessionManager.getEntries()`.","blocks":[7]},{"title":"Custom tools: sd_create, sd_ready, sd_show, sd_update, sd_close, sd_dep, sd_search","description":"Register seven custom tools via `pi.registerTool` inside `session_start`. Each is a thin shim over `sd <cmd> --json`, parsing the response and returning structured results to the LLM. Tool definitions live in `extensions/pi/tools.ts` with typebox schemas. `sd_create({ title, type?, priority?, description?, assignee? })`, `sd_ready({ type?, assignee?, label?, priority_max?, limit? })`, `sd_show(id)`, `sd_update(id, { status?, priority?, assignee?, extensions? })`, `sd_close(id, { reason? })`, `sd_dep({ action: \"add\"|\"remove\"|\"list\", issue, depends_on? })`, `sd_search({ query, status?, type?, limit? })`. Each tool sets `promptGuidelines` telling the LLM to prefer the tool over bash for that operation. Worktree-aware: all shellouts use `ctx.cwd`, not `process.cwd()`. After any write tool fires, invalidate the cached ready list immediately (don't wait for `agent_end`).","blocks":[7]},{"title":"Autocomplete: #sd-* completion provider + input-event reference expansion","description":"Port pi's `github-issue-autocomplete.ts` to `extensions/pi/autocomplete.ts`. Register an autocomplete provider triggered on `#` followed by `sd-` token. Source: the cached `sd list --json` (preloaded once per session, refreshed when the ready cache invalidates). Match strategy: prefix match on id first (`#sd-1a2b…`), then fuzzy on title. On `input` event, scan the message for `#sd-<4hex>` references; for each match, shell `sd show <id> --json` and append the result as an inline blob the LLM sees but the user doesn't (configurable via `pi.reference_expansion`). Cache hits within the same input event so repeated references in one message hit one shellout.","blocks":[7]},{"title":"Commands: /sd, /sd:ready, /sd:create, /sd:show, /sd:close, /sd:claim + currentIssueId state","description":"Register six slash commands. `/sd` prints a short reference (mirrors `sd prime` top bullets). `/sd:ready` renders the cached ready list in a TUI panel via `ctx.ui.custom`. `/sd:create` walks `ctx.ui.input` (title) → `ctx.ui.select` (type from enum, priority from enum) → confirm. `/sd:show <id>` renders `sd show <id> --json` in a panel. `/sd:close <id>` confirms then shells `sd close <id>`. `/sd:claim <id>` runs `sd update <id> --status in_progress`, sets `currentIssueId` in extension state via `pi.appendEntry(\"seeds-state\", { currentIssueId })`, and updates the status widget to prepend `working: <id>`. Autocompletion on `<id>` args uses the same cached list. Commands import from a `commands.ts` module, registered inside `session_start`.","blocks":[7]},{"title":"sd setup pi recipe + pi-aware onboarding marker variant","description":"Register the `pi` recipe inside `src/commands/setup.ts`. `sd setup pi` writes `.pi/settings.json` (creates or merges idempotently) adding the `@os-eco/seeds-cli` package entry, drops a default `.pi/seeds.json` with `enabled: true`, and updates the CLAUDE.md onboarding marker. `sd setup pi --check` returns `not_installed | up_to_date | outdated`. `sd setup pi --remove` reverses both changes. Bump `ONBOARD_SCHEMA` from 4 to 5 in `src/commands/onboard.ts`. Emit a `:pi` suffix on the schema marker (`<!-- seeds-onboard-schema:5:pi -->`) when the pi recipe is installed; emit the bare marker otherwise. Snippet body becomes a short pi-aware variant when installed (drops the `sd prime` instruction and the manual-quick-reference because the extension auto-injects them) and stays the current full snippet otherwise. `onboard.ts` reads install state from the marker suffix; setup.ts writes it.","blocks":[7]},{"title":"Tests + README","description":"Unit tests against a mock `ExtensionAPI` covering: session_start prime caching, before_agent_start systemPrompt append, status widget format, all seven tools (happy path + worktree resolution + error surface from non-zero `sd` exit), `#sd-*` autocomplete (prefix + fuzzy), input-event reference expansion (single ref, multi-ref dedup, no-match no-op), agent_end mtime detection + ready refresh, currentIssueId persistence across reload, and setup.ts (`pi` recipe write/check/remove idempotency + marker suffix toggle + ONBOARD_SCHEMA bump migration). Quality gates green: `bun test && bun run lint && bun run typecheck`. Write `extensions/pi/README.md` covering install (`pi install npm:@os-eco/seeds-cli` or `sd setup pi`), config (every `pi.*` key with defaults), tools (one-line per), commands (one-line per), autocomplete behavior, and troubleshooting (peer-dep optional warning, worktree behavior, double-prime if marker not migrated)."}],"risks":["Pi extension API drift — pinned to ^0.74.0; minor bumps may add or remove hook fields. Watch for breaking changes in session_start event shape, registerTool definition, sendMessage delivery modes, and the autocomplete provider contract. Same risk mulch's pl-5563 calls out.","Prime sections shape drift — `sd prime --json` sections (closeProtocol/rules/commandGroups/workflows) just landed in commit 3187ba1 and have no golden snapshot test guarding consumer-side stability. If section names or shapes change, auto-prime breaks silently — the systemPrompt append would emit a partial blob. Add a defensive parser in extensions/pi/lib/prime.ts that falls back to the legacy `content` field if `sections` is missing or malformed, and file a follow-up seed for a consumer-contract test.","peerDependenciesMeta.optional behavior — users who `npm install -g @os-eco/seeds-cli` for the CLI alone must not see peer-dep warnings about `@earendil-works/pi-coding-agent` or `typebox`. Verify with a clean install before merging step 1; same shape mulch pl-5563 step 1 validates.","Cache invalidation race with sibling worktrees / merges — mtime check on `.seeds/issues.jsonl` catches local writes and most external writes, but a `git merge` that fast-forwards while pi is idle won't trigger `agent_end`. The cached ready list will be stale until the next user prompt. Acceptable for V1; document in README and offer a `/sd:refresh` command if step 5's command surface has slack. Stronger fix is a filesystem watcher inside `session_start` — explicitly deferred.","Onboarding marker migration breakage — bumping `ONBOARD_SCHEMA` 4 → 5 marks every existing seeds-onboarded project as `outdated` until they re-run `sd onboard`. This is the intended migration path but it's a user-visible churn pulse. Make sure the snippet body change is meaningful (i.e. the pi-aware variant is genuinely shorter when installed) — otherwise we're bumping for nothing. Document the migration in the seeds CHANGELOG.","Reference expansion budget — `#sd-*` reference expansion shells `sd show --json` per unique reference and inlines the result. A user pasting a list of 20 IDs spawns 20 subprocesses and 20 blobs in the LLM context. Cap at a configurable `pi.reference_expansion.max_refs` (default 5) and surface a status-line warning when truncated. Configurable so warren-driven heavy-context sessions can raise it.","sd setup as new command surface — adding `sd setup` introduces a new top-level command needing entries in: command router (src/index.ts), `sd prime` command groups (the new command fits under agentIntegration), shell completions (completions.ts), and the `sd --help` listing. Easy to miss one and ship a half-wired command. Step 1 must touch all four; verify via the completions.test.ts golden file."],"acceptance":["`pi -e ./extensions/pi/index.ts` loads cleanly in this repo with no errors and no peer-dep warnings on `npm install -g @os-eco/seeds-cli`","After `session_start`, `ctx.getSystemPrompt()` contains the rendered `closeProtocol` + `rules` sections from `sd prime --json`, in that order, when `pi.auto_prime` is true","`pi.prime.sections = [\"rules\"]` set via `sd config set pi.prime.sections '[\"rules\"]'` causes only rules to inject on the next session_start","Status widget reads `sd: <n> ready / <n> in-progress / <n> blocked` after `session_start` and refreshes within 1s of `agent_end` when `.seeds/issues.jsonl` mtime changed","All seven custom tools (sd_create, sd_ready, sd_show, sd_update, sd_close, sd_dep, sd_search) are registered, visible in pi's tool list, and a happy-path call to each returns structured JSON the LLM can read without re-parsing","`sd_close` with a non-existent id returns a structured error including the underlying `sd` stderr, not a thrown exception","Typing `#sd-` in the input opens an autocomplete list of cached ready ids; selecting one inserts the full `#sd-<id>` token","Sending a message containing `#sd-<id>` causes the extension to inline `sd show <id> --json` as a hidden context blob, capped at `pi.reference_expansion.max_refs` (default 5) unique references per message","`/sd:claim <id>` sets `sd update <id> --status in_progress` and updates the status widget to prepend `working: <id>`; the prefix survives `/reload`","`sd setup pi` writes `.pi/settings.json` idempotently (second run is a no-op), updates the CLAUDE.md schema marker to `:pi` suffix, and `sd setup pi --check` returns `up_to_date`","`sd setup pi --remove` reverses both changes; `sd setup pi --check` returns `not_installed`","Snippet body in CLAUDE.md is the short pi-aware variant when the `:pi` suffix is present; bare snippet otherwise","Inside a `git worktree`, all `sd_*` tools and shellouts resolve `.seeds/` via `ctx.cwd` and operate on the main repo's issues, matching `sd` CLI behavior","`sd config schema` includes the new `pi` block with `auto_prime`, `status_widget`, `prime.sections`, `cache.invalidate_on_write`, and `reference_expansion.max_refs` properties, each with title + default; `config.test.ts` golden snapshot updated","`bun test && bun run lint && bun run typecheck` all green","`extensions/pi/README.md` exists with install, config, tools, commands, autocomplete, and troubleshooting sections"]},"children":["seeds-2774","seeds-3a2a","seeds-adb1","seeds-e9d0","seeds-5103","seeds-89d2","seeds-17a8"],"createdAt":"2026-05-15T20:59:11.099Z","updatedAt":"2026-05-15T22:07:01.270Z","name":"Build pi-seeds extension"}
{"id":"pl-23f6","seed":"seeds-045e","template":"feature","status":"approved","revision":1,"sections":{"context":"Agent dogfood audit of the newly-shipped @os-eco/pi-seeds extension (commit at HEAD on 2026-05-15) surfaced 12 findings. The extension is functional and the test suite is green (822 pass), but the surface that agents actually touch has rough edges that will cause friction the moment real LLM sessions hit it:\n\n• Doc/CLI mismatches that break the very first thing an agent tries (`sd config show pi` errors).\n• Tool-surface gaps where the close-protocol prime injection tells the LLM to run rituals that have no first-class tool (e.g. `sd sync`).\n• Single-id tools where the CLI is plural-aware (`sd_close`, `sd_show`).\n• Lifecycle quirks (silent no-op slash commands, stale status widget, `closeProtocol` hard-coding `bun test` in every project regardless of stack).\n• Reference-expansion regex that won't match the natural `#sd-<4hex>` shorthand an LLM is most likely to emit.\n\nLeft unaddressed, each one degrades the trust agents place in the extension — a silent no-op slash command or a tool that takes a single id when the LLM passed three is the kind of thing that quietly trains the model to fall back to raw Bash, defeating the whole point of shipping structured tools.","approach":"Land 12 small, independently-shippable child seeds, one per finding. Group by area so reviewer context stays tight:\n\n• 3 children touch `src/commands/config.ts` (positional path, unset GC, unset-path return shape) — same file, near-trivial edits, sequenced to avoid rebase pain.\n• 2 children update agent-facing prose (`sd prime`'s default closeProtocol; extension README sections on autocomplete + auto-prime visibility).\n• 4 children land in `extensions/pi/lib/tools.ts` (new `sd_sync` + optional `sd_list`/`sd_blocked`/`sd_stats`/`sd_label`; multi-id `sd_close`/`sd_show`; richer `sd_ready` description; `sd_update` no-fields error).\n• 1 child updates `extensions/pi/lib/commands.ts` (notify when commands disabled).\n• 1 child updates `extensions/pi/lib/autocomplete.ts` (shorthand reference regex + README note).\n• 1 child updates `extensions/pi/index.ts` + `lib/status.ts` (status widget refresh on before_agent_start, or add `/sd:refresh`).\n\nEach child is one PR, one test file delta, and shouldn't exceed ~150 LOC. Priority leans P1 (critical-path agent UX) for the bug-class findings, P2 for tool gaps, P3 for cosmetic doc edits. The umbrella seed (`seeds-045e`) tracks rollup status.","alternatives":[{"name":"Ship one mega-PR that touches every finding","rejected_because":"Twelve unrelated edits in one diff is unreviewable and rebases poorly. The seeds plan-per-child convention is specifically optimized against this — each child is independently mergeable, independently revertible, and lets us land the high-priority bug fixes (#1, #4, #6) without waiting on the cosmetic README updates."},{"name":"Defer the tool-surface gaps (#5, #6, #7, #8) until 'real agent usage data'","rejected_because":"Finding #6 (no `sd_sync` tool) is in the auto-injected closeProtocol every session — the gap is structural, not empirical. Waiting for data means every agent in the meantime either shells out to Bash (training the wrong reflex) or silently skips the sync step. Same with `sd_close` multi-id: the protocol literally says `sd close <id1> <id2> ...` and the tool can't satisfy that signature. Ship now, observe later."},{"name":"Add an `sd_*` tool for every CLI subcommand (full parity)","rejected_because":"Overkill. The audit identified specific gaps tied to the close-protocol / common workflows. Tools like `sd_doctor`, `sd_migrate-from-beads`, `sd_plan submit` are low-traffic and Bash-shelling them costs the LLM little. Keep the tool surface curated; expand if data says otherwise."}],"steps":[{"title":"Fix `sd config show <path>` doc/CLI mismatch — accept positional path","description":"Audit finding #1. `sd config show pi` errors with 'too many arguments for show', but the README and CLAUDE.md tell agents to run exactly that. The actual syntax is `sd config show --path pi`.\n\n**Fix:** make `sd config show` accept either `show <path>` (positional) or `show --path <path>` (current flag form). Positional wins if both are passed; emit a deprecation note if both differ.\n\n**Files touched:** `src/commands/config.ts`, `src/commands/config.test.ts`, `extensions/pi/README.md` (if any examples need updating).\n\n**Tests:** positional-only, flag-only, both-agree, both-disagree (positional wins).\n\n**Quality gates:** `bun test && bun run lint && bun run typecheck`. Done when an agent running `sd config show pi` gets sensible output and the README example works as written.","blocks":[2,3]},{"title":"Make `sd config show --path <unset>` return null instead of erroring","description":"Audit finding #3. Today `sd config show --path pi` (when no `pi` block exists) returns `{success:false, error:'Path not found: pi'}` and exits 1. The error string reads like the pi binary is missing, and any tool probing optional keys has to special-case the failure.\n\n**Fix:** return `{success: true, value: null, path: '<p>'}` for unset paths. Keep exit 0. Reserve `success: false` for malformed paths (e.g. `..`, empty string) or genuine config-load errors.\n\n**Why:** warren V2's schema-driven config editor (per seeds CLAUDE.md) probes pi.* keys to render the UI. The current shape makes 'unset' indistinguishable from 'config file corrupt'.\n\n**Files touched:** `src/commands/config.ts`, `src/commands/config.test.ts`. Update `extensions/pi/README.md` if it documented the old shape.\n\n**Tests:** unset top-level key, unset nested key, malformed path still errors, set key returns same shape as before.","blocks":[]},{"title":"Garbage-collect empty parent objects on `sd config unset`","description":"Audit finding #2. After `sd config set pi.reference_expansion.max_refs 3` then `sd config unset pi.reference_expansion.max_refs`, the YAML retains `pi: { reference_expansion: {} }` instead of cleaning up the now-empty parents.\n\n**Fix:** after deleting the leaf, walk up the dotted path and remove any object that has become empty. Stop walking at the root (don't delete project/version).\n\n**Edge cases:** sibling keys at the same level (don't collapse if siblings remain), arrays (don't apply — arrays aren't navigable via dotted path anyway), comments in YAML (the parser strips them — accept that as known limitation).\n\n**Files touched:** `src/commands/config.ts`, `src/commands/config.test.ts`.\n\n**Tests:** unset leaf cleans up empty parent; unset leaf with sibling preserves parent; unset whole subtree (already works) still works.","blocks":[]},{"title":"Drop Bun-specific commands from `sd prime` default closeProtocol","description":"Audit finding #4. `sd prime` ships `[ ] 3. Run quality gates:          bun test && bun run lint && bun run typecheck` as step 3 of the closeProtocol. The pi extension auto-injects this on every session_start, so agents in Python/Go/Rust seeds-tracked projects get told to run `bun test`.\n\n**Fix:** change the default step to a stack-neutral phrasing, e.g. `Run quality gates (tests, lint, typecheck per project conventions)`. Keep the slot for project-specific overrides via a future `.seeds/config.yaml` field (e.g. `prime.close_protocol.quality_gates_command`), but don't block this child on shipping the override — just remove the false universal.\n\n**Files touched:** `src/commands/prime.ts`, `src/commands/prime.test.ts`. Update `CLAUDE.md` rendered output if snapshot tests exist.\n\n**Tests:** rendered output no longer contains the literal `bun test` string in the default close protocol; structured `closeProtocol.steps[2]` still describes quality gates conceptually.","blocks":[]},{"title":"Add `sd_sync` LLM tool (and consider sd_list, sd_blocked, sd_stats, sd_label)","description":"Audit finding #6. The auto-injected closeProtocol ends with `sd sync && git push`. There is no `sd_sync` tool, so the LLM either shells out via Bash (defeating the structured-tool design) or silently skips the most-mentioned ritual.\n\n**Fix (this child):** add `sd_sync` with `{ status?: boolean, dryRun?: boolean }` schema, plus `sd_list` (parity with `sd_ready` filter flags but accepting `--status`, `--all`) and `sd_blocked` (zero args). Skip `sd_stats` / `sd_label` for this child — file follow-ups if usage data warrants.\n\n**Files touched:** `extensions/pi/lib/tools.ts`, `extensions/pi/lib/tools.test.ts`, `extensions/pi/README.md` (custom tools table), `extensions/pi/index.ts` (registerSeedsTools call).\n\n**Tests:** mirror the existing tool test pattern (sd_create / sd_ready) — exec invocation shape, --json appended, success + error paths.\n\n**Done when:** an agent running its close-protocol checklist can call `sd_sync` as a first-class tool and gets back `{success, command:'sync', staged:[...], committed:bool}` (or whatever the CLI's actual `sd sync --json` shape is — verify before authoring the schema).","blocks":[]},{"title":"Accept multiple ids in `sd_close` and `sd_show` tools","description":"Audit finding #5. The CLI supports `sd close <id1> <id2> <id3>` and `sd show <id1> <id2>`, and the closeProtocol checklist literally reads `sd close <id1> <id2> ...`. The agent tools take a single `id: string`, so batch-close = N tool calls.\n\n**Fix:** change the schema to accept either:\n  - `id: string` (preserve back-compat), OR\n  - `ids: string[]` (new, plural form)\n\nIf both are passed, prefer `ids` and concat. Forward the positional list to `sd close <ids…>` / `sd show <ids…>`. Single-id callers see no behavior change.\n\n**Files touched:** `extensions/pi/lib/tools.ts` (buildCloseTool, buildShowTool, schemas), `extensions/pi/lib/tools.test.ts`, `extensions/pi/README.md`.\n\n**Tests:** single id (back-compat), multi-id ids array, both passed (ids wins), neither passed (validation error), CLI receives positional list correctly.","blocks":[]},{"title":"Make `sd_update` with no fields return a validation error","description":"Audit finding #8. `sd_update <id>` with no other fields currently returns `success:true` and re-emits the unchanged issue. An LLM that forgot to pass the flag gets no signal that it did nothing — easy silent failure mode.\n\n**Fix:** in `buildUpdateTool.execute`, after the optional-flag pushes, check whether `args.length` is still just `['update', id]`. If so, return a structured error `{success:false, command:'update', error:'sd_update requires at least one field to change (status, priority, title, labels, etc.)'}` before shelling out.\n\n**Files touched:** `extensions/pi/lib/tools.ts`, `extensions/pi/lib/tools.test.ts`.\n\n**Tests:** sd_update with only id → structured error; sd_update with --status alone → still works; sd_update with --set-labels '' (empty string clears) → still works (don't regress the deliberate empty-string sentinel).","blocks":[]},{"title":"Surface plan-aware fields in sd_ready/sd_search tool descriptions","description":"Audit finding #7. `sd_ready` returns rich payloads including `plan_id`, `plan_status`, `plan_children` per issue, but the tool description just says `Returns { success, issues, count }`. Agents reading the tool list won't know plan context is in there.\n\n**Fix:** expand the `description` fields on `buildReadyTool` and `buildSearchTool` to mention that issues include plan_id / plan_status / plan_children when the issue is part of a plan, and label set when present. Same for `buildShowTool` which already mentions 'dependency status'.\n\n**Files touched:** `extensions/pi/lib/tools.ts`. No code logic change; description-only edit. Probably no new tests needed, but verify `tools.test.ts` doesn't snapshot descriptions in a way that breaks.\n\n**Done when:** the description string conveys 'plan context is available' without forcing the LLM to inspect raw output to discover it.","blocks":[]},{"title":"Notify (not silently no-op) when slash commands disabled via pi.commands=false","description":"Audit finding #9. The slash commands register at extension load and live forever; their handler stub `buildCommandDeps()` returns `undefined` when `pi.commands=false`, and `if (!deps) return` runs without notifying. The command appears in pi's autocomplete but does nothing.\n\n**Fix:** when `buildCommandDeps()` returns undefined inside a handler, call `pi.notify` (or whatever UI primitive is available at command-invocation time — the handler has the `ui` context indirectly via `sessionUi` if the session is active) with a one-line warning: `'/sd:ready disabled — set pi.commands: true in .seeds/config.yaml'`.\n\n**Files touched:** `extensions/pi/lib/commands.ts` (or `index.ts` where the deps-getter is closed over), `extensions/pi/lib/commands.test.ts`.\n\n**Edge case:** session_start hasn't fired yet (no UI context) — fall back to a stderr-style chat message or just no-op gracefully. Test both branches.\n\n**Done when:** toggling `pi.commands: false` and then pressing `/sd:ready` produces a visible warning instead of silence.","blocks":[]},{"title":"Add `#sd-<4hex>` shorthand to reference regex + document","description":"Audit finding #10. The reference regex `#sd-([A-Za-z][A-Za-z0-9_-]*-[A-Fa-f0-9]{4,})\\b` requires the full `<project>-<hex>` form. So `#sd-7164` (project-implicit) doesn't expand, even though that's the natural shorthand both humans and LLMs reach for. The autocomplete inserts the full token, masking the gap for typists but not for LLM-emitted text.\n\n**Fix:** extend the regex to also accept `#sd-<4+hex>` as a shorthand. Resolution: look up the bare hex in the local cache; if exactly one match, expand it; if zero or multiple, leave the token literal and surface a status warning. The full `#sd-<project>-<hex>` form remains canonical and gets autocompleted.\n\n**Files touched:** `extensions/pi/lib/autocomplete.ts` (`REF_REGEX`, `expandSeedsReferences`, `extractSeedsReferences`), `extensions/pi/lib/autocomplete.test.ts`, `extensions/pi/README.md` (Autocomplete & reference expansion section).\n\n**Tests:** bare 4-hex matches; bare 4-hex with ambiguous resolution emits a warning + leaves literal; full form still works; non-hex shorthand (`#sd-abcd` where abcd isn't hex) doesn't match.","blocks":[]},{"title":"Refresh status widget on before_agent_start (not just agent_end)","description":"Audit finding #11. The cache invalidation hook is on `agent_end`, so a `git pull` while pi is idle leaves the widget stale until the next agent turn completes. Better: also refresh on `before_agent_start` so the model that's about to run sees fresh counts.\n\n**Fix:** factor the mtime-stat + refresh body out of the `agent_end` hook (it's already a small block in `extensions/pi/index.ts`) and invoke it from `before_agent_start` as well. Guard against double-refresh: if mtime hasn't changed since the last refresh, skip. The existing `lastIssuesMtimeMs` already does this.\n\n**Files touched:** `extensions/pi/index.ts`. Optional: add a `/sd:refresh` slash command that forces a refresh independent of mtime (helps when the file system has the same mtime resolution as a busy sequence of writes).\n\n**Tests:** status.test.ts covers `readStatus` already. Add an integration-shaped test: simulate two before_agent_start events with mtime unchanged → only one refresh. Mtime bumped between them → both refresh.","blocks":[]},{"title":"Document auto-prime visibility + #sd reference gotcha in extension README","description":"Audit finding #12 (and partial #1 cleanup). The default auto-prime injection (~819 chars) lands in `systemPrompt` invisibly. Agents inspecting their own system prompt will see 'Session Close Protocol' from nowhere and may misattribute it.\n\n**Fix:** add a short subsection to `extensions/pi/README.md`:\n  - 'What gets injected' — point to `sd prime --compact` for inspection, list the default sections, note `pi.prime.sections: []` as the kill switch.\n  - Update the Autocomplete section to mention the `#sd-<project>-<hex>` canonical form vs the new shorthand (depends on the regex child, but the README edit can be sequenced after or merged with that step).\n  - Replace any remaining `sd config show pi` examples with the positional-or-flag form once step 1 lands.\n\n**Files touched:** `extensions/pi/README.md`. No code changes, no new tests.\n\n**Done when:** a fresh reader of the README understands (a) what's silently injected, (b) how to inspect it, (c) the reference shorthand caveat.","blocks":[]}],"risks":["Changing `sd config show` to accept a positional path could collide with a hypothetical future subcommand named the same as a config path (e.g. `sd config show pi-something`). Mitigation: positional is treated as `--path` if it doesn't start with `--`; reserve future subcommands by prefixing with verbs (`sd config diff`, not `sd config pi`).","Returning `{success:true, value:null}` from `sd config show --path <unset>` is a JSON-schema-breaking change for any tool today that special-cases `success:false` to mean 'unset'. Mitigation: warren's only consumer is still in development; ship the change with a CHANGELOG note and search the ecosystem (`rg 'success.*false.*config.*Path not found'`) for callers.","Multi-id `sd_close` could be misused by an LLM batch-closing the wrong issues. Mitigation: the close_reason is shared across the batch — keep that constraint, and document that batching is intended for 'I just finished issues A, B, C' not 'close everything matching X'.","Hard-coded `bun test` removal regresses the seeds repo's own CLAUDE.md if downstream relies on the literal string. Mitigation: this repo's CLAUDE.md already has its own `Build & Test Commands` block — verify the prime injection isn't being relied on as the source of truth here.","Multiple children touch `extensions/pi/lib/tools.ts` (steps 5, 6, 7, 8). Land them serially or pre-coordinate rebases. The umbrella seed should reflect this dependency in commits even though the plan steps don't strictly require `blocks` linkage."],"acceptance":["All 12 child seeds closed with passing quality gates (`bun test && bun run lint && bun run typecheck`).","`sd config show pi` works as documented (no error) in a fresh `sd init` project.","`sd config show --path pi` in a project without pi.* config returns `{success:true, value:null}` with exit 0.","`sd config unset pi.<deep.path>` leaves no orphan empty objects in `.seeds/config.yaml`.","`sd prime --json` no longer contains the literal string `bun test` in the default closeProtocol steps.","`sd_sync`, `sd_list`, `sd_blocked` registered as LLM tools with passing unit tests in `extensions/pi/lib/tools.test.ts`.","`sd_close` and `sd_show` accept `ids: string[]` and forward correctly to the CLI; existing single-id `id: string` callers still work.","`sd_update` with no field flags returns `{success:false, error: '...'}` instead of a silent re-emit.","Tool descriptions for `sd_ready` and `sd_search` mention plan-aware payload fields.","Pressing `/sd:ready` with `pi.commands: false` produces a visible notification, not silence.","`#sd-<4hex>` shorthand expands to the canonical issue when unambiguous; ambiguous matches surface a warning.","Status widget refreshes on `before_agent_start` when issues.jsonl mtime has changed since last refresh.","`extensions/pi/README.md` documents auto-prime injection visibility and the `#sd` reference shorthand caveat.","Umbrella seed `seeds-045e` closed with a summary commit referencing every child."]},"children":["seeds-f029","seeds-1bdd","seeds-6f43","seeds-19bd","seeds-52ac","seeds-c830","seeds-c969","seeds-ab15","seeds-02f6","seeds-46be","seeds-560e","seeds-f9af"],"createdAt":"2026-05-15T22:45:52.043Z","updatedAt":"2026-05-15T22:45:52.043Z","name":"pi-seeds audit follow-ups"}
{"id":"pl-dee8","seed":"seeds-a2de","template":"feature","status":"approved","revision":1,"sections":{"context":"After submitting a plan, the only way to change sections or step metadata is sd plan submit --overwrite, which requires re-submitting the entire plan JSON from scratch. This is high-friction for common edits: fixing a typo in the approach section, renaming a step, or changing a step's priority. Users end up either living with mistakes or going through the full overwrite ceremony. An in-place edit command (sd plan edit) would let users make targeted, field-level changes with a single CLI invocation, matching the ergonomics of sd update for issues.","approach":"Add a new sd plan edit subcommand with field-level flags: --name for the plan label, --section <name> <text> for replacing section content, and --step <i> combined with --title/--priority/--type for step metadata. The implementation follows the pattern established by runOutcome and runReview: resolve plan id via resolvePlanIdArg, acquire outer plans lock + inner issues lock, mutate in place, bump revision, write. Step metadata edits propagate to the corresponding child seed (looked up via plan.children[stepIndex]). Approach section changes trigger a backref refresh on all child seeds (reusing applyPlanBackref). Validation is lightweight — no full template re-validation, just type/range checks on individual fields. This avoids the complexity of structural edits (add/remove/reorder steps) which would require child spawning/orphaning logic and block-index renumbering.","alternatives":[{"name":"Interactive JSON editor (sd plan edit --interactive)","rejected_because":"Seeds is a CLI tool consumed by agents and scripts. Interactive editing requires a $EDITOR flow or TUI, adds complexity, and doesn't compose well with automation. Flag-based edits are atomic, scriptable, and testable."},{"name":"Extend --overwrite with partial merge semantics","rejected_because":"Overwrite is designed for full plan replacement with child matching. Adding partial-merge to it would complicate an already complex function (applyOverwrite is 280 lines) and conflate two different use cases. A separate edit command keeps concerns clean."},{"name":"Include structural edits (add/remove/reorder steps) in V1","rejected_because":"Structural edits require spawning/orphaning children, renumbering plan_step_index on all siblings, and rewriting step.blocks indices. This is the complexity of applyOverwrite and doesn't belong in a lightweight edit command. Users who need structural changes should use --overwrite."}],"steps":[{"title":"Register sd plan edit subcommand and implement --name editing","type":"task","priority":1,"blocks":[2,3]},{"title":"Implement --section editing with approach backref refresh","type":"task","priority":1},{"title":"Implement --step metadata editing with child-seed propagation","type":"task","priority":1},{"title":"Update docs: CLAUDE.md, README, PLAN_SPEC, prime, onboard","type":"task","priority":2}],"risks":["Approach section edits must refresh backrefs on all children — if a plan has many children this is O(n) issue writes, but plans rarely exceed ~10 steps so this is acceptable.","Step index addressing (--step <i>) uses 1-based indices to match step.blocks convention — must be consistent and well-documented to avoid off-by-one confusion.","Concurrent edits from multiple agents could race on the same plan — existing advisory lock model handles this (same as overwrite), but worth noting."],"acceptance":["sd plan edit <id> --name <text> updates plan.name, bumps revision, persists","sd plan edit <id> --section approach <text> replaces plan.sections.approach and refreshes backrefs on all child seeds","sd plan edit <id> --section context <text> replaces plan.sections.context without touching children","sd plan edit <id> --step 2 --title <text> renames step 2's child seed title and bumps revision","sd plan edit <id> --step 2 --priority 1 changes the child seed's priority","sd plan edit <id> --step 2 --type bug changes the child seed's type","Multiple flags in one invocation apply atomically (e.g. --name X --section approach Y)","All edits bump plan.revision and update plan.updatedAt","JSON output mode (--json) works for all edit operations","Invalid --step index (out of range) exits non-zero with clear error","Tests cover: name edit, section edit, approach backref refresh, step title/priority/type propagation, combined edits, out-of-range step, missing plan"]},"children":["seeds-9b12","seeds-21f2","seeds-64cf","seeds-d457"],"createdAt":"2026-05-26T18:34:14.072Z","updatedAt":"2026-05-26T18:34:14.072Z","name":"In-place plan editing"}
{"id":"pl-86aa","seed":"seeds-a3bd","template":"feature","status":"approved","revision":1,"sections":{"context":"The seeds test suite takes ~99s for 825 tests (8.3 tests/sec). Root cause: 22 of 37 test files invoke every CLI operation via Bun.spawn(['bun', 'run', 'src/index.ts', ...]), spawning an estimated 2,000-3,000 subprocesses per run. Each subprocess pays full Bun startup + TypeScript module resolution + commander parsing overhead. The 15 unit test files that import functions directly complete in ~2.5s at 130+ tests/sec — a 25x throughput difference. The top 6 files (plan.test.ts at 38s, label 6s, create 5.8s, unblock 5.2s, list 5s, dep 4.6s) account for 65s / 65% of total runtime. Critically, every command already exports a `run(args, seedsDir)` function — the tests simply don't use them. The refactor is a test-level change: swap subprocess spawning for direct function calls with stdout/stderr capture.","approach":"Build a lightweight in-process test harness that captures console.log/console.error/process.stdout.write/Bun.write(Bun.stdout) output, then convert command tests to call the exported run() functions directly instead of spawning subprocesses. Commands already export run(args, seedsDir?) — no production code refactor needed. A small set of CLI smoke tests (5-10) remain subprocess-based to verify the commander wiring, --help, --version, and exit codes. The conversion is mechanical and file-by-file, so each step is independently shippable and testable.","alternatives":[{"name":"Pre-compile a binary with bun build and spawn that","rejected_because":"Reduces per-spawn overhead but doesn't eliminate it; still ~1,000+ process spawns paying OS-level fork/exec cost"},{"name":"Use bun --preload to cache module resolution across spawns","rejected_because":"bun test doesn't support preload for spawned child processes; each subprocess still does full module resolution independently"},{"name":"Run test files in parallel with more workers","rejected_because":"bun already runs files in parallel; the bottleneck is sequential subprocess spawns within each file, not cross-file parallelism"},{"name":"Batch setup by sharing tmpdir across tests in a describe block","rejected_because":"Saves some beforeEach spawns but most subprocess calls are in test bodies; also couples tests and makes failures harder to isolate"}],"steps":[{"title":"Build in-process test harness for capturing command output","description":"Create src/test-harness.ts: a runCommand(args, seedsDir) helper that (1) monkey-patches console.log, console.error, process.stdout.write, and Bun.write to capture output into buffers, (2) calls the command's exported run() function, (3) restores originals in a finally block, (4) returns {stdout, stderr, exitCode} matching the subprocess-based run() signature so test migration is a drop-in replacement. Handle process.exitCode reads. Export a createTestDir() helper that does mkdtemp + returns cleanup function. Export an initTestProject(dir) that calls init's run() directly.","blocks":[2,3,4,5,6]},{"title":"Convert plan.test.ts to in-process (38s → ~2-3s target)","description":"The single largest bottleneck: 182 tests, 38s, ~460 subprocess spawns. Replace the file-local run() helper with the test harness import. Convert beforeEach sd-init calls to use initTestProject(). Convert all Bun.spawn([...CLI...]) calls to runCommand(). Verify all 182 tests pass. This step alone should cut suite time by ~35s.","blocks":[]},{"title":"Convert label, create, unblock, dep, list tests to in-process (~27s → ~2s target)","description":"The next 5 slowest files: label.test.ts (24 tests, 6s), create.test.ts (35 tests, 5.8s), unblock.test.ts (17 tests, 5.2s), list.test.ts (23 tests, 5s), dep.test.ts (16 tests, 4.6s). Same mechanical conversion: swap subprocess run() for harness runCommand(). These share an identical test structure pattern so the conversion is repetitive. Combined ~27s savings.","blocks":[]},{"title":"Convert remaining command tests to in-process (~30s → ~2s target)","description":"Convert the remaining 15 command test files: doctor (4.3s), tpl (3.3s), update (3s), format (2.8s), search (2.5s), config (2.1s), ready (2.1s), onboard (2s), plan-submit-record-decision (2s), show (1.7s), setup (1.5s), plan-mulch (1.4s), sync (1s), prime (0.65s), completions (0.3s). sync.test.ts needs special handling since it also spawns git commands — keep those as real spawns. plan-submit-record-decision.test.ts spawns a fake ml binary — keep that pattern but convert the sd calls.","blocks":[]},{"title":"Add CLI smoke test file for subprocess-level coverage","description":"Create src/commands/cli-smoke.test.ts with 5-10 tests that actually spawn the CLI binary. Cover: --help output, --version output, unknown command error + exit code, sd init + sd create + sd list round-trip, --json flag produces valid JSON, --quiet suppresses non-JSON output. This preserves confidence that the commander wiring and process-level behavior work correctly, without needing every test to pay subprocess cost.","blocks":[]},{"title":"Validate full suite performance and clean up","description":"Run full suite, verify all 825+ tests pass, measure total time (target: <15s). Run lint + typecheck. Remove any dead subprocess helper code from converted files. Verify CI passes. Document the test harness pattern in CLAUDE.md testing section so future tests use in-process by default.","blocks":[]}],"risks":["Console/stdout monkey-patching may miss output paths — some commands use process.stdout.write directly or Bun.write(Bun.stdout); the harness must intercept all four output channels","Commands that set process.exitCode need careful handling — the harness must snapshot and restore exitCode around each call to avoid test pollution","Chalk color output may differ between subprocess (which detects a pipe) and in-process (which detects a TTY) — tests that assert on ANSI codes may need NO_COLOR=1 or force-color env","sync.test.ts depends on real git operations (git init, git add, git commit) — those subprocess calls must remain; only the sd CLI calls should be converted","plan-submit-record-decision.test.ts creates a fake ml binary on PATH — the PATH manipulation works in-process but needs to happen before the run() call, not before a spawn"],"acceptance":["All existing tests pass (825+ tests, 0 failures)","Full suite completes in under 15 seconds (down from ~99s)","No subprocess spawning in command tests except cli-smoke.test.ts and explicitly justified cases (git in sync.test.ts, fake binaries in plan-mulch/record-decision tests)","bun test && bun run lint && bun run typecheck all pass","Test harness is documented and new tests default to in-process pattern"]},"children":["seeds-4770","seeds-efd7","seeds-807e","seeds-c709","seeds-d1ca","seeds-f78d"],"createdAt":"2026-05-26T19:11:06.266Z","updatedAt":"2026-05-26T19:11:06.266Z","name":"Test suite subprocess elimination"}
{"id":"pl-e5a8","seed":"seeds-576c","template":"feature","status":"approved","revision":1,"sections":{"context":"Warren's patrol agents (nightwatch, bugwatch) create seeds plans to decompose their automated work. Child seeds spawned from plan steps need to carry agent-name labels (e.g. 'nightwatch', 'bugwatch') so issues are traceable to the originating agent. Today, SubmittedStep in src/commands/plan.ts has no labels field — the only way to label plan children is post-hoc sd label add calls, which is fragile and verbose in agent prompts. Adding labels at plan-step level lets agents declare labels declaratively in the plan JSON and have them flow through to child seeds at spawn time.","approach":"Add an optional labels: string[] field to SubmittedStep and the STEP_SCHEMA in plan-schema.ts. Reuse the same normalization logic as sd label add (lowercase, trim, dedup). In the fresh-spawn path of runSubmit, set issue.labels from the normalized step labels. In the adoption path (both submit-time existing_seed and runAdopt post-submit), merge step labels additively into the adopted seed's existing labels using Set dedup. Validate in STEP_SCHEMA that labels is an array of non-empty strings. Keep the overwrite path consistent — new steps carry labels, matched children get labels merged. This follows the established pattern of how other step fields (type, priority, plan_template) flow from step definition to child seed.","alternatives":[{"name":"Top-level plan labels inherited by all children","rejected_because":"Too coarse — different steps may need different labels (e.g. one step tagged 'nightwatch' another tagged 'debt'). Per-step labels are more flexible and match the existing per-step type/priority model."},{"name":"Use extensions field instead of first-class labels","rejected_because":"Labels are already a first-class Issue field with normalization, filtering (sd list --label), and dedup semantics. Using extensions would bypass all of that and require consumers to re-implement normalization."}],"steps":[{"title":"Add labels to STEP_SCHEMA and SubmittedStep interface","type":"task","priority":1,"blocks":[2,3,4],"description":"In src/plan-schema.ts, add a 'labels' property to STEP_SCHEMA: { type: 'array', items: { type: 'string', minLength: 1 } }. In src/commands/plan.ts, add 'labels?: string[]' to the SubmittedStep interface. This is schema-only — no behavioral changes yet."},{"title":"Wire labels into fresh-spawn path of runSubmit","type":"task","priority":1,"blocks":[4],"description":"In runSubmit's fresh-spawn loop (both fresh-submit and overwrite paths), normalize step.labels using the same lowercase/trim/dedup logic as label.ts normalizeLabels, then set issue.labels on the spawned child Issue when labels are present and non-empty after normalization. Extract or reuse the normalizeLabels helper so it's importable from plan.ts."},{"title":"Wire labels into adoption paths (submit-time and runAdopt)","type":"task","priority":1,"blocks":[4],"description":"In the adoption branch of runSubmit (existing_seed path), merge normalized step.labels additively into the adopted seed's existing labels using Set dedup — same merge semantics as sd label add. Also update the runAdopt post-submit function to accept and merge labels when the step at --step index has labels. For overwrite adoption (matched children), merge labels additively."},{"title":"Add tests for step labels across all paths","type":"task","priority":1,"blocks":[5],"description":"In src/commands/plan.test.ts, add test cases: (1) fresh-spawn with labels — child seeds receive normalized labels. (2) Adoption with labels — labels merge into existing seed labels without duplicates. (3) Overwrite with labels — new step labels merge into matched children. (4) Validation rejects empty-string labels. (5) Labels normalize (uppercase→lowercase, whitespace trimmed, duplicates removed). (6) Steps without labels — children have no labels (backward compat)."},{"title":"Update CLAUDE.md and sd plan prompt output to document labels field","type":"task","priority":2,"description":"Update the CLAUDE.md plan command reference to mention the optional labels field on plan steps. Ensure sd plan prompt --json output includes labels in the step schema description so LLMs know the field exists when filling plan templates."}],"risks":["normalizeLabels is currently local to label.ts — extracting it to a shared location or duplicating the logic needs to avoid import cycles","Overwrite path label merge must not clobber labels that users added manually after initial spawn — additive-only merge is critical"],"acceptance":["sd plan submit with step labels spawns child seeds that have those labels (normalized) on the Issue","sd plan submit with step.existing_seed and step.labels merges labels additively into the adopted seed","sd plan submit --overwrite with step labels merges labels into matched children additively","STEP_SCHEMA rejects plans where a step label is an empty string after trimming","All existing plan.test.ts tests continue to pass (backward compatibility)","bun test && bun run lint && bun run typecheck all pass"]},"children":["seeds-7561","seeds-745e","seeds-bac9","seeds-9f86","seeds-2848"],"createdAt":"2026-05-26T19:23:11.958Z","updatedAt":"2026-05-26T19:23:11.958Z","name":"Plan step labels for child seeds"}
{"id":"pl-09b0","seed":"seeds-c1db","template":"refactor","status":"approved","revision":1,"sections":{"context":"Code-patrol scan of the seeds repo on 2026-05-27. bun test (784 pass), bun run lint (clean), and bun run typecheck (clean) all succeed. Beneath that green-bar the scan surfaced eight quality issues across consistency, correctness, and doc-drift surfaces — each small enough to fix as an isolated PR without touching public APIs.","behavior_invariant":"All eight fixes are internal restructuring under the no-feature/no-public-API patrol contract. After every step: (1) bun test, bun run lint, and bun run typecheck stay green; (2) every CLI command keeps the same flags, positional args, and exit-code semantics; (3) JSON output schemas only gain fields that bring divergent commands back in line — no field is renamed or removed; (4) on-disk storage in .seeds/ keeps the same shape and lock semantics. The patrol does not add capabilities, new commands, dependencies, or style-only reformatting.","approach":"Land one targeted fix per child seed in roughly the order the findings were discovered: start with the highest-impact correctness bug (stripAnsi escape), then the inconsistencies that cross multiple commands (search-vs-list/ready JSON, printWarning stream/quiet, --limit validation), then localized validation/safety guards (update title, self-block, onboard outdated branch), then the documentation drift in CLAUDE.md. All steps are independent except for the final Release step, which blocks on every preceding fix.","steps":[{"title":"Fix stripAnsi regex in src/format.ts to include the ESC byte","type":"bug","priority":1,"labels":["nightwatch"],"blocks":[9],"description":"src/format.ts:35 defines `const ANSI_REGEX = /\\[[0-9;]*m/g;` — note the missing ESC (\\x1b) prefix. The comment above the regex even asserts the pattern needs the ESC control character, but the literal does not. Result: `stripAnsi(\"\\x1b[31mred\\x1b[0m\")` returns `\"\\x1bred\\x1b\"` instead of `\"red\"`, so `--format plain` output and `output.plain()` leave orphan ESC bytes whenever chalk emits color (TTY mode). Reproduce in Bun: `bun -e 'const RE=/\\[[0-9;]*m/g; console.log(JSON.stringify(\"\\x1b[31mred\\x1b[0m\".replace(RE,\"\")));'` prints `\"\\u001bred\\u001b\"`. Fix: change the regex to `/\\x1b\\[[0-9;]*m/g` (matching src/commands/format.test.ts's `ANSI_RE = new RegExp(`${String.fromCharCode(27)}\\\\[[0-9;]*m`)`). Add a unit test in src/format.test.ts (new file) that calls `stripAnsi(\"\\x1b[31mhello\\x1b[0m\")` and expects `\"hello\"`, plus an idempotency case on already-clean input. Existing tests pass because chalk skips color in non-TTY subprocesses; the new direct test catches the regression."},{"title":"Annotate sd search --json with plan_status/plan_children like list/ready","type":"task","priority":2,"labels":["nightwatch"],"blocks":[9],"description":"src/commands/list.ts and src/commands/ready.ts both wrap each JSON-mode issue with `issueJsonWithPlan(issue, planForIssue(planCtx, issue))`, adding `plan_status` and `plan_children` when an owning plan exists. src/commands/search.ts (around line 117-125) emits the raw `issues` array with no plan annotation, so consumers calling `sd search --json` get a shape that diverges from list/ready for the same data. Fix: in search.ts, after `readIssues`, call `loadPlanContext(dir)` and reuse the same `issueJsonWithPlan` shape (lift the helper into a small shared module or inline a copy — keep the field names byte-identical). Update src/commands/search.test.ts (or add a case) to assert that a searched issue belonging to a plan carries `plan_status` and `plan_children` in --json. No changes to non-json output formats."},{"title":"Route printWarning to stderr and honor _quiet","type":"bug","priority":2,"labels":["nightwatch"],"blocks":[9],"description":"src/output.ts:29-32 — `printWarning` uses `console.log` (stdout) and does not check `_quiet`, while `printError` writes to stderr and `printSuccess` short-circuits on `_quiet`. Warnings on stdout pollute piped JSON/IDs output (e.g. `sd sync` worktree warning would land in a stdout pipeline). Fix: change `printWarning` to use `console.error` and add the `if (_quiet) return;` guard at the top, matching `printSuccess`'s shape exactly. Audit callers (`printWarning` is currently called from src/commands/sync.ts worktree branch) — verify none rely on stdout placement. Add a test in src/output.test.ts (if missing, add one) that asserts: (a) `printWarning` writes to stderr, (b) `setQuiet(true); printWarning(\"x\")` produces no stderr."},{"title":"Validate --limit input in list/ready/search","type":"bug","priority":2,"labels":["nightwatch"],"blocks":[9],"description":"src/commands/list.ts:62, src/commands/ready.ts:79, src/commands/search.ts:89 all use `Number.parseInt(limitStr, 10) || 50`. Two problems: (1) `--limit 0` silently becomes 50 (the `|| 50` swallows a valid zero); (2) negative integers and non-numeric strings other than 0 fall through — `--limit -3` calls `issues.slice(0, -3)` which drops the last three results without error. Fix: parse once into `n`, then if `Number.isNaN(n) || n < 0` emit the same JSON/plain error path the `--sort` validator uses (`{ success: false, command, error: ...}` + `process.exitCode = 1`). Choose one consistent semantics for 0 — recommend treating `--limit 0` as `0` (return empty set) rather than silently rewriting to the default. Mirror the validation in all three files; add a test per command verifying that `--limit -1` produces a clear error and that `--limit 0` returns an empty list without the 50-default surprise."},{"title":"Reject empty/whitespace --title on sd update","type":"bug","priority":3,"labels":["nightwatch"],"blocks":[9],"description":"src/commands/create.ts:50-52 rejects an empty/whitespace `--title`. src/commands/update.ts (around line 103) writes `patch.title = flags.title` with no trim or non-empty check, so `sd update <id> --title \"   \"` will clobber the title with whitespace. Fix: in update.ts, when `typeof flags.title === \"string\"`, trim it; throw `\"--title must not be empty\"` if the trimmed value is empty, otherwise set `patch.title = trimmed`. Add a test in src/commands/update.test.ts (the `sd update --extensions` describe block already shows the validation-error pattern to follow) covering the empty-string and whitespace-only cases."},{"title":"Reject self-block in sd block and sd dep add","type":"bug","priority":3,"labels":["nightwatch"],"blocks":[9],"description":"src/commands/block.ts (run, after positional parsing) and src/commands/dep.ts (run, `add` branch around line 71) do not check `issueId === blockerId` / `issueId === dependsOnId`. The current implementations happily write `issue.blockedBy = [issue.id]` and `issue.blocks = [issue.id]`, permanently self-blocking the issue (it never appears in `sd ready` again, and `sd unblock --from <self>` is the only escape). Fix: in both commands, throw a clear error (`\"An issue cannot block itself\"`) before acquiring the lock. Add a one-line test per command asserting the error is raised and no write happens (compare jsonl mtime or read-back state)."},{"title":"Surface onboard outdated-branch silent failure when replaceMarkerSection returns falsy","type":"bug","priority":3,"labels":["nightwatch"],"blocks":[9],"description":"src/commands/onboard.ts (lines around 100-115, status === \"outdated\" branch): when `replaceMarkerSection(content, snippet)` returns a falsy value, the function falls through to the end of the if-block, returns successfully, and writes nothing — no success message, no error, no non-zero exit code. The user sees `sd onboard` exit 0 with no output and assumes the file was updated. Fix: when `updated` is falsy, either throw `Error(\"Failed to update existing seeds section in <file> — markers malformed?\")` or print a descriptive error and set `process.exitCode = 1`. Add a test in src/commands/onboard.test.ts that constructs a CLAUDE.md with the schema version marker mangled so the replace path fails, and asserts a non-zero exit and a clear error message."},{"title":"Reconcile CLAUDE.md lock-constants doc with src/types.ts values","type":"task","priority":3,"labels":["nightwatch"],"blocks":[9],"description":"CLAUDE.md's Concurrency section (\"Advisory file locks (O_CREAT | O_EXCL, 30s stale, 50ms retry, 5s timeout)\") drifts from the actual constants in src/types.ts: LOCK_STALE_MS=30_000 (matches), LOCK_RETRY_MS=100 (doc says 50ms), LOCK_TIMEOUT_MS=30_000 (doc says 5s). Same string appears verbatim in CLAUDE.md only — README.md doesn't repeat it. Fix: update the CLAUDE.md sentence to read \"30s stale, 100ms retry, 30s timeout\" so it tracks the code. Do not touch the constants. Grep for the same drifted phrasing in docs/ and PLAN_SPEC.md just to be safe."},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"labels":["nightwatch"],"blocks":[],"description":"After all preceding patrol fixes land and quality gates are green, run the project's /release slash command (.claude/commands/release.md) to cut a patch release that bundles them."}],"acceptance":["bun test, bun run lint, and bun run typecheck all stay green after each child seed lands.","stripAnsi correctly removes real ANSI escape sequences (asserted in a new src/format.test.ts case).","sd search --json emits plan_status and plan_children on planned issues, matching sd list --json and sd ready --json.","printWarning writes to stderr and is suppressed under -q/--quiet.","--limit -1 and other invalid values produce a clear error path in list/ready/search; --limit 0 returns zero results instead of silently using 50.","sd update --title \"   \" exits non-zero with a clear message and leaves the issue untouched.","sd block <id> --by <id> and sd dep add <id> <id> exit non-zero with a clear self-block error and write nothing.","sd onboard never returns success with no action when the outdated-branch replace fails.","CLAUDE.md's lock-constants sentence matches the values in src/types.ts.","The Release step is executed last and only after every preceding patrol fix is merged."]},"children":["seeds-d6dd","seeds-92ef","seeds-c273","seeds-9d2a","seeds-7000","seeds-fd19","seeds-206f","seeds-b1d2","seeds-ce1c"],"createdAt":"2026-05-27T11:05:56.380Z","updatedAt":"2026-05-27T11:05:56.380Z","name":"nightwatch patrol 2026-05-27"}
{"id":"pl-f4dc","seed":"seeds-76cf","template":"bug","status":"approved","revision":1,"sections":{"context":"Agents and humans regularly guess `--label` on `sd create` because `sd list`, `sd ready`, and `sd search` already accept `--label` for filtering. Today commander rejects the unknown option, forcing a retry. This is a low-risk, agent-friendly UX fix: add `--label` as a hidden alias for `--labels` on `sd create` so both forms work without changing the canonical flag or documentation surface.","reproduction":"Steps to reproduce on main:\n1. `cd` into a seeds-initialized project (or run `sd init` in a tmp dir).\n2. Run `sd create --title \"x\" --label \"foo,bar\"`.\nObserved: commander exits non-zero with `error: unknown option '--label' (Did you mean --labels?)` and no issue is created.\nExpected: the issue is created with labels `[\"foo\", \"bar\"]`, identical behavior to passing `--labels \"foo,bar\"`.","root_cause":"src/commands/create.ts only declares `--labels <labels>` on the commander subcommand (line 128) and only reads `flags.labels` in `run()` (line 67). There is no `--label` alias, so commander short-circuits before the action runs. The fix is purely additive: declare a hidden `--label` alias on the commander surface and let `run()` fall back to `flags.label` when `flags.labels` is absent. No schema, storage, or formatter changes are required — labels are already comma-split, lowercased, and dedup-filtered downstream.","approach":"Two-file change, no public API break: (1) In `src/commands/create.ts`, register a hidden `--label <labels>` option using commander's `Option(...).hideHelp(true)` so `--help` output stays clean, and forward `opts.label` into the internal arg vector as `--labels <value>` (preferring `opts.labels` when both are passed, matching commander's last-wins semantics for the canonical flag). Also accept `flags.label` as a fallback in the in-process `run()` path so the test harness (which calls `run(args)` directly via the parseArgs helper) sees the alias. (2) Add a focused test in `src/commands/create.test.ts` covering both the commander entry point and the alias-only case. (3) Note the alias under `## [Unreleased]` in `CHANGELOG.md`. Rejected alternatives: renaming `--labels` to `--label` (breaking change for existing scripts); leaving the alias visible in `--help` (clutters the canonical surface — bug explicitly asks for hidden).","steps":[{"title":"Add hidden --label alias to sd create (commander + run parser)","description":"Edit `src/commands/create.ts`:\n\n1. Import `Option` from commander alongside the existing `Command` type import.\n2. In `register(program)`, replace the `.option(\"--labels <labels>\", \"Comma-separated labels\")` line with two registrations: the canonical visible `--labels` plus `program.command(...).addOption(new Option(\"--label <labels>\", \"Alias for --labels\").hideHelp())`. Concretely: keep the existing `.option(\"--labels <labels>\", ...)` and chain `.addOption(new Option(\"--label <labels>\", \"Alias for --labels\").hideHelp())` before `.option(\"--json\", ...)`.\n3. Extend the action's destructured opts type with `label?: string;` and, in the args-building block, push `\"--labels\", opts.label` when `opts.labels` is unset and `opts.label` is set (prefer the canonical flag if both are supplied).\n4. In `run()`, change the `labelsRaw` derivation (current line 67) from `typeof flags.labels === \"string\" ? flags.labels : undefined` to also fall back to `flags.label` when `flags.labels` is absent — e.g. `const labelsRaw = typeof flags.labels === \"string\" ? flags.labels : typeof flags.label === \"string\" ? flags.label : undefined;`.\n\nDo not change README, do not change the canonical `--labels` flag, do not alter the labels normalization pipeline (still comma-split, lowercased, deduped). Keep biome/tsc clean (`noUncheckedIndexedAccess`).","priority":2,"type":"task","labels":["bugwatch"],"blocks":[2]},{"title":"Add test coverage for --label alias on sd create","description":"Edit `src/commands/create.test.ts` to add a test case (use the existing in-process `runCli`/`runCliJson` harness, matching the style of neighboring tests) that:\n\n1. Runs `sd create --title \"alias check\" --label \"foo,bar\" --json` against a fresh tmp seeds dir.\n2. Asserts exit code 0 and that the created issue (looked up via the returned id) has `labels: [\"foo\", \"bar\"]` — i.e. byte-identical to the `--labels` path.\n3. Optionally add a second assertion: when both `--labels \"a\"` and `--label \"b\"` are passed, `--labels` wins (so this captures the precedence rule from step 1).\n\nDo not delete or rewrite existing `--labels` tests; this is additive. Follow the colocated real-I/O convention (mx-d82ab9): use `mkdtemp` via the existing test helpers, no mocks.","priority":2,"type":"task","labels":["bugwatch"],"blocks":[4]},{"title":"Note --label alias under Unreleased in CHANGELOG.md","description":"Edit `CHANGELOG.md`: under the existing `## [Unreleased]` heading (line 8), add a bullet under a `### Added` (or matching) subsection: `- `sd create` now accepts `--label` as a hidden alias for `--labels` (seeds-76cf).` Follow Keep a Changelog conventions (mx-b3a697). Do not touch any other version section, do not add link refs at the bottom (the existing Unreleased link suffices).","priority":3,"type":"task","labels":["bugwatch"],"blocks":[4]},{"title":"Release: run /release per .claude/commands/release.md","description":"Run the project's `/release` workflow as defined in `.claude/commands/release.md`. This step is blocked by all preceding steps; it should only run once the alias is implemented, tested, and changelogged.","priority":2,"type":"task","labels":["bugwatch"]}],"acceptance":["`sd create --title \"x\" --label \"foo,bar\"` exits 0 and produces an issue with labels [\"foo\",\"bar\"], identical to the --labels form.","`sd create --help` does NOT list `--label` (alias is hidden via Option.hideHelp()).","Both --labels and --label paths share the same comma-split / lowercase / dedup normalization.","When --labels and --label are both supplied, --labels wins.","`bun test src/commands/create.test.ts` passes including the new alias test.","`bun run lint` and `bun run typecheck` are clean.","CHANGELOG.md has an Unreleased entry referencing seeds-76cf."]},"children":["seeds-3222","seeds-354a","seeds-678b","seeds-a1e6"],"createdAt":"2026-05-27T16:02:03.480Z","updatedAt":"2026-05-27T16:02:03.480Z","name":"Accept --label alias on sd create"}
{"id":"pl-ca00","seed":"seeds-464f","template":"refactor","status":"approved","revision":1,"sections":{"context":"Code-quality patrol surfaced six small inconsistencies across the CLI: identical helper duplicated in three list-style commands, blocked output missing plan parity, drift risk between version.ts and package.json in the upgrade command, palette/style inconsistencies in upgrade output, unknown-command error skipping --json mode in the top-level dispatch, and the init gitattributes patch not checking each merge=union line independently.","behavior_invariant":"All existing CLI surfaces keep their current observable contracts: every command still accepts the same flags and exits with the same codes for the same inputs; --json payloads remain a strict superset (new keys may appear, but no existing key is renamed, removed, or retyped); human output keeps the forest palette (brand ✓, accent, muted) and printWarning/printSuccess/printError helpers; sd init keeps merge=union for all three JSONL files; sd doctor's check count is unchanged; bun test, bun run lint, and bun run typecheck all stay green.","approach":"Each finding lands as an isolated, single-file (or single-helper-extraction) PR. No public API changes, no new commands, no dependency edits. Where helpers exist (plan-context, output.ts, version.ts), prefer reusing them over inlining or re-reading sources. Tests are updated only where the observable behavior is touched (e.g. blocked --json now carries plan_status/plan_children when present; init re-runs add missing merge=union lines).","steps":[{"title":"Extract duplicated issueJsonWithPlan helper into plan-context.ts","type":"task","priority":3,"labels":["nightwatch"],"blocks":[7],"description":"Three command files contain a byte-identical copy of issueJsonWithPlan: src/commands/list.ts:155-164, src/commands/ready.ts:194-203, src/commands/search.ts:16-25. Confirmed via `diff` — bodies are identical. This is drift risk: any future change to the JSON shape (e.g. PLAN_SPEC adds plan_outcome) has to be mirrored across three files. Fix: move the function (and its return type) into src/plan-context.ts as a named export next to summarisePlanChildren / planLineSuffix (the natural home — plan-context.ts already owns plan-annotation helpers for these same call sites). Update the three command files to import it. No behavior change; existing tests for list/ready/search JSON output should continue to pass unchanged."},{"title":"Annotate sd blocked --json with plan_status/plan_children for parity with list/ready/search","type":"task","priority":3,"labels":["nightwatch"],"blocks":[7],"description":"src/commands/blocked.ts emits `{ success, command: 'blocked', issues: blocked, count }` in JSON mode (lines 36-43), but unlike sd list / sd ready / sd search (which all run each issue through issueJsonWithPlan and surface plan_status / plan_children), it ships the raw Issue array with no plan annotation. seeds-92ef already established the parity contract for search; blocked is the remaining outlier consumers (warren) hit. Fix: in src/commands/blocked.ts, loadPlanContext once after readIssues, and map blocked issues through the shared issueJsonWithPlan helper (extracted in step 1 if landed, otherwise duplicate the same shape pending step 1) in the json case. Add a test in src/commands/blocked.test.ts (create it if missing — there's no existing file) covering: (a) blocked issue with no plan -> JSON has no plan_status key (b) blocked issue whose plan_id resolves -> JSON has plan_status and plan_children matching the plan row. Human-mode output (plain/compact/default) is out of scope for this step."},{"title":"Use VERSION constant from src/version.ts in sd upgrade instead of re-reading package.json","type":"task","priority":3,"labels":["nightwatch"],"blocks":[7],"description":"src/commands/upgrade.ts:7-10 defines getCurrentVersion() which reads `../../package.json` via Bun.file and parses it. The project ships src/version.ts (single-line `export const VERSION = ...`) precisely so callers can read the version without CLI side-effects (see comment in src/index.ts), and CI verifies version.ts and package.json stay in lockstep. The upgrade path re-reading package.json is both a drift risk (if version.ts and package.json disagreed for any reason, upgrade would diverge from --version output) and dead weight given an in-tree import suffices. Fix: replace `getCurrentVersion()` with `import { VERSION } from '../version.ts'` and inline its uses; drop the now-unused new URL / Bun.file dance. Existing upgrade tests should continue to pass — verify by running src/commands/*.test.ts (or grep for an upgrade.test.ts; if none, no new test is required since the observable output is unchanged)."},{"title":"Use brand/printSuccess/printWarning helpers in sd upgrade output","type":"task","priority":3,"labels":["nightwatch"],"blocks":[7],"description":"src/commands/upgrade.ts uses chalk.green('✔') (lines 32, 52, 78) and chalk.yellow('!') (line 34) as ad-hoc colored prefixes, but the rest of the CLI standardizes on the forest palette via src/output.ts: brand('✓') for success, printWarning for warnings, printSuccess for success-with-message. Glyph also differs (✔ heavy check vs ✓ light check used everywhere else). Fix: replace the chalk.green('✔')-prefixed success lines with printSuccess('Already up to date (<ver>)') / printSuccess('Upgraded to <latest>'), and the chalk.yellow('!')-prefixed update-available line with printWarning('Update available: <current> → <latest>'). Keep the `checkOnly && !upToDate` branch's process.exitCode = 1 behavior. The `Upgrading <pkg> from <a> to <b>...` line (plain console.log) can stay plain — the goal is consistency on success/warning glyphs, not adding color to every line. JSON output is untouched."},{"title":"Emit JSON error on unknown-command branch in src/index.ts when --json is present","type":"task","priority":3,"labels":["nightwatch"],"blocks":[7],"description":"src/index.ts:148-168 detects an unknown first positional command and writes `Unknown command: <name>[. Did you mean <best>?]` to stderr as plain text, then sets exit code 1. The rest of the CLI honors `--json` for error paths (every command threads jsonMode through and the top-level main().catch() also emits JSON when --json is in argv). The unknown-command branch is the lone exception: a caller that runs `sd nosuchcmd --json` gets a plain-text stderr line and zero stdout, breaking JSON-only consumers. Fix: in the `!knownNames.includes(firstArg)` branch, mirror the main().catch() shape — when `rawArgs.includes('--json')` (the same check used at module top for the catch handler), write `JSON.stringify({ success: false, command: firstArg, error: 'Unknown command: <name>[. Did you mean <best>?]' })` via Bun.write(Bun.stdout, ...) and still set process.exitCode = 1; otherwise keep the existing stderr behavior. Add a test (extend src/suggestions.test.ts or a new src/cli-smoke.test.ts case) asserting the JSON shape for `sd nosuchcmd --json`."},{"title":"Append missing merge=union .gitattributes lines per-file in sd init","type":"bug","priority":3,"labels":["nightwatch"],"blocks":[7],"description":"src/commands/init.ts:47-56 reads an existing .gitattributes and short-circuits if it includes the substring '.seeds/issues.jsonl', appending all three merge=union lines only when issues.jsonl is absent. Edge case: a repo initialized by an older seeds version (or a user who hand-edited .gitattributes to add only the issues line) will permanently miss the plans.jsonl and templates.jsonl merge=union directives, so parallel-branch merges on those files won't auto-resolve. Fix: split the entry into three lines and, for each, check independently whether it's present (e.g. `existing.includes('.seeds/plans.jsonl')`); append only the missing ones, preserving a single trailing newline. Add a test in src/commands/init.test.ts that pre-creates `.gitattributes` containing only the issues.jsonl line and asserts `sd init` (when .seeds/ is also missing or being re-initialized) appends the plans.jsonl and templates.jsonl lines. Keep the existing fresh-init behavior (no .gitattributes -> write all three) byte-identical."},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"labels":["nightwatch"],"blocks":[],"description":"After all preceding patrol steps land, run /release per .claude/commands/release.md to cut a release covering this patrol's fixes."}],"acceptance":["bun test passes (all 811+ tests green, plus any new tests added in steps 2/5/6)","bun run lint reports no errors","bun run typecheck reports no errors","sd list, sd ready, sd search, sd blocked all return identical issue shapes in --json mode when a plan is attached to a surfaced issue (plan_status + plan_children keys present)","sd --version output equals the VERSION constant from src/version.ts; sd upgrade --check uses the same source","sd nosuchcmd --json emits a JSON error object on stdout and exits non-zero; sd nosuchcmd (no --json) still writes the plain stderr line","Running sd init twice in a repo whose .gitattributes is missing one of the three .seeds/*.jsonl merge=union lines results in all three lines present"]},"children":["seeds-455c","seeds-5f5c","seeds-a665","seeds-c9cc","seeds-8aa5","seeds-545e","seeds-6ac8"],"createdAt":"2026-05-28T11:06:53.834Z","updatedAt":"2026-05-28T11:06:53.834Z","name":"nightwatch patrol 2026-05-28"}
{"id":"pl-0f31","seed":"seeds-a417","template":"feature","status":"done","revision":1,"sections":{"context":"The seeds repo (this repo — @os-eco/seeds-cli, the `sd` CLI) is currently below Agent-Readiness Level 5. Warren (the os-eco flagship) and canopy (M1) are already at L5; the rest of os-eco — plot, mulch, burrow, and now seeds — needs to match warren's ceiling. Seeds is the cross-repo issue-tracking surface every other os-eco tool depends on, so closing this uplift unlocks ecosystem-wide L5. The 7 children below port the L5 toolkit (templates/l5-toolkit/ at the os-eco root) into seeds, baselining ratchet budgets from seeds' current state, then seal the milestone with a passing `check:all` and an interactive `/readiness-report` run from seeds/.","approach":"Sequentially apply 7 implementer steps, each closing a discrete band of VAL-SEEDS-* assertions from /Users/jayminwest/Projects/os-eco/docs/l5-uplift/validation-contract.md. Step 1 fills the governance baseline catch-up (dependabot/cooldown, PR template, issue templates). Step 2 ports the configs + .devcontainer + .env.example + .gitignore extension. Step 3 adds the file-size and debt-marker ratchets with seeds-specific tracker prefixes. Step 4 wires coverage + reporters + junit. Step 5 authors AGENTS.md, the validator, RUNBOOK.md, and the first .factory/skills/<name>/SKILL.md. Step 6 adds pino + pino-pretty with redact, the pre-commit hook + prepare script, and the labels.yml + sync workflow. Step 7 wires check:all into both package.json and ci.yml, runs the four-gate suite, and prompts the user to run /readiness-report. Each child uses adapt-don't-overwrite semantics: existing strengths (the large CLAUDE.md, any existing dependabot.yml that already has the cooldown block) are preserved or extended, not replaced.","alternatives":[{"name":"One mega-child that does everything","rejected_because":"Each band of work has its own verification surface and its own VAL-SEEDS-* assertions; mixing them yields a child too large for any single agent session to land cleanly and obscures which artifact closed which criterion when an audit fails."},{"name":"Mirror canopy's child layout exactly","rejected_because":"Canopy didn't need a governance baseline catch-up — it already had dependabot/PR template/issue templates pre-mission. Seeds needs an explicit first child for that work or VAL-SEEDS-001/002/003 stay open."}],"steps":[{"title":"seeds-baseline-catchup","type":"task","priority":1,"blocks":[2],"labels":["l5-uplift","seeds-port"]},{"title":"seeds-repo-scaffolding","type":"task","priority":1,"blocks":[3],"labels":["l5-uplift","seeds-port"]},{"title":"seeds-ratchet-scripts","type":"task","priority":1,"blocks":[4],"labels":["l5-uplift","seeds-port"]},{"title":"seeds-test-coverage-and-reporters","type":"task","priority":1,"blocks":[5],"labels":["l5-uplift","seeds-port"]},{"title":"seeds-agents-md-validator-runbook-skills","type":"task","priority":1,"blocks":[6],"labels":["l5-uplift","seeds-port"]},{"title":"seeds-pino-logger-and-governance","type":"task","priority":1,"blocks":[7],"labels":["l5-uplift","seeds-port"]},{"title":"seeds-check-all-and-ci-finalize","type":"task","priority":1,"blocks":[],"labels":["l5-uplift","seeds-port","milestone-seal"]}],"risks":["Overwriting the existing large CLAUDE.md or the existing seeds-specific .gitignore entries instead of extending them.","Baselining ratchet budgets too tight (smaller than observed) — the ratchet then fails on the very next innocuous commit.","Forgetting to wire a new check script into package.json's check:all aggregator OR into ci.yml — the auditor only credits criteria whose scripts are invoked end-to-end.","Treating the .github/dependabot.yml that already exists as 'done' without verifying it has the cooldown block (default 5d, major 14d, minor 5d, patch 3d) that closes min_release_age.","Hand-editing .seeds/issues.jsonl or .seeds/plans.jsonl — always go through `sd` commands so advisory locks and atomic writes apply.","Stopping before the user has pasted the final /readiness-report into .factory-mission-scratch/seeds-readiness-report.md — the milestone is sealed by that report, not by the implementer."],"acceptance":["All 7 child seeds are closed in dependency order with their per-child verification commands all exiting 0.","From /Users/jayminwest/Projects/os-eco/seeds/, `bun run lint && bun run typecheck && bun test && bun run check:all` all exit 0.","The user-run /readiness-report (saved to .factory-mission-scratch/seeds-readiness-report.md) shows `Level: 5 / 5` and every VAL-SEEDS-001..024 assertion is satisfied (file-existence checks pass on disk; criterion checks at numerator=denominator=1).","No commits are added by the planning step itself (this plan is open + open + open through to the milestone-seal child; the implementer agents own all commits)."]},"children":["seeds-f749","seeds-561e","seeds-e4f4","seeds-4731","seeds-d03c","seeds-92f2","seeds-317e"],"createdAt":"2026-05-28T03:19:17.081Z","updatedAt":"2026-05-28T18:03:19.430Z","name":"L5 uplift: seeds"}
{"id":"pl-b42c","seed":"seeds-42d8","template":"refactor","status":"approved","revision":1,"sections":{"context":"Code-patrol scan of seeds @ 2026-05-29 surfaced two production-affecting consistency/quality issues: (1) the top-level error handler in src/index.ts dynamically imports ./log.ts, which loads pino — but pino lives in devDependencies, so end-user installs of @os-eco/seeds-cli will throw 'Cannot find module pino' instead of reporting the actual command error; (2) the --json output contract is inconsistent: outputJson() emits 2-space-indented JSON, but three exit points in src/index.ts emit compact JSON for the same --json flag, so the shape callers see depends on which code path fires.","behavior_invariant":"All existing tests in bun test continue to pass. The `sd` CLI exit codes, command surface, public API signatures, and stdout/stderr JSON keys remain unchanged. Logging behavior under SEEDS_DEBUG=1 (when pino is installed) is preserved. --json callers continue to receive valid JSON on a single line break; the only observable change is uniform indentation across all --json exit points and graceful handling when pino is absent.","approach":"Two narrow, independent fixes followed by a release. Neither touches public APIs, command surfaces, or dependencies. Step 1 hardens the lazy pino import in the main() catch so a missing pino module no longer masks the real error. Step 2 normalizes --json formatting across the four exit points in src/index.ts and src/output.ts so the same flag emits the same shape. Step 3 releases.","steps":[{"title":"Make lazy pino import in main() catch resilient to missing module","type":"bug","priority":2,"labels":["nightwatch"],"blocks":[3],"description":"src/index.ts:187-199 wires `await import('./log.ts')` inside the global error handler. ./log.ts imports pino at module top, and pino is declared in devDependencies (package.json), not dependencies. For any non-dev install of @os-eco/seeds-cli, every thrown error from a command will trigger a 'Cannot find module pino' rejection that surfaces *instead of* the actual error message the user needs.\n\nFix: wrap the dynamic import + log.debug call in a try/catch so a missing pino is silently swallowed (debug observability is best-effort and only matters under SEEDS_DEBUG=1, which dev-style installs use). Do NOT move pino to dependencies — that violates the no-dependency-changes rule and AGENTS.md's 'minimal runtime dependencies' contract. Do NOT change the user-facing error message routing (stderr chalk red or JSON payload) — leave that exactly as-is.\n\nAdd a test in src/log.test.ts (or a sibling) verifying that when ./log.ts import throws synthetically (e.g. via a mock), the main catch still emits the user-facing error on stderr/stdout and sets exitCode=1. A minimal regression test is enough; no need to actually uninstall pino.\n\nFiles: src/index.ts (the main().catch block, ~lines 187-205). Tests: src/index.test.ts or extend src/log.test.ts. Acceptance: `bun test`, `bun run lint`, `bun run typecheck` all green; manual sanity that a real error still reaches stderr."},{"title":"Normalize --json indentation across all CLI exit points","type":"task","priority":3,"labels":["nightwatch"],"blocks":[3],"description":"The --json output contract is split between two styles:\n  - outputJson() in src/output.ts:17 emits `JSON.stringify(data, null, 2)` (2-space indent).\n  - Three exit points in src/index.ts emit compact `JSON.stringify(payload)` for the same --json flag:\n      * src/index.ts:140  --version --json\n      * src/index.ts:173  unknown-command path\n      * src/index.ts:198  main().catch error envelope\n\nCallers piping `sd ... --json` get different shapes depending on which path fires, which complicates parsing and snapshot tests.\n\nFix: pick ONE style and apply it everywhere. Recommended: keep outputJson()'s 2-space-indented form (current dominant style — confirmed by every src/commands/*.ts call site). Update the three src/index.ts emissions to either (a) route through outputJson() if convenient, or (b) inline `JSON.stringify(payload, null, 2)`. Trailing newline must be preserved at each site (currently all four append `\\n`).\n\nUpdate any tests that assert on the exact JSON form of --version --json, unknown-command --json, or the main-catch error envelope. Search: `grep -rn 'unknown command\\|did you mean\\|Cannot find\\|--version --json' src --include='*.test.ts'`.\n\nNo behavior change beyond whitespace. Do NOT alter JSON keys, key order, or success/error envelope shape. Do NOT change which stream (stdout vs stderr) each path uses.\n\nFiles: src/index.ts (lines ~138-144, ~166-175, ~187-202). Tests: update assertions in src/cli-smoke.test.ts and src/suggestions.test.ts if they pin compact-JSON layout. Acceptance: `bun test`, `bun run lint`, `bun run typecheck` green; manually run `bun src/index.ts --version --json` and `bun src/index.ts bogusCommand --json` and confirm both are indented like every other --json response."},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"labels":["nightwatch"],"blocks":[],"description":"Final patrol step. Run /release per .claude/commands/release.md (or RUNBOOK.md if the release.md file is absent) to ship the two fixes from steps 1 and 2. This step is blocked by every preceding step and must run last."}],"acceptance":["bun test exits 0 with all 914+ existing tests still passing.","bun run lint, bun run typecheck, bun run check:size, bun run check:debt, bun run check:agents all exit 0.","Manually invoking `bun src/index.ts --version --json`, `bun src/index.ts bogusCommand --json`, and a command that throws under `--json` all return JSON in the same indentation style as `bun src/index.ts list --json` (or any other outputJson() call site).","Simulating a missing pino module (e.g. test that mocks import('./log.ts') to throw) confirms the main() catch still surfaces the original error on stderr/stdout and exits 1 — the pino failure no longer eclipses the real error.","No new runtime dependencies added; package.json dependencies block unchanged."]},"children":["seeds-3287","seeds-49dd","seeds-248a"],"createdAt":"2026-05-29T11:04:36.101Z","updatedAt":"2026-05-29T11:04:36.101Z","name":"nightwatch patrol 2026-05-29"}
{"id":"pl-ee1e","seed":"seeds-2c5b","template":"refactor","status":"approved","revision":1,"sections":{"context":"Automated nightwatch patrol on 2026-05-30 surfaced four small quality issues in the seeds repo: (1) a real bug where `sd update --title` stores the user's title without trimming whitespace, while `sd create --title` already trims — same field, two storage shapes; (2) a JSON/non-JSON behavior mismatch in `sd upgrade --check` where the non-JSON path sets `process.exitCode = 1` when an update is available but the --json path does not; (3) dead code — `bunx knip` flags three unused exports (`writeConfig`, `isFormatMode`, `formatExtensionsLine`) and four unused exported types (`DomainSource`, `SectionRequest`, `PlanOutcome`, `ConvoyStatus`) that no caller imports; (4) configuration drift — `knip.json` emits 7 hints about stale ignore entries (dist/**, coverage/**, test-results/**, .seeds/**), no-match `extensions/**/*.ts` patterns, and a redundant `src/index.ts` entry. None of these are blocking but each is a small, isolated, in-scope fix. Lint, typecheck, all 919 tests, and quality-gate scripts are currently green; these changes must keep them green.","behavior_invariant":"All existing public CLI surfaces keep their current contracts. `sd update --title` still validates non-empty titles and rejects whitespace-only titles, exactly as today. `sd upgrade` (no --check) keeps its current behavior in both human and JSON modes. The `--json` envelope shape for every command stays as `{success, command, ...}` — no key renames, no removals from JSON payloads. `bun run lint`, `bun run typecheck`, `bun test`, `bun run check:size`, `bun run check:debt`, `bun run check:agents`, and `bun run check:coverage` all continue to exit 0. No public TypeScript signature changes, no new dependencies, no new CLI flags or subcommands.","approach":"Land each finding as a single small PR. Order: independent fixes first (trim bug, upgrade exit code, knip.json hints), then the dead-code removal (which is the largest churn), then the release step at the end. Each step touches a narrow file set, ships with the new/updated tests that lock in the fix, and runs the full gate suite locally before close. No abstractions introduced, no module boundaries moved. If during dead-code removal any export turns out to actually be referenced from outside the project (extensions, scripts), keep it and note it in the close reason instead of forcing the removal.","steps":[{"title":"Fix sd update --title to trim before storing","type":"bug","priority":2,"blocks":[],"labels":["nightwatch"]},{"title":"Make sd upgrade --check --json exit non-zero when update available","type":"bug","priority":3,"blocks":[],"labels":["nightwatch"]},{"title":"Clean up stale knip.json hints","type":"task","priority":3,"blocks":[],"labels":["nightwatch"]},{"title":"Remove unused exports and types flagged by knip","type":"task","priority":3,"blocks":[],"labels":["nightwatch"]},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"blocks":[],"labels":["nightwatch"]}],"acceptance":["`bun run lint`, `bun run typecheck`, and `bun test` all exit 0 after every step lands.","`bunx knip` reports zero unused exports and zero unused exported types after the dead-code step.","`bunx knip` reports zero configuration hints after the knip.json cleanup step.","A new test asserts `sd update --title '  hello  '` stores `title === 'hello'` (matches `sd create` trim behavior).","A new test asserts `sd upgrade --check --json` sets a non-zero process exit code when `current !== latest`, matching the non-JSON path."]},"children":["seeds-dfe7","seeds-15ae","seeds-c437","seeds-faab","seeds-f7ef"],"createdAt":"2026-05-30T11:06:12.067Z","updatedAt":"2026-05-30T11:06:12.067Z","name":"nightwatch patrol 2026-05-30 fixes"}
{"id":"pl-b3fe","seed":"seeds-7f14","template":"refactor","status":"approved","revision":1,"sections":{"context":"Routine code-patrol pass over the seeds repo. All quality gates (`bun test`, `bun run lint`, `bun run typecheck`) currently pass on main, but a scan of the CLI command surface, JSON-output contract, store layer, and test coverage turned up six concrete findings: 2 JSON-envelope inconsistencies, 1 missing input-validation bug, 1 unnecessary `as any` cluster, 1 test-coverage gap across five commands, and 1 file-lock race condition. None require API changes, dependency changes, or new commands — they are internal fixes only.","behavior_invariant":"After every step, the full quality-gate set must still pass: `bun run lint`, `bun run typecheck`, `bun test`, `bun run check:debt`, `bun run check:size`, `bun run check:coverage`, `bun run check:agents`. No public CLI surface change (no new commands, no removed flags, no removed JSON keys). All existing `--json` keys keep their names and types — the envelope fixes are purely *additive* (adding `success`/`command` alongside the existing payload key). The JSONL on-disk schema is unchanged. `sd plan submit` continues to spawn the same children for the same plan input. The advisory-lock contract (one writer, atomic temp-file rename) remains.","approach":"One small PR per finding, ordered so envelope/validation/type/test fixes (independent, low risk) land first and the lock-race correctness fix lands after the test scaffolding in step 5 makes regression testing easier. Final step is the standard release bundle.\n\nStep-by-step targets:\n\n1. `src/commands/plan.ts:404-407` — `if (jsonMode) { await outputJson({ plan_request: planRequest }); return; }` is the only `outputJson` call in the CLI that omits the `{success, command}` envelope. Change to `outputJson({ success: true, command: \"plan prompt\", plan_request: planRequest })`. Update plan.test.ts plan-prompt JSON assertions and any docs.\n\n2. `src/index.ts:135-148` — the `--version --json` early-exit emits `{ name, version, runtime, platform }` via `console.log(JSON.stringify(...))`. Wrap in `{ success: true, command: \"version\", name, version, runtime, platform }` and emit through `Bun.write(Bun.stdout, ...)` so the output path matches `outputJson`. Update cli-smoke.test.ts or add a small test for the new envelope.\n\n3. `src/commands/tpl.ts:97-101` — `tpl step add` reads `flags.priority` (default \"2\"), runs `Number.parseInt(priorityStr, 10)`, and stores the result unchecked. `Number.parseInt(\"abc\")` returns `NaN` and large numbers are accepted. Mirror the validation already in `src/commands/create.ts:62-65` and `src/commands/update.ts:122-125`: support `P0..P4` parsing AND range-check `0..4`, throw `--priority must be 0-4 or P0-P4`. Add tpl.test.ts cases for the rejection paths.\n\n4. `src/test-harness.ts:154,166,208` — three `(Bun as any).write` casts in the Bun.write monkey-patch. Replace with a typed local interface or a single `Bun.write`-typed alias so `as any` disappears. `bun run typecheck` must stay clean and `bun test src/test-harness.test.ts` must still pass.\n\n5. Co-located unit tests are missing for `src/commands/blocked.ts`, `src/commands/stats.ts`, `src/commands/migrate.ts`, `src/commands/close.ts`, and `src/commands/plan-show.ts`. Add `blocked.test.ts`, `stats.test.ts`, `migrate.test.ts`, `close.test.ts`, and `plan-show.test.ts` using the existing in-process test harness pattern (`src/test-harness.ts`). Each file needs at least one happy-path case and one error-path case (e.g. close non-existent id, stats on empty store, plan-show unknown plan id, migrate on non-beads dir, blocked with mixed open/closed blockers). `bun run check:coverage` must not regress and the new tests must pass `bun run check:agents`.\n\n6. `src/store.ts:18-43` `acquireLock` — when the lock file is detected as stale via `statSync` + mtime check, the code calls `unlinkSync(lock); continue;`. Between two concurrent processes both seeing a stale lock, process A's `unlinkSync` can remove process B's *freshly acquired* (non-stale) lock if B's openSync(wx) happens to win the race between A's stat and A's unlink. Fix by making the stale-claim atomic: e.g. rename the stale lock to a uniquely-named path before unlinking, and only proceed when the rename succeeds; or check the lock's mtime again after unlink and before the next openSync. Add a regression test in store.test.ts that simulates the concurrent-stale scenario.\n\n7. Release: run `/release` per `.claude/commands/release.md` — bumps version in lockstep via `bun run version:bump`, adds a CHANGELOG entry summarising the patrol, runs final gates, and produces a tag-ready commit.","steps":[{"title":"Wrap `sd plan prompt --json` output in standard {success, command} envelope (plan.ts:405)","type":"bug","priority":2,"blocks":[7],"labels":["nightwatch"]},{"title":"Wrap `sd --version --json` output in standard envelope; route through Bun.write (index.ts:135-148)","type":"bug","priority":3,"blocks":[7],"labels":["nightwatch"]},{"title":"Validate --priority range in `sd tpl step add` (tpl.ts:97-101)","type":"bug","priority":2,"blocks":[7],"labels":["nightwatch"]},{"title":"Remove `as any` casts from src/test-harness.ts Bun.write monkey-patch (lines 154, 166, 208)","type":"task","priority":3,"blocks":[7],"labels":["nightwatch"]},{"title":"Add co-located unit tests for blocked / stats / migrate / close / plan-show commands","type":"task","priority":2,"blocks":[7],"labels":["nightwatch"]},{"title":"Fix stale-lock TOCTOU race in src/store.ts acquireLock","type":"bug","priority":2,"blocks":[7],"labels":["nightwatch"]},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"blocks":[],"labels":["nightwatch"]}],"acceptance":["All quality gates green on each landed PR: `bun run lint`, `bun run typecheck`, `bun test`, `bun run check:debt`, `bun run check:size`, `bun run check:coverage`, `bun run check:agents`.","Every `--json` output that previously emitted a bare object (sd plan prompt, sd --version) now carries `success` and `command` keys, matching the rest of the CLI's JSON contract; no existing JSON key has been renamed or dropped.","`sd tpl step add --priority 99` and a non-numeric value are both rejected with the same error message shape `sd create` and `sd update` use; existing valid values (0..4, P0..P4) keep working and have test coverage.","src/test-harness.ts contains zero `as any` casts; `bun test` and `bun run typecheck` stay clean.","src/commands/blocked.ts, stats.ts, migrate.ts, close.ts, and plan-show.ts each have a co-located *.test.ts with at least one happy-path and one error-path case; `bun run check:coverage` does not regress.","src/store.ts acquireLock cannot remove another process's freshly-acquired lock: after detecting a stale lock, the unlink + re-open is atomic relative to other claimants, and a new regression test in store.test.ts demonstrates the prior race no longer fires.","Final Release step has bumped package.json + src/version.ts in lockstep via `bun run version:bump`, added a CHANGELOG entry summarising the patrol, and produced a tag-ready commit per RUNBOOK.md."]},"children":["seeds-1ea2","seeds-fb8e","seeds-6c54","seeds-84a3","seeds-643c","seeds-6c64","seeds-e91c"],"createdAt":"2026-05-31T11:07:32.839Z","updatedAt":"2026-05-31T11:07:32.839Z","name":"Nightwatch patrol 2026-05-31"}
{"id":"pl-78eb","seed":"seeds-67a6","template":"feature","status":"done","revision":1,"sections":{"context":"seeds already has the canonical terse check scripts (check:size/debt/dups/deps/agents/coverage) backed by l5-toolkit scripts and .jscpd.json/knip.json, but its check:all is a plain && chain that omits lint and typecheck and has no quiet runner, no `verify` alias, and no check:ci-parity. Conforming to docs/check-all-standard.md (os-eco-9048) means adopting the byte-identical runner (os-eco-5db7) and adding the missing gates. Depends on root tracker pl-760e steps 1-2.","approach":"Swap the && chain for the canonical quiet runner with a GATES manifest in the standard's order (folding in the already-present lint and typecheck scripts), then add the generalized ci-parity gate and the verify alias. seeds has no UI bundle or OpenAPI surface, so conditional gates are out of scope; only the core gate set applies.","steps":[{"title":"Add the canonical scripts/check-all.ts quiet runner copied byte-identical from templates/l5-toolkit/scripts/check-all.ts, with seeds's exported GATES manifest in the standard's order: lint, typecheck, check:agents, check:dups, check:deps, check:size, check:debt, check:coverage, check:ci-parity (last) -- note lint and typecheck must be ADDED to the manifest (they exist as scripts but are absent from the current && chain). Replace package.json check:all with `bun scripts/check-all.ts`, add `verify`: `bun run check:all`, and add scripts/check-all.test.ts. Confirm the quiet-output contract.","type":"task","priority":2,"blocks":[2]},{"title":"Add scripts/check-ci-parity.ts (with test) copied from templates/l5-toolkit/scripts, importing the GATES array from check-all.ts; add the check:ci-parity script as the final gate in the manifest. Reconcile any residual non-canonical gate name. Verify check:ci-parity passes against .github/workflows/ci.yml.","type":"task","priority":2,"blocks":[3]},{"title":"Wire .github/workflows/ci.yml to invoke the canonical gates (or `bun run check:all`) so the local manifest matches CI. Run `bun run check:all` and `bun run verify` green end-to-end with the quiet-output contract and a passing check:ci-parity.","type":"task","priority":2,"blocks":[]}],"acceptance":["check:all is `bun scripts/check-all.ts` with the byte-identical runner and a GATES manifest in the standard's order including lint and typecheck; verify aliases check:all.","scripts/check-ci-parity.ts imports GATES and passes against .github/workflows/ci.yml.","`bun run check:all` runs green with the quiet-output contract."]},"children":["seeds-3540","seeds-ef60","seeds-0483"],"createdAt":"2026-06-12T04:53:29.001Z","updatedAt":"2026-06-12T05:47:00.642Z","name":"Adopt canonical check:all standard (seeds)","outcome":"success","outcomeNote":"9/9 gates green; runner+parity byte-identical to l5-toolkit; biome bumped to ^2.4.14 for fleet baseline"}
{"id":"pl-1496","seed":"seeds-6804","template":"refactor","status":"approved","revision":1,"sections":{"context":"Static scan of seeds@HEAD on 2026-06-02 surfaced a small cluster of quality issues that have crept in around shared CLI plumbing: an unused export, duplicated parsing logic, inconsistent error-message phrasing across commands, one command (`sd sync`) that bypasses the project's standard print/JSON-error helpers, an edge-case bug in the JSONL appenders, and stale knip configuration entries. None of these are urgent on their own, but each is a 'broken-window' that pulls the codebase away from its own conventions (CLAUDE.md: single source of truth for parsing/validation, output.ts owns user-facing prints, no dead exports, knip stays clean). Baseline gates are green: `bun run lint`, `bun run typecheck`, `bun test` (953 pass), `bun run check:size`, `bun run check:debt` all pass; `bunx knip` reports the unused exports and config hints fixed here.","behavior_invariant":"Every user-observable behavior of the `sd` CLI stays identical: identical exit codes, identical JSON payload shapes and key order for every command, identical human-readable text (modulo the consolidated `Valid: a|b|c` wording for invalid-enum errors, which is itself a stated normalization), identical lock semantics in `src/store.ts`, identical `.seeds/issues.jsonl|templates.jsonl|plans.jsonl` on-disk format. `bun test` must stay at 953 pass / 0 fail (plus any new regression tests added for the appender bug), `bun run lint` / `bun run typecheck` / `bun run check:size` / `bun run check:debt` must stay green, and `bunx knip` must report zero unused exports and zero configuration hints when the patrol lands.","approach":"Land each finding as its own small PR. Order independent fixes first (dead code, knip config, error-message wording) and dependent fixes later. No public API changes, no new commands, no dependency churn. Every behavior-changing fix (appender newline normalization, sync.ts output routing) ships with a regression test that fails before the fix and passes after. The final step is the standard /release per .claude/commands/release.md, gated on every preceding step.","steps":[{"title":"Drop unused MIN_PRIORITY / MAX_PRIORITY exports in src/priority.ts","type":"task","priority":3,"blocks":[],"labels":["nightwatch","dead-code"]},{"title":"Clean stale knip.json entries (pino-pretty, bun-types)","type":"task","priority":3,"blocks":[],"labels":["nightwatch","config"]},{"title":"Normalize 'Valid: a|b|c' wording across enum validation errors","type":"task","priority":3,"blocks":[],"labels":["nightwatch","consistency"]},{"title":"Replace local parsePriority in src/commands/update.ts with shared src/priority.ts helpers","type":"task","priority":3,"blocks":[],"labels":["nightwatch","duplication"]},{"title":"Route sd sync output through printSuccess + outputJson error path","type":"task","priority":3,"blocks":[],"labels":["nightwatch","consistency"]},{"title":"Fix appendIssue/appendTemplate/appendPlan trailing-newline edge case in src/store.ts","type":"bug","priority":2,"blocks":[],"labels":["nightwatch","bug"]},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"blocks":[],"labels":["nightwatch"]}],"acceptance":["bun run lint, bun run typecheck, bun test, bun run check:size, bun run check:debt, bun run check:agents all exit 0 on the final state.","bunx knip reports zero unused exports and zero configuration hints.","A new regression test in src/store.test.ts proves that appendIssue against a file with no trailing newline produces a well-formed JSONL file (two records on two lines, not concatenated on one).","`sd sync` (success, no-op, and git-failure paths) emits the standard '✓ ...' brand line on stdout (or printError on stderr) and, when --json is set, emits the canonical {success, command, ...} payload — verified by tests in src/commands/sync.test.ts.","All 'Invalid --X value: <val>. Valid: …' error messages in src/commands/ and src/format.ts use the same delimiter (`|`), verified by grep + a focused test."]},"children":["seeds-cee8","seeds-3918","seeds-3df8","seeds-4b6a","seeds-b9fa","seeds-1b6b","seeds-5787"],"createdAt":"2026-06-02T11:06:34.084Z","updatedAt":"2026-06-02T11:06:34.084Z","name":"nightwatch patrol 2026-06-02"}
{"id":"pl-a847","seed":"seeds-1ea8","template":"refactor","status":"done","revision":1,"sections":{"context":"Code-patrol scan on 2026-06-09 surfaced five small quality issues across src/. (1) sd search lacks plan-annotation parity with sd list and sd ready in human/plain output. (2) The plan-suffix branch in sd list and sd ready writes via process.stdout.write and bypasses the -q/--quiet flag enforced through src/output.ts printIssueOneLine. (3) src/log.ts resolveLevel() casts SEEDS_LOG_LEVEL to pino.Level without validation, so an unknown value crashes logger init at import time. (4) src/priority.ts parsePriority + isValidPriority accept fractional input ('2.5' -> 2 via Number.parseInt) while src/filter.ts parsePriorityToken rejects it with a regex guard; the same flag is policed inconsistently across commands. (5) src/store.ts appendIssue / appendTemplate / appendPlan mutate JSONL files without acquiring the lock themselves and trust callers to wrap them in withLock — the contract is enforced by convention but absent from the JSDoc. None of these changes touch a public CLI flag, command name, or JSON payload key.","behavior_invariant":"User-visible CLI contract is preserved end-to-end: command names, flag names, exit codes, and --json payload shape (success, command, error, issues, count, query) stay identical to today. Human-format output keeps existing wording and counts. Priority semantics stay 0-4 / P0-P4 with the existing PRIORITY_ERROR message verbatim. Locking semantics and on-disk JSONL format are unchanged. Every test in src/ and scripts/ that passes today still passes; new regression tests cover the specific gaps being closed.","approach":"Land one small, focused PR per finding. Each PR adds or extends a co-located *.test.ts before changing behavior. No new dependencies, no module reorganization, no public-API signature changes. Inconsistencies are resolved by making the laxer call site match the stricter one (priority parsing) or by porting an existing helper (plan annotation in search). Quality gates (bun test, bun run lint, bun run typecheck, bun run check:size, bun run check:debt, bun run check:agents) must all pass on each PR. Per-step detail: STEP 1 — src/commands/list.ts default branch (~lines 145-158) and src/commands/ready.ts default branch (~lines 178-192) use `if (suffix) process.stdout.write(...) else printIssueOneLine(...)`; process.stdout.write skips the _quiet gate that printIssueOneLine respects. Fix: route the suffix-bearing line through an output.ts helper (e.g. extend printIssueOneLine to take an optional suffix, or add printIssueOneLineWithSuffix) so -q suppresses both branches. Add regression tests in list.test.ts and ready.test.ts using the existing test-harness capture. STEP 2 — src/commands/search.ts (~lines 135-165) calls issueJsonWithPlan for json mode but plain and default modes render via formatIssueOneLine without planLineSuffix, unlike list.ts and ready.ts. Port the same pattern: load planCtx (already imported elsewhere in the file or via loadPlanContext), build planForIssue, append planLineSuffix in plain and default. Add a search.test.ts case verifying a seed under an active plan renders the suffix. STEP 3 — src/log.ts resolveLevel() does `const explicit = process.env.SEEDS_LOG_LEVEL as pino.Level | undefined` — replace the bare cast with a check against pino.levels.values keys (trace/debug/info/warn/error/fatal/silent). Unknown values fall back to the SEEDS_DEBUG / info default. Add a log.test.ts case for an unknown level. STEP 4 — src/priority.ts parsePriority: after stripping the optional P/p prefix, validate the remaining substring matches /^\\d+$/ before Number.parseInt; return NaN otherwise so callers raise PRIORITY_ERROR. Mirrors src/filter.ts parsePriorityToken. Extend priority.test.ts to cover '2.5', '2a', ' 2 ', 'P2.5', and confirm '2' and 'P2' still pass. STEP 5 — src/store.ts: add JSDoc to appendIssue, appendTemplate, appendPlan stating 'Caller must hold withLock(<correspondingPath>(seedsDir), …) — these helpers do not acquire the file lock themselves; concurrent writes without the lock can lose data.' No code change; documentation-drift fix. STEP 6 — release per .claude/commands/release.md once all five fixes are merged.","steps":[{"title":"Fix -q/--quiet bypass in sd list and sd ready plan-suffix branch","type":"bug","priority":2,"blocks":[6],"labels":["nightwatch"]},{"title":"Add plan annotation parity to sd search (default + plain formats)","type":"task","priority":3,"blocks":[6],"labels":["nightwatch"]},{"title":"Validate SEEDS_LOG_LEVEL in src/log.ts resolveLevel()","type":"bug","priority":3,"blocks":[6],"labels":["nightwatch"]},{"title":"Reject fractional --priority input in src/priority.ts","type":"bug","priority":3,"blocks":[6],"labels":["nightwatch"]},{"title":"Document withLock contract on store append helpers (JSDoc only)","type":"task","priority":4,"blocks":[6],"labels":["nightwatch"]},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":3,"blocks":[],"labels":["nightwatch"]}],"acceptance":["bun test, bun run lint, bun run typecheck all exit 0 after every step","bun run check:size, bun run check:debt, bun run check:agents all exit 0 after every step","sd list -q and sd ready -q produce no stdout when matching issues carry plan suffixes (new regression test in list.test.ts / ready.test.ts)","sd search --format plain (and default) renders plan suffixes identically to sd list, verified by a new search.test.ts case","Setting SEEDS_LOG_LEVEL to an unknown value does not throw at logger init; falls back to info (new log.test.ts case)","sd create --priority 2.5 and sd tpl step add --priority 2.5 are rejected with PRIORITY_ERROR; existing valid 0-4 and P0-P4 values still accepted (new priority.test.ts cases)","src/store.ts JSDoc on appendIssue / appendTemplate / appendPlan explicitly states the caller must hold withLock on the corresponding file path","No public API surface (function signatures, CLI flags, --json payload keys) changes","/release executed successfully and CHANGELOG.md updated for the patrol fixes"]},"children":["seeds-6848","seeds-350d","seeds-96f0","seeds-8f1d","seeds-5827","seeds-a15c"],"createdAt":"2026-06-09T11:05:56.593Z","updatedAt":"2026-06-09T11:32:51.756Z","name":"nightwatch patrol 2026-06-09 fixes"}
{"id":"pl-a786","seed":"seeds-2718","template":"refactor","status":"done","revision":1,"sections":{"context":"Nightwatch patrol on 2026-06-16 found three small consistency gaps. (1) sd list/search accept arbitrary --status values and sd list/search/ready accept arbitrary --type values without validation — invalid input silently filters to an empty result instead of erroring the way sd create/update/tpl/plan do (cf. src/commands/update.ts:87-88 for --status, src/commands/update.ts:117-118 for --type). (2) src/index.ts:194 emits the unknown-command JSON error via raw writeStdout(JSON.stringify(payload, null, 2)) instead of routing through outputJson(payload) like every other JSON output site. (3) src/index.ts declares `const jsonMode = process.argv.includes(\"--json\")` *after* the `main()` function definition even though main() reads jsonMode through closure — a TDZ-fragile ordering that should be hoisted above main(). All other gates are green (bun test 993/993, bun run check:all 9/9).","behavior_invariant":"Existing valid invocations of sd list/search/ready and the unknown-command JSON error path must keep emitting byte-identical output. Specifically: (a) sd list/search/ready with valid --status (open|in_progress|closed) and valid --type (task|bug|feature|epic) still pass through unchanged; (b) the unknown-command JSON envelope shape stays { success: false, command, error, suggestion? } with two-space JSON.stringify indentation and a trailing newline; (c) all 993 existing tests in bun test continue to pass; (d) bun run check:all stays 9/9.","approach":"Surgical edits, no architecture changes. Each step is a single small PR that mirrors an existing pattern already in the codebase (the same validation idiom used by sd update, the same outputJson() helper used by every command). Keep the new --status / --type error messages in the same `Invalid --<flag> value: X. Valid: a|b|c` shape as src/commands/update.ts so the wording stays uniform across the CLI surface.","steps":[{"title":"Validate --status and --type values in sd list, search, and ready","type":"bug","priority":2,"labels":["nightwatch"],"blocks":[3]},{"title":"Tidy src/index.ts unknown-command JSON path: use outputJson() and hoist jsonMode declaration above main()","type":"task","priority":3,"labels":["nightwatch"],"blocks":[3]},{"title":"Release: run /release per .claude/commands/release.md","type":"task","priority":2,"labels":["nightwatch"],"blocks":[]}],"acceptance":["bun test passes (>=993 tests, 0 failures)","bun run check:all is 9/9","sd list --status bogus, sd list --type bogus, sd search foo --status bogus, sd search foo --type bogus, sd ready --type bogus all exit 1 with `Invalid --<flag> value: bogus. Valid: ...` on stderr (and a {success:false, command, error} JSON envelope under --json)","Each updated command has at least one test covering the new validation failure on both human and --json output","Unknown-command JSON path in src/index.ts goes through outputJson() and `jsonMode` is declared above the `main()` function"]},"children":["seeds-d305","seeds-e93c","seeds-47c1"],"createdAt":"2026-06-16T11:07:52.499Z","updatedAt":"2026-06-16T11:20:00.484Z","name":"nightwatch patrol fixes 2026-06-16"}
{"id":"pl-611d","seed":"seeds-cfc7","template":"feature","status":"approved","revision":1,"sections":{"context":"`sd plan list --status <s>` only does an exact status match, so there is no way to ask for \"every plan except done\". Plan status is derived from children in src/plan-lifecycle.ts (draft | approved | active | done; done means all children closed). Agents and humans need a one-shot view of plans that still have work, i.e. status !== \"done\". This adds `sd plan ready` to fill that gap, mirroring the existing `sd plan list` filters and output.","approach":"Add a `plan ready` subcommand in src/commands/plan.ts that reads plans, filters to status !== \"done\" (strictly not-done; draft/approved/active all shown), applies the same passthrough filters as `plan list` (--seed, --template, --outcome) plus --json, sorts newest-first, and renders via a shared output helper extracted from runList so `plan ready` and `plan list` stay byte-identical in format. Chosen over adding a `--not-done`/negation flag to `plan list` because a named `ready` verb mirrors the top-level `sd ready` mental model and keeps `plan list` filters purely positive/exact. Documentation and a patch release close out the plan.","steps":[{"title":"Extract shared plan-list rendering helper in src/commands/plan.ts","type":"task","priority":2,"blocks":[2],"labels":["plan-ready"]},{"title":"Implement and register `sd plan ready` (status !== done) in src/commands/plan.ts","type":"feature","priority":2,"blocks":[3,4],"labels":["plan-ready"]},{"title":"Add tests for `sd plan ready` covering not-done filter, passthrough filters, --json, and empty case","type":"task","priority":2,"blocks":[5],"labels":["plan-ready"]},{"title":"Document `sd plan ready` in CLAUDE.md, AGENTS.md, sd prime, and CHANGELOG [Unreleased]","type":"task","priority":2,"blocks":[5],"labels":["plan-ready"]},{"title":"Run .claude/commands/release.md flow with a patch version bump","type":"task","priority":2,"blocks":[],"labels":["plan-ready"]}],"acceptance":["`sd plan ready` lists plans with status draft, approved, or active, and excludes status done.","`sd plan ready --json` returns the same shape as `sd plan list --json` (success, command, plans, count).","`sd plan ready` honors --seed, --template, and --outcome passthrough filters and sorts newest-first.","`sd plan list` human + JSON output is unchanged (byte-identical) after the shared-helper extraction.","bun run check:all passes and a patch release is cut via the release flow."]},"children":["seeds-03f2","seeds-25de","seeds-80eb","seeds-347c","seeds-8b2c"],"createdAt":"2026-06-20T19:08:27.726Z","updatedAt":"2026-06-20T19:08:27.726Z","name":"sd plan ready"}
